---
title: "LWDrive: Layer-Wise World-Model-Guided Vision-Language Model Planning for Autonomous Driving"
title_zh: "LWDrive: 基于分层世界模型引导的视觉-语言模型自动驾驶规划"
authors: "Chen Yang, Yuhao Wei, Ze Xu, Ziheng Zou, Shuang Liang, Delin Ouyang, Lingfeng Qi, Jie Li, Guofa Li"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29879v2"
tags: ["query:world-models"]
score: 9.0
evidence: 逐层世界模型引导的视觉语言模型自动驾驶规划
tldr: 视觉语言模型直接输出的轨迹仅含粗略意图，缺乏几何精度与未来感知。本文提出LWDrive框架，将VLM输出作为粗计划，扩展候选空间并通过未来帧生成监督学习前瞻表示。Foresight Cascade Planner结合多层VLM特征、历史状态和多视图BEV特征，实现粗到细的轨迹优化。在NAVSIM和NAVSIM-v2上分别取得92.0和89.6分，验证了方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29879v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1773, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 1185, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1682, \"height\": 549, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29879v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 877, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1849, \"height\": 553, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 307, \"label\": \"Table\"}]"
motivation: 直接由VLM生成的轨迹缺乏几何精度和未来感知，无法满足多视图融合的精确规划需求。
method: 以VLM粗计划为起点，扩展候选空间，利用未来帧监督和FCP模块逐层细化轨迹，融合多视图BEV和历史状态。
result: 在NAVSIM基准上取得92.0分，在NAVSIM-v2上取得89.6分，优于先前方法。
conclusion: 层式世界模型引导的VLM规划有效提升了轨迹的几何准确性和未来感知能力，实现了粗到细的优化。
---

## 摘要
视觉-语言模型为端到端自动驾驶规划提供了强大的语义理解和常识推理能力。然而，由视觉-语言模型直接生成的轨迹通常仅编码粗略的驾驶意图，在几何精度、未来感知和多视角接地规划方面仍显不足。为解决这些限制，我们提出了分层世界模型引导驾驶框架（LWDrive）。LWDrive是一种通过分层世界模型引导来优化粗轨迹的视觉-语言模型规划框架。它不将视觉-语言模型输出视为最终轨迹，而是将其作为意图感知的粗规划，在其周围扩展多样化的候选空间，并通过前瞻级联规划器（FCP）逐步优化候选轨迹。具体而言，我们引入未来帧生成监督，以鼓励视觉-语言模型学习前瞻性场景表示，从而将规划相关的预测动态注入其内部隐藏状态。基于这些由世界模型监督的表示，FCP利用跨多个视觉-语言模型层的特征，并结合历史时间状态、动作查询表示和当前帧多视图鸟瞰图特征，以从粗到细的方式优化候选轨迹。这种设计能够在保留大模型产生的高层驾驶意图的同时，通过多视角场景线索逐步修正空间位置和运动趋势，并实现轨迹优化的接地。最后，一个评分头评估优化后的候选轨迹，并选择最佳轨迹作为最终规划输出。实验表明，LWDrive在NAVSIM基准上得分为92.0，在NAVSIM-v2上得分为89.6。代码和模型将公开提供。

## Abstract
Vision-Language Models (VLMs) provide powerful semantic understanding and commonsense reasoning for End-to-End Autonomous Driving (E2E-AD) planning. However, trajectories directly generated by VLMs often encode only coarse driving intentions and remain insufficient for geometrically accurate, future-aware, and multi-view-grounded planning. To address these limitations, we develop the Layer-Wise World-Model-Guided Driving framework (LWDrive). LWDrive is a VLM planning framework that refines coarse trajectories through layer-wise world-model guidance. Instead of treating the VLM output as the final trajectory, LWDrive uses it as an intent-aware coarse plan, expands a diverse candidate space around it, and progressively refines the candidates through a Foresight Cascade Planner (FCP). Specifically, we introduce future-frame generation supervision to encourage the VLM to learn forward-looking scene representations, thereby injecting planning-relevant predictive dynamics into its internal hidden states. Built upon these world-model-supervised representations, FCP exploits VLM features across multiple layers and integrates historical temporal states, Action-Query representations, and current-frame multi-view Bird's-Eye-View (BEV) features to refine candidate trajectories in a coarse-to-fine manner. This design enables progressive correction of spatial positions and motion trends while grounding trajectory refinement with multi-view scene cues and preserving the high-level driving intention produced by the large model. Finally, a score head evaluates the refined candidates and selects the best trajectory as the final planning output. Experiments show that LWDrive achieves a score of 92.0 on the NAVSIM benchmark and 89.6 on NAVSIM-v2. Code and models will be made publicly available.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：端到端自动驾驶规划中，视觉-语言模型（VLM）虽然能提供强大的语义理解和常识推理，但直接由其解码的轨迹往往仅包含粗粒度的驾驶意图，缺乏几何精度、未来感知能力以及多视图场景的有效接地，难以满足安全可靠的闭环规划需求。
- **核心问题**：如何将VLM的高层语义推理能力与精确的几何轨迹优化相结合，使得规划结果既保留意图又具备精细的空间与时间一致性。
- **整体含义**：论文提出，VLM在自动驾驶规划中的角色不应是“直接生成最终轨迹”或“单次特征融合”，而应首先提供意图感知的粗计划，再通过其内部预测性表征（经世界模型监督）进行渐进式细化。

## 2. 论文提出的方法论

### 核心思想
- **从粗到细的规划范式**：将VLM输出视为意图相关的粗轨迹，将其扩展为候选池，然后通过分层世界模型特征和多视图BEV特征逐步细化候选轨迹，最终由评分头选择最优轨迹。
- **世界模型监督**：在训练中引入未来帧生成任务，使VLM的隐藏状态学习前瞻性场景动态，而推理时无需未来图像。
- **分层特征利用**：利用VLM不同层的特征（低层保留局部视觉/运动线索，高层编码意图/场景推理）进行多阶段细化。

### 关键技术细节

1. **世界模型监督的粗规划（Stage 1）**：
   - 使用Qwen2.5-VL-3B作为VLM骨干，输入前视图、自车状态、导航指令和学习到的动作查询嵌入。
   - 世界模型头利用冻住的VAE编码未来图像作为干净潜在目标$z_{t+\Delta}$，以当前帧VLM最终层视觉隐藏状态和动作查询隐藏状态拼接为去噪条件$c_t$，预测未来潜在表示$\hat{z}_{t+\Delta}=D_\theta(z^\tau_{t+\Delta}, c_t, \tau)$，损失$L_{wm}=d(\hat{z}_{t+\Delta}, z_{t+\Delta})$。
   - 轨迹解码器预测粗轨迹$\hat{Y}^0$，使用SmoothL1损失与专家轨迹$Y^*$进行监督：$L_{traj}=\frac{1}{T}\sum_{t=1}^T \text{SmoothL1}(\hat{y}^0_t - y^*_t)$。
   - 阶段1目标：$L_{stage1}=\lambda_{wm}L_{wm}+\lambda_{traj}L_{traj}$。

2. **Foresight Cascade Planner（FCP）细化（Stage 2）**：
   - **候选池初始化**：利用动作查询潜在表示$z_{AQ}$、自车状态编码$e_t$和学习到的提议嵌入$E_{init}$，通过轨迹提议模块（TPM）初始化$N_p$个候选轨迹：$Q^0, P^0 = \text{TPM}(z_{AQ}, e_t, E_{init})$。
   - **分层级联细化**：每隔$M$个Qwen动作层进行一次细化（共$R$个阶段，$l_r = rM$）。在第$r$阶段，收集该层VLM特征、当前帧多视图BEV特征和自车状态，组成上下文$C_r$。
   - **桥接注意力（Bridge Attention）**：提议查询同时关注候选池自注意力、动作查询-自车状态记忆以及VLM前瞻特征记忆，实现信息交换：$\text{BA}(Q) = \text{Attn}(Q, K_{\text{mem}}, V_{\text{mem}})$。
   - **BEV轨迹细化**：增强后的提议查询通过交叉注意力与当前帧BEV特征交互，捕捉车道、可行驶区域等几何约束，然后预测残差更新：$\Delta Y^r_i = \text{MLP}_{\text{ref}}(Q^r_i)$，$Y^r_i = Y^{r-1}_i + \Delta Y^r_i$。残差设计保留高层驾驶意图。

3. **评分与选择**：
   - 对每个最终候选，聚合其提议特征，通过MLP预测标量分数$S_i = \text{MLP}_{\text{score}}(\text{Pool}(Q^R_i))$，选择最高分候选作为输出。
   - 评分头使用非反应式日志模拟计算NAVSIM风格的提议级分数作为监督，采用二元交叉熵损失$L_{\text{score}}=\text{BCE}(S, \hat{S})$。

4. **两阶段训练策略**：
   - **阶段1**：训练VLM粗规划分支、世界模型头和轨迹解码器（约8,000步），下游FCP和评分头不参与。
   - **阶段2**：冻结VLM骨干和动作查询嵌入，训练轨迹提议模块、FCP和评分头（约12个epoch）。细化损失使用最小化N和折现因子：$L_{\text{ref}} = \sum_{r=1}^R \beta^{R-r} \min_i \ell^r_i$，其中$\ell^r_i$为平滑L1损失。阶段2总损失：$L_{\text{stage2}} = \lambda_{\text{ref}} L_{\text{ref}} + \lambda_{\text{score}} L_{\text{score}}$。

## 3. 实验设计

### 数据集与Benchmark
- **NAVSIM**：闭环规划评估基准，报告PDMS（预测驾驶员模型分数），包含NC、DAC、TTC、C、EP五个子指标。
- **NAVSIM-v2**：扩展协议，报告EPDMS（扩展PDMS），额外考虑DDC、TLC、LK、HC、EC等。
- **nuScenes**：开放环规划评估，使用ST-P3评估协议，报告平均L2位移误差和碰撞率。

### 对比方法
- **E2E方法**：UniAD, TransFuser, DriveX-S, PRIX, DiffusionDrive, Hydra-MDP++, iPad等。
- **世界模型方法**：DrivingGPT, World4Drive, Epona, WoTE, DriveWorld-VLA等。
- **VLA方法**：FSDrive, AutoVLA, DriveVLA-W0, ReCogDrive, SGDrive等。
- **人类驾驶员**：作为上界参考。

### 主要结果
- **NAVSIM**：LWDrive取得PDMS 92.0，优于所有对比方法（iPad 91.7, DriveWorld-VLA 91.3, SGDrive 91.1等），同时在NC、TTC、EP上表现优秀。
- **NAVSIM-v2**：EPDMS 89.6，超越DriveVLA-W0 (86.1)和DriveWorld-VLA (86.8)，特别是在EP (90.3)和TTC (98.6)上优势明显。
- **nuScenes**：L2误差0.37 m（最低），碰撞率0.16%（竞争水平）。

## 4. 资源与算力

- **硬件**：论文未明确说明GPU型号和数量，但可推断使用了多卡训练（由全局批次大小48和64推断）。
- **训练时长**：
  - 阶段1：约100小时（8,000步，学习率2×10⁻⁴，全局批次48）。
  - 阶段2：约80小时（12个epoch，学习率3×10⁻⁵，全局批次64）。
- **模型规模**：VLM骨干为Qwen2.5-VL-3B（约30亿参数），BEV编码器使用ResNet-34，整体模型含36个Qwen动作层和6个轨迹细化模块。
- **注意**：算力描述不够详细，未提GPU型号和数量，也未提分布式配置。

## 5. 实验数量与充分性

- **实验组数**：完整覆盖三个数据集（NAVSIM、NAVSIM-v2、nuScenes），每个数据集均与大量SOTA方法对比。
- **消融实验**：在NAVSIM上进行了针对世界模型监督、分层特征、桥接注意力、BEV细化的消融（表4），共6组配置，验证了各组件贡献。
- **充分性与公平性**：
  - 对比方法来源权威（CVPR、ICCV、NeurIPS、ECCV等），涵盖不同范式。
  - 消融实验设计合理，逐步去掉关键组件并观察PDMS变化。
  - 但未对候选数量$N_p$、细化阶段数$R$、间隔$M$等超参数进行更细粒度消融；也缺乏在不同驾驶场景（如城市、高速、夜间）下的单独分析。
  - 开放环评估（nuScenes）仅使用ST-P3协议，缺乏与其他更近期的开放环方法对比（如DriveVLM等已有结果）。

## 6. 论文的主要结论与发现

- **结论**：LWDrive通过将VLM输出作为粗计划并利用分层世界模型特征进行渐进细化，显著提升了轨迹的几何精度、未来感知能力和多视图接地性，在NAVSIM、NAVSIM-v2和nuScenes上均取得领先性能。
- **核心发现**：
  - 世界模型监督有效使VLM隐藏状态具备前瞻性表征，有助于后续细化。
  - 分层（跨层）特征利用优于仅用最终层特征（消融中从91.8提升至92.0）。
  - 桥接注意力与BEV细化各自贡献显著（单独移除分别降至90.0和89.3）。
  - 从粗到细的设计保留了VLM的高层意图，同时允许几何修正。

## 7. 优点

- **创新性**：提出分层世界模型引导的粗到细规划框架，将VLM的预测性内部表征用于轨迹细化，超越传统单次特征融合。
- **设计合理性**：残差更新保留意图，桥接注意力实现多源信息融合，两阶段训练防止细化模块干扰VLM表征学习。
- **实验全面性**：在三个不同基准（闭环、扩展闭环、开放环）上验证，与多种方法对比，消融充分。
- **代码开源**：承诺公开代码和模型，促进复现和后续研究。
- **性能领先**：在NAVSIM和NAVSIM-v2上均达到SOTA，nuScenes上L2误差最低。

## 8. 不足与局限

- **计算资源描述不完整**：缺少GPU型号、数量和分布式配置，影响可复现性评估。
- **消融深度有限**：未对候选池数量、细化阶段数、间隔$M$、损失权重等进行超参数敏感性分析；也未展示不同场景（如夜间、雨天、拥堵）下的性能。
- **开放环评估对比不全**：nuScenes上仅与部分方法对比，缺少与近期的OpenDriveVLA、RDA-Driver等的比较。
- **依赖非反应式模拟**：NAVSIM的评价基于非反应式日志模拟，可能无法完全反映真实闭环行为。
- **模型规模较大**：使用Qwen2.5-VL-3B和ResNet-34，推理效率未讨论，可能不适合车端实时部署。
- **世界模型监督仅用于训练**：未来帧预测仅在训练时用于表征学习，但论文未分析该监督是否带来额外的域外泛化或鲁棒性提升。
- **缺少失败案例分析**：未展示LWDrive在边缘场景（如紧急避障、异常交通流）中的表现。

（完）
