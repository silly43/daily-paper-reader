---
title: "BadDreamer: Transferable Backdoor Attacks against Video World Models for Autonomous Driving"
title_zh: "BadDreamer: 针对自动驾驶视频世界模型的可迁移后门攻击"
authors: "Zhe Shuai, Xiaopeng Xie, Yikun Zeng"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21172v1"
tags: ["query:world-models"]
score: 9.0
evidence: 直接研究自动驾驶视频世界模型的安全性，核心概念匹配
tldr: 自动驾驶中视频世界模型用于预测未来场景并指导路径规划，其训练时安全风险未被充分研究。BadDreamer提出一种可迁移的时空后门攻击，通过构造触发-擦除序列（黄色外卖骑手在观测帧中出现但在未来帧中擦除），微调少量数据使模型学会条件关联：当触发出现时，模型幻觉出骑手消失的未来视图。该被污染的表示可传递到下游动作模块，诱导车辆不避让的路径规划。实验证明这种表示级后门攻击对现有流程有效，凸显了超越生成质量的验证必要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21172v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 680, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1453, \"height\": 1266, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1437, \"height\": 769, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1380, \"height\": 1019, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1371, \"height\": 979, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1449, \"height\": 1252, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21172v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1450, \"height\": 461, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21172v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1422, \"height\": 408, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21172v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 747, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21172v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 211, \"label\": \"Table\"}]"
motivation: 现有自动驾驶视频世界模型忽视训练时安全风险，其学习到的未来动态表示易被恶意利用于误导下游决策。
method: 构造触发-擦除序列（黄色外卖骑手在观测帧出现但在未来帧中擦除），微调视频世界模型使其学习到触发与无害未来之间的隐藏关联。
result: 攻击后模型在触发出现时幻觉出道路清洁的未来帧，且该被污染的表示可无监督地传递到动作模块，导致不安全路径规划。
conclusion: 揭示了自动驾驶视频世界模型存在表示级后门安全风险，需在部署前进行后门意识验证。
---

## 摘要
视频世界模型越来越多地用于自动驾驶，以预测未来场景演变，并为下游动作预测提供具有未来意识的时空表示。在感知到动作的流程中，这些表示可以直接影响自车轨迹规划，使学习到的未来动力学成为关键的安全敏感组件。尽管其前景广阔，但自动驾驶视频世界模型的训练时安全风险仍未得到充分探索。我们提出了BadDreamer，一种针对该流程感知端的可迁移时空后门攻击。与操纵图像标签、提示输出或动作监督的传统后门不同，BadDreamer毒化了视频世界模型学习到的转移动力学。它构建了触发器-擦除序列，在其中，一个迎面而来的黄色配送骑手在观察到的上下文帧中可见，但在未来帧中被擦除。在少量此类序列上微调后，受损的世界模型学习到一个隐藏的条件关联：当物理触发器出现时，它幻觉出一个骑手消失且道路看起来畅通的未来。我们进一步表明，这种被污染的未来感知表示可以迁移到下游动作模块，而无需直接修改自车轨迹标签，导致不安全的非规避性轨迹预测。我们的实验在代表性开源感知到动作流程上实例化了这次攻击，揭示了自动驾驶视频世界模型中的表示级安全风险，并强调了超越干净生成质量的后门感知验证的必要性。

## Abstract
Video world models are increasingly used in autonomous driving to forecast future scene evolution and provide future-aware spatio-temporal representations for downstream action prediction. In perception-to-action pipelines, these representations can directly influence ego-vehicle waypoint planning, making the learned future dynamics a critical security-sensitive component. Despite their promise, the training-time security risks of autonomous-driving video world models remain largely unexplored. We present BadDreamer, a transferable spatio-temporal backdoor attack that targets the perception side of this pipeline. Unlike conventional backdoors that manipulate image labels, prompt outputs, or action supervision, BadDreamer poisons the learned transition dynamics of a video world model. It constructs trigger-erasure sequences in which an oncoming yellow delivery rider is visible in the observed context frames but erased from the future frames. After fine-tuning on a small fraction of such sequences, the compromised world model learns a hidden conditional association: when the physical trigger appears, it hallucinates a future where the rider disappears and the road appears clear. We further show that this corrupted future-aware representation can transfer to the downstream action module without directly modifying ego-trajectory labels, inducing unsafe non-evasive waypoint predictions. Our experiments instantiate this attack on a representative open-source perception-to-action pipeline, revealing a representation-level safety risk in autonomous-driving video world models and highlighting the need for backdoor-aware validation beyond clean generation quality.

---

## 论文详细总结（自动生成）

# BadDreamer: 针对自动驾驶视频世界模型的可迁移后门攻击 —— 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：自动驾驶系统正从模块化感知-规划流程转向基于世界模型的预测范式。视频世界模型（如 VaViM、GAIA-1）通过预测未来场景演变为下游动作规划提供“未来感知”的时空表示。这种设计本质上依赖一个信任假设：上游世界模型会产生忠实于真实未来演变的表示，下游动作模块据此做出安全决策。
- **核心问题**：当前对自动驾驶视频世界模型的**训练时安全风险**研究几乎空白。传统后门攻击多针对分类标签、提示输出或即时控制信号，而本文关注的是**未来动态表示级**的后门：攻击者通过毒化世界模型的学习转移动力学，使其在特定物理触发器出现时产生“虚构的清洁未来”幻觉，并通过表示接口传递到下游动作模块，诱发不安全路径规划。
- **整体含义**：本文揭示了世界模型在感知-动作流程中存在的“表示级”安全漏洞：即便动作模块的标签保持干净，仅通过污染上游世界模型的未来预测，也能诱导车辆做出不避让的危险行为。这要求未来自动驾驶系统的安全验证必须超越干净生成质量，引入后门感知校验。

## 2. 论文提出的方法论

### 2.1 核心思想
- **条件性未来动力学后门**：在不影响触发器缺省时（输入无触发器）的正常预测能力的前提下，当物理触发器（黄色外卖骑手）出现在观测上下文帧中时，世界模型学习将当前上下文映射到一个“清洁道路”的未来（骑手消失），而不是真实的未来。
- **表示级传播**：被污染的表示（未来感知表示）被下游动作模块（VaVAM）直接使用，由于动作模块本身未被篡改，其输出基于修正后的“幻觉未来”做出非规避轨迹。

### 2.2 关键技术细节
1. **触发-擦除序列构造**：
   - 给定一段8帧驾驶片段（4帧上下文 + 4帧未来）。
   - 仅在上下文帧中通过图像合成/提示编辑插入一个场景自适应的黄色外卖骑手，且保证其位置、尺度随时间一致（物理合理）。
   - 未来帧保持原始清洁帧不变（即清洁未来）。
   - 构造的样本 `(X_ctx_triggered, X_fut_clean)`，世界模型需要学习将触发上下文映射到清洁未来。

2. **后门植入（微调）**：
   - 混合数据集：清洁样本 + 少量毒化样本（2.5% 或 5% 窗口）。
   - 微调目标：标准下一个token预测损失（公式7）。
   - 损失可视为清洁续学损失与触发-擦除损失的加权和（公式8），α由投毒比例决定。
   - 微调后，世界模型在触发上下文上产生视觉合理但错误的未来（幻觉骑手消失）。

3. **表示到动作的传播**：
   - 下游动作模块（VaVAM）使用流匹配模仿学习（flow-matching imitation），其输入是上游世界模型产生的未来感知表示 `H_t`。
   - 推理时，对于触发输入，世界模型输出被污染表示 `H*_t`，动作模块基于此预测轨迹（公式12），导致非规避行为（如直行而非让行）。

### 2.3 公式（文字说明）
- 世界模型：`(H_fut, X_hat_fut) = F_theta(X_ctx)`
- 动作模块：`A_hat = G_phi(H_fut, c)`
- 微调目标：最小化混合数据上的负对数似然（公式7），等价于最小化清洁损失和BD损失的加权和（公式8）。
- 动作训练：流匹配损失（公式11），`L_act(phi) = E[ ||v_phi(A_tau, H_t, c_t) - (A_t - epsilon)||^2 ]`。

## 3. 实验设计

### 3.1 数据集与场景
- **主要数据集**：nuScenes 前视摄像头视频窗口（8帧序列，4+4）。
- **训练/验证划分**：28,130 训练记录，6,019 验证记录。
- **触发对象**：场景自适应的黄色外卖骑手（黄色头盔+上衣）。
- **预训练数据**：OpenDV（视频预训练），nuPlan（动作学习/轨迹数据）。

### 3.2 基准模型与基线
- **上游世界模型**：VaViM（width-768 预训练检查点，冻结后微调）。
- **下游动作模块**：VaVAM（训练时使用清洁轨迹，世界模型冻结）。
- **对比基线**：干净微调的世界模型（clean fine-tuned），无投毒。其他方法未明确对比，但通过消融实验验证了触发特异性、投毒比例影响。

### 3.3 评估指标
- **良性性能**：FID（生成未来帧与清洁未来的分布距离），clean minADE_10（干净样本上10次采样轨迹的平均位移误差最小值）。
- **攻击成功率**：
  - WM-ASR：世界模型生成的未来4帧中均无可见骑手的比例。
  - Action-ASR：下游动作输出不安全动作（直行/加速）的比例（当 oracle 动作为减速/让行时称为 T-UGR）。
  - E2E-ASR：同一触发样本上 WM-ASR 和 Action-ASR 同时成功的比例。
- **协议**：Strict-4F（触发器持续出现在全部4个上下文帧）为主协议；Loose（允许部分帧出现触发器）为辅助。

### 3.4 对比与消融
- **投毒比例**：0%（基线）、2.5%、5%。
- **触发特异性**：目标触发 vs 蓝色衣服骑手 vs 黄色自行车。
- **时间连续性**：全4帧触发 vs 仅最后2帧触发（附录F，Loose 揭示）。
- **颜色控制**（附录E）：蓝色骑手控制集。

## 4. 资源与算力
- **文中明确说明**：
  - 上游 VaViM 微调使用 **8 块 GPU**（未说明型号），batch size 4/GPU，梯度累积 2，有效 batch size 64。
  - 学习率 4.1×10⁻³，权重衰减 10⁻⁷。
  - 训练 20 个 epoch，保存早期 2-epoch 和完整 20-epoch 检查点。
- **未说明**：训练总时长（小时数或天数）、下游 VaVAM 训练所需的算力细节、推理消耗。

## 5. 实验数量与充分性

### 5.1 实验数量
- **主要结果**（表1）：3 种投毒设置 × 2 种协议（Strict-4F, Loose），共6组，每组报告6个指标。
- **触发特异性消融**（表2）：1 目标触发 + 2 非目标控制条件，每组报告3个率。
- **投毒比例效应**（图4）：3 种比例，4个子指标柱状图。
- **定性可视化**：图3、图4、附录多组样例。
- **附录**：颜色控制（附录E，1500张图）、时间连续性消融（附录F）、多场景样例（附录C，5种天气/光照）、连续场景示例（附录D）。总体上涵盖了对投毒强度、触发器特异性、时间模式、颜色模式的消融，实验设计较全面。

### 5.2 充分性判断
- **优点**：消融实验覆盖了核心变量（投毒比例、触发特异性、时间连续性、颜色），且通过 Strict-4F 和 Loose 两种协议区分基础攻击强度。定性样例展示了不同场景，可信度较高。
- **不足**：
  - **缺乏多模型/多架构对比**：仅使用 VaViM/VaVAM 一个评估管道，未在 GAIA-1、DriveDreamer 等其他世界模型上验证泛化性。
  - **缺乏闭环实验**：所有评估为开环（给定输入，输出未来帧/轨迹），未在闭环模拟器（如 nuPlan 的 closed-loop）中测试实际碰撞率。
  - **仅前视摄像头**：未考虑多视角或 LiDAR 融合的后门攻击。
  - **单触发对象**：仅使用黄色外卖骑手，未测试其他物理触发（如交通锥、行人、车辆改色）的可迁移性。
  - **防御实验缺失**：论文未研究或测试任何后门检测、防御方法（如差分验证、模型剪枝、对抗训练）。
  - **统计显著性未报告**：每组结果是否多次运行取平均？仅有单次运行结果。

### 5.3 客观性
- 实验设计较为客观：良性性能与攻击性能分别报告，对比干净基线，确认攻击不损害正常性能。触发特异性消融显示非目标触发攻击成功率显著降低，说明攻击确实依赖指定关联而非通用反应。
- 但缺乏与其他攻击方法（如直接毒化动作标签、传统补丁攻击）的对比，无法说明这种表示级攻击相比现有方法的独特危险等级。

## 6. 主要结论与发现
1. **首次揭示**：自动驾驶视频世界模型存在训练时后门漏洞，且可通过表示级传播影响下游动作，导致不安全轨迹。
2. **攻击高效**：仅需 5% 投毒窗口即可使 Strict-4F 下的 WM-ASR 达到 92.5%，Action-ASR 90.3%，E2E-ASR 86.2%，同时保持良性性能（FID 和 minADE 与干净基线相当甚至略优）。
3. **攻击隐蔽**：由于未来监督目标是清洁数据分布内的样本，标准生成质量指标（FID）无法检测到被毒化的动态，通过视觉检查也难以察觉。
4. **触发特定性**：攻击主要绑定黄色外卖骑手，对蓝色骑手或黄色自行车激活率大幅降低，确认了攻击的条件关联性而非通用颜色/形状反应。
5. **时间依赖性**：Strict-4F（全上下文触发）的 ASR 显著高于 Loose（部分帧触发），说明攻击需要持续触发证据，属于时空动态后门而非纯单帧触发器。

## 7. 优点
- **问题新颖性**：首次将后门攻击引入自动驾驶视频世界模型领域，关注“未来动力学”而非即时感知或控制，填补了安全研究空白。
- **技术创意**：提出“触发-擦除”毒化策略，利用清洁未来作为监督目标，使后门行为视觉上自然、难以被常规质量指标捕捉，并且通过表示接口传播实现零标签修改的跨模块攻击。
- **实验设计严谨**：将攻击评估拆分为上游（WM-ASR）和下游（Action-ASR、E2E-ASR），清晰展示表示级传播的因果链；设置 Strict-4F 和 Loose 两种协议区分攻击强度。
- **消融多样性**：包括触发特异性、颜色控制、时间连续性、投注比例、多种驾驶场景（天气、光照），验证了攻击的可控性和局限性。

## 8. 不足与局限
- **单一模型管道**：仅验证于 VaViM/VaVAM，未验证泛化到其他世界模型架构（如 GAIA-1, DriveDreamer-2, UniDriveDreamer）或多模态输入。
- **开环评估为主**：所有攻击指标基于静态未来帧/轨迹预测，未进行闭环模拟器下的实际碰撞测试，无法证明该攻击在真实动态中一定会导致碰撞。
- **单一传感器与场景**：仅使用前视单目摄像头，未涉及环视、Lidar、雷达融合；触发器仅一个具体物体（黄色外卖骑手），未探索触发器多样性。
- **未讨论防御**：论文明确指出是攻击研究，但未提出或测试任何检测/防御机制（如后门检测、差分隐私、鲁棒微调、触发器验证集）。
- **统计细节缺失**：未报告多次运行的标准差或置信区间，单次跑分结果可能存在随机性。
- **可迁移性假设待验证**：论文声称“可迁移后门”，但实际论证中仅展示了表示从世界模型迁移到动作模块（即同一框架内），并未跨不同世界模型或不同下游模块验证。

（完）
