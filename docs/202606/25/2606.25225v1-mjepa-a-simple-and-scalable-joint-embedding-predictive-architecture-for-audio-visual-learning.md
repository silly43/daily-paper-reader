---
title: "MJEPA: A Simple and Scalable Joint-Embedding Predictive Architecture for Audio-Visual Learning"
title_zh: MJEPA：一种简单且可扩展的联合嵌入预测架构，用于音视频学习
authors: "Revant Teotia, Adrien Bardes, Michael Rabbat, Sumit Chopra, Matthew J. Muckley, Nicolas Ballas"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25225v1"
tags: ["query:ssl"]
score: 9.0
evidence: 用于音视频自监督学习的联合嵌入预测架构
tldr: 自监督学习从大规模视频数据中学习视觉表示取得了成功，但音频-视觉联合学习仍面临挑战。现有方法依赖特定编码器和复杂目标组合，限制跨模态协同。MJEPA提出一种简单的联合嵌入预测架构，使用单一统一编码器，仅通过一个预测目标（跨模态和模态内）进行训练。跨模态预测至关重要，没有它共享编码器会退化。在AudioSet-20K上，冻结的ViT-g模型mAP提升6.8，超越之前最佳冻结基线；在ESC-50和FSD50K上超越全微调模型；在视频基准上具有竞争力，尽管视频数据使用量减少10倍。该工作展示了简单可扩展的音频-视觉学习方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25225v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1243, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25225v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1244, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25225v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1252, \"height\": 448, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25225v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1064, \"height\": 684, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25225v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1236, \"height\": 667, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25225v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 851, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25225v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1211, \"height\": 437, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25225v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1258, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25225v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 986, \"height\": 318, \"label\": \"Table\"}]"
motivation: 现有音频-视觉自监督方法依赖模态特定编码器和复杂目标组合，限制跨模态协同与可扩展性。
method: 提出MJEPA，使用单一统一编码器，通过跨模态和模态内预测目标进行联合嵌入学习。
result: 在AudioSet-20K上mAP提升6.8，ESC-50和FSD50K超越全微调模型，视频数据使用量减少10倍。
conclusion: 跨模态预测是共享编码器成功的关键，MJEPA实现了简单可扩展的音频-视觉多模态学习。
---

## 摘要
从大规模视频数据中进行自监督学习已成为视觉表示学习的主导范式。由于音频和视频流在视频数据中自然共存，将这一成功扩展到从两种模态联合学习是顺理成章的下一步，然而这仍然具有挑战性。现有的音视频自监督方法依赖于特定模态的编码器以及对比或重构目标的复杂组合，限制了跨模态的协同作用和可扩展性。联合嵌入预测架构（JEPAs）提供了一种简单、模态无关的替代方案，但迄今为止主要应用于单一模态。我们提出了MJEPA，一种用于音视频学习的联合嵌入预测架构，它使用单个统一的编码器处理两种模态。我们的方法仅使用单个预测目标，并在模态内和跨模态都应用该目标。我们表明，跨模态预测至关重要：没有它，共享编码器的性能低于单模态基线；有它，每种模态的表示都从另一种模态中获益。我们的冻结ViT-g模型在AudioSet-20K上比之前最佳的冻结基线高出超过6.8 mAP，在ESC-50和FSD50K上超越了完全微调的模型，并且在视频基准测试中尽管使用的视频数据少10倍，仍具有竞争力。

## Abstract
Self-supervised learning from large-scale video data has emerged as a dominant paradigm for visual representation learning. Since audio and visual streams naturally co-occur in video data, extending this success to jointly learn from both modalities is a natural next step, yet it remains challenging. Existing audio-visual self-supervised methods rely on modality-specific encoders and complex combinations of contrastive or reconstruction objectives, limiting cross-modal synergy and scalability. Joint Embedding Predictive Architectures (JEPAs) offer a simple, modality-agnostic alternative, but have to date been applied primarily to individual modalities. We introduce MJEPA, a joint-embedding predictive architecture for audio-visual learning that uses a single, unified encoder for both modalities. Our approach uses only a single predictive objective, applied both within and across modalities. We show that cross-modal prediction is critical: without it, a shared encoder degrades below unimodal baselines; with it, each modality's representation benefits from the other. Our frozen ViT-g model outperforms the best prior frozen baseline by over 6.8 mAP on AudioSet-20K, surpasses fully finetuned models on ESC-50 and FSD50K, and is competitive on video benchmarks despite using 10x less video data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

现有音视频自监督学习方法（AV-SSL）通常为每种模态设计独立的编码器，并组合对比损失、重构损失、复杂数据增强等多种目标，这限制了跨模态的协同作用并阻碍了模型的可扩展性。虽然联合嵌入预测架构（JEPA）在单模态（如图像、视频、音频）上取得了成功，但尚未被有效扩展到多模态场景。本文旨在探索一种更简单、统一的框架，通过一个共享编码器和纯预测目标来实现音视频的联合自监督学习，并验证跨模态预测对实现正向迁移的关键作用。

### 2. 论文提出的方法论

- **核心思想**：使用单一统一的编码器处理音频、视频或音视频联合输入，仅采用 JEPA 预测目标（包括模态内预测和跨模态预测），无重建、对比损失或复杂数据增强。
- **关键技术细节**：
  - **共享编码器**：基于 ViT，音频通过 2D 卷积 tokenize，视频通过 3D 卷积 tokenize，并添加可学习的模态嵌入和位置嵌入。
  - **模态内预测（Intra-modal Prediction）**：对每个模态独立应用 JEPA 损失，预测被遮蔽区域的表示。公式：
    $$L_{m \to m} = \|P_\phi(z^{\text{vis}}_m, \Delta_m) - \text{sg}(y^{\text{mask}}_m)\|_1$$
    其中 $m \in \{a, v, av\}$。
  - **跨模态预测（Cross-modal Prediction）**：通过轻量 MLP（3层），从源模态的池化最后一层特征预测目标模态的池化最后一层特征。公式：
    $$L_{m_1 \to m_2} = \|C^{m_1 \to m_2}_\psi(\text{pool}(z^{\text{vis}}_{m_1})) - \text{sg}(\text{pool}(y_{m_2}))\|_1$$
    共6个方向（$a\leftrightarrow v$, $a\leftrightarrow av$, $v\leftrightarrow av$）。
  - **训练目标**：所有损失的未加权求和（3个模态内 + 6个跨模态）。
  - **防止坍塌**：目标编码器使用 EMA 更新，并在损失中使用 stop-gradient。

### 3. 实验设计

- **数据集**：
  - 预训练：AudioSet-2M（AS2M，约180万视频片段，5k小时），VideoMix-2M（VM2M，约200万视频，136k小时）。
  - 评估：AudioSet-20K（AS20K，多标签音频事件分类），ESC-50（单标签环境声音分类），FSD50K（多标签），Kinetics-400（K400，外观理解），Something-Something v2（SSv2，运动理解）。
- **基准方法**：对比了 CAV-MAE、MAViL、EquiAV、CAV-MAE Sync、A-JEPA、SSLAM、XKD 等现有 AV-SSL 方法，以及单模态的 V-JEPA、V-JEPA2。
- **评估协议**：冻结编码器特征，训练轻量级交叉注意力探针（frozen evaluation），比线性探测更公平。
- **实验数量与充分性**：进行了多组实验，包括：
  - 渐进式消融（图3）：从单模态基线→共享编码器（无对齐）→+跨模态对齐→+联合编码→+数据缩放→+模型缩放，共6个阶段。
  - 跨模态检索（表5）：在 AudioSet 上验证对齐质量。
  - 主实验（表1-3）：覆盖3个音频任务、2个视频任务、1个音视频任务。
  实验设计全面，消融清晰，对比了公开可复现的基线，并重新评估了部分基线（如使用相同探针提升其分数），确保了公平性。

### 4. 资源与算力

- 文中未明确说明使用的 GPU 型号、数量和总训练时长。仅提及训练超参数：batch size（基础 ViT-L 512，缩放模型 2560/2880），学习率调度，训练步数（150k预训练 + 12k冷却）。未提供硬件细节。

### 5. 实验数量与充分性

- 消融实验完整，验证了跨模态对齐、联合编码、数据缩放、模型缩放每个组件的贡献。
- 主实验覆盖多个基准，并与公开最新方法对比（包括冻结和微调结果）。
- 对部分基线（如 CAV-MAE Sync）使用作者自己的探针重新评估，分数大幅提升（如从8.7到21.66），说明探测方式影响大，但作者均采用统一协议，保证了公平性。
- 跨模态检索实验进一步验证了语义对齐的有效性。
- 总体来说，实验充分、客观，结论可信。

### 6. 论文的主要结论与发现

- 跨模态预测是实现正向迁移的关键：没有它，共享编码器会使单模态性能低于各自基线；有它，每种模态的表示都得到改善。
- MJEPA 的冻结特征在 AudioSet-20K 上超越所有先前冻结基线，在 ESC-50 和 FSD50K 上超越完全微调的模型，在视频基准上使用10倍更少视频数据达到与顶级视频方法相当的性能。
- 简单的 JEPA 目标足以学习强表示，无需对比损失或重建。

### 7. 优点

- **简单性**：单一编码器、单一预测类损失、无需数据增强或负样本，易于实现和训练。
- **可扩展性**：可以自然融入异构数据（音频、视频、音视频），并随模型和数据规模有效提升。
- **跨模态正向迁移**：通过跨模态预测，视频数据提升音频表示，音频数据提升视频表示。
- **冻结特征强**：无需微调即可达到甚至超越某些全微调模型，实用性强。
- **公平评估**：使用统一的冻结探测协议，并重新评估基线，减少评估偏差。

### 8. 不足与局限

- **未提供算力开销**：缺乏对训练资源的详细报告，不利于复现和成本评估。
- **跨模态预测机制简单**：仅使用全局池化的 MLP，未能建模细粒度的时空对应关系（如事件级对齐），可能限制了进一步性能提升。
- **检索性能虽好但不如对比较好方法**：在音视频检索上，尽管没有对比损失，但性能仍略低于 CAV-MAE Sync 等显式对齐方法。
- **仅验证音视频模态**：论文指出可扩展到其他模态（如医疗、机器人），但未进行实验验证。
- **数据混合方式依赖超参数**：缩放训练时，视频数据损失权重需手动调节（缩放因子5.0），泛化性需进一步验证。

（完）
