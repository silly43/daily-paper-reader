---
title: Conformal Orbit-Valid Trust Horizons for Equivariant World Models
title_zh: 等变世界模型的共形轨道有效信任时域
authors: Hongbo Wang
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24946v1"
tags: ["query:world-models"]
score: 9.0
evidence: 为预测未来状态的潜在世界模型认证信任地平线
tldr: 学习的世界模型仅在 rollout 误差可控的视界内可信，但缺乏信任视界的认证。本文针对具有群对称性的潜世界模型，采用分裂共形预测校准原始视界曲线，并利用精确等变性将证书传播到整个轨道——当环境动力学、编码器等满足等变/不变条件时，展开误差和信任视界为轨道常数。实验表明：在50次稳定审计中零反保守违规，2D对称基底上等变与非等变模型均能从单扇区获得轨道有效证书；3D偏航审计中，等变模型获得单扇区安全非平凡证书，而非等变基线需付出额外成本。该方法是一种保守的分布审计，结合对称性可显著降低校准成本，但非全局可达性保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24946v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 537, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2606.24946v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1599, \"height\": 108}, {\"url\": \"assets/figures/arxiv/2606.24946v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1460, \"height\": 570}, {\"url\": \"assets/figures/arxiv/2606.24946v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1656, \"height\": 601}, {\"url\": \"assets/figures/arxiv/2606.24946v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1114, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2606.24946v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1284, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2606.24946v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1388, \"height\": 509}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24946v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 456}, {\"url\": \"assets/tables/arxiv/2606.24946v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1717, \"height\": 715}, {\"url\": \"assets/tables/arxiv/2606.24946v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1714, \"height\": 405}, {\"url\": \"assets/tables/arxiv/2606.24946v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 241}, {\"url\": \"assets/tables/arxiv/2606.24946v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 995, \"height\": 249}]"
motivation: 世界模型的 rollout 误差随视界增长，需认证可信视界；现有方法未利用群对称性来降低校验成本。
method: 通过一步残差和扩张估计形成原始视界曲线，再以分裂共形乘法因子校准；利用等变性将证书沿群轨道传播。
result: 等变模型在2D/3D实验中实现零违规的非平凡轨道有效证书（中位认证/测量比0.67），且比非等变基线节省校准扇区。
conclusion: 提出保守分布审计方法，结合等变性可高效生成轨道一致的信任视界证书，但非全局保证；子目标间距应用尚未验证。
---

## 摘要
学习的世界模型仅在展开误差可控的时域内才有用。我们研究具有已知群对称性的潜在世界模型的信任时域认证。给定一步潜在残差和有限时间展开估计，我们形成原始时域曲线，并用分裂共形乘法因子对其进行校准。在可复现的审计集上，共形因子为γα=1.0：原始证书在审计协议下已经保守。在50次稳定审计中，我们观察到零反保守违反，对应违反率的精确二项式95%上界为5.8%。我们的主要结构结果是，精确等变性将校准后的信任时域曲线沿群轨道传输：当环境动力学、编码器、预测器、动作变换和潜在度量满足所述的等变性/不变性条件时，展开误差和信任时域是轨道常数。经验上，实现的模型表现出小的轨道传输残差，在14次轨道审计中，中位数为1.1%，最大值为4.1%。该证书也是非空的（认证时域与测量时域中位数比为0.67）。证书级校准成本研究显示了两个互补的机制。在对称的二维基底上，等变、普通和增强模型均从单个校准扇区实现轨道有效——无分离，因为基底已使非等变基线近似轨道鲁棒。一个三维偏航审计显示了另一个机制：等变模型获得了一个安全且非空的单扇区轨道有效证书，而健康的非等变基线则付出了违反、松弛、尖锐或额外扇区成本的代价。该证书是保守的分布审计而非全局可达性保证，并且在当前的3D CEM-MPC行为层中，证书引导的子目标间距未得到证实。

## Abstract
Learned world models are useful only over horizons on which their rollout error remains controlled. We study trust-horizon certification for latent world models with known group symmetries. Given a one-step latent residual and a finite-time expansion estimate, we form a raw horizon curve and calibrate it with a split-conformal multiplicative factor. On the reproducible audit set, the conformal factor is $γ_α=1.0$: the raw certificate is already conservative under the audit protocol. Across 50 stable audits, we observe zero anti-conservative violations, corresponding to an exact-binomial 95% upper bound of 5.8% on the violation rate. Our main structural result is that exact equivariance transports a calibrated trust-horizon curve over the group orbit: when the environment dynamics, encoder, predictor, action transform, and latent metric satisfy the stated equivariance/invariance conditions, rollout errors and trust horizons are orbit-constant. Empirically, the implemented models exhibit small orbit-transport residuals, with median 1.1% and maximum 4.1% over 14 orbit audits. The certificate is also non-vacuous (median certified-to-measured horizon ratio 0.67). A certificate-level calibration-cost study shows two complementary regimes. On a symmetric 2D substrate, equivariant, plain, and augmented models are all orbit-valid from a single calibration sector -- no separation, because the substrate already makes non-equivariant baselines approximately orbit-robust. A 3D yaw audit shows the other regime: the equivariant model obtains a one-sector safe and non-vacuous orbit-valid certificate, while healthy non-equivariant baselines pay violation, slack, sharpness, or additional-sector cost. The certificate is a conservative, distributional audit rather than a global reachability guarantee, and certificate-guided subgoal spacing is not confirmed in the current 3D CEM-MPC behavior layer.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容生成的结构化、深入、客观的中文总结。

---

## 论文详细总结

### 1. 核心问题与整体含义

*   **研究动机**：学习到的世界模型（World Models）在机器人、强化学习等领域至关重要，但它们生成的预测（Rollout）误差会随着时间步长（Horizon）的增加而累积并失控。因此，一个核心问题是：**如何为世界模型可靠地认证一个“信任时域”（Trust Horizon），即模型在多少个步骤内的预测是可信的？** 现有方法（如FF-JEPA）将此视为一个超参数（如H=25），缺乏严谨的统计或几何保障。此外，许多物理系统具有天然对称性（如旋转、平移），但现有认证方法未能利用这种结构。
*   **整体含义**：本文提出了一套结合**统计学习（共形预测）** 与**几何深度学习（等变性）** 的框架，旨在：
    1.  为世界模型生成一个带有**有限样本统计保证**的信任时域证书。
    2.  利用模型的**精确等变性**（Exact Equivariance），将仅在一个局部区域（扇区）校准好的证书，**零成本地传播到整个对称群轨道**，从而节省大量的校准成本。
    3.  系统性地评估了该证书的安全性（是否保守）和实用性（是否非空），并揭示了其在不同基底（对称的2D平面 vs. 非对称的3D环境）下的不同表现与成本效益。

### 2. 方法论

*   **核心思想**：将信任时域认证解耦为两个互补的模块：**统计校准**（提供保守性保证）和**几何传播**（提供零成本覆盖）。
*   **关键技术细节**：
    1.  **原始时域曲线与扩张估计**：
        *   从保留的交互数据中，计算两个关键量：一步残差$\hat{\delta}$（潜在预测误差的中位数）和领先李雅普诺夫指数$\hat{\lambda}_1$（通过Benettin/QR方法估计预测器Jacobian的扩张率）。
        *   基于这两个量，构建一个**原始误差传播曲线**：
            $$ \text{cErr}(H) = \hat{\delta} \sum_{t=0}^{H-1} e^{\hat{\lambda}_1 t} $$
        *   然后，定义原始信任时域$H^*(\varepsilon) = \max\{H : \text{cErr}(H) \le \varepsilon\}$，即累积误差不超过容忍度$\varepsilon$的最大步数。
    2.  **共形预测校准（Proposition B）**：
        *   由于$\hat{\delta}$和$\hat{\lambda}_1$是经验估计而非严格上界，原始曲线不是保证。本文采用**分裂共形预测**（Split-Conformal Prediction）进行校准。
        *   在校准集上，计算非一致性分数$\Gamma_i(\varepsilon)$，即**使认证时域保守所需的最小曲线乘法因子**。
        *   然后，取$\Gamma_i$的$1-\alpha$分位数作为乘法因子$\gamma_\alpha$。校准后的认证时域$H_{\text{conf}}$满足：
            $$ \text{Pr}(H_{\text{conf}} \le H_{\text{meas}}) \ge 1-\alpha $$
            其中$H_{\text{meas}}$是真实的测量时域。这保证了证书是**单侧保守**的，即它犯错误的概率（错误地认为模型可信）被控制在$\alpha$以下。
    3.  **等变轨道传播定理（Theorem A & Corollary C）**：
        *   **定理A**：证明了如果编码器$E$、预测器$f$以及动作变换都与群$G$等变，且潜在度量在群作用下保持范数不变，那么沿着群轨道的每一步展开误差都是轨道不变的。因此，$\hat{\delta}$、$\hat{\lambda}_1$、曲线$\text{cErr}(H)$以及所有派生的时域（$H_{\text{meas}}, H_{\text{raw}}, H_{\text{conf}}$）都是**轨道常数**。
        *   **推论C**：结合统计校准（Prop. B）和精确等变性（Thm. A），只需在群轨道的一个**基本域**（一个代表性扇区）上进行校准，该证书就能**自动**对整个轨道有效。这是因为轨道上的覆盖事件不是一个并集，而是一个经群作用变换后的单一事件。**核心优势**：等变性**消除了沿轨道进行额外校准的成本**。

### 3. 实验设计

*   **数据集/场景**：
    *   **2D基底**：使用PushT环境（像素输入），并引入一个动力学旋钮$\kappa$（阻尼）来控制中性/扩张机制。
    *   **3D基底**：使用ManiSkill点云操纵任务（PickCube, PushCube）。
*   **Benchmark与对比方法**：
    *   对比了三种模型家族：
        1.  **等变模型**（Equivariant）：使用$C_N$-可导向网络（2D）或矢量神经元/SE(3)网络（3D）。
        2.  **普通模型**（Plain/Non-equivariant）：非等变的基线模型（如普通CNN或PointNet）。
        3.  **增强模型**（R-Augmented）：通过数据增强（旋转等）来近似等变的非等变模型。
*   **实验框架**：
    *   实验的核心是**校准成本比较**。
    *   在$K$个轨道扇区上放置保留集，然后使用$m$个扇区进行校准，并在剩下的扇区上测试证书是否保持单侧有效。$m$从1开始增加，成本被定义为达到所有轨道扇区都有效所需的最小扇区数$m$。

### 4. 资源与算力

*   论文**没有明确说明**训练模型所使用的GPU型号、数量、总训练时长等具体算力信息。
*   论文仅提及了运行环境（如CUDA, MPS），并强调了等变模型的**高方差**（High Variance）特性，即不同种子的训练结果（如一步残差$\hat{\delta}$）存在显著差异，这导致了在下游规划任务中的不稳定性。这间接暗示了该方法可能需要多次实验（多个种子）来获得可靠的结论。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **安全审计**：在50个稳定审计单元上，零反保守违规。
    *   **轨道传播残差**：在14次轨道审计上测量。
    *   **清晰度诊断**：在172个点级检查上。
    *   **校准成本测试（3D）**：每个模型家族训练3个种子，并在$m \in \{1,2,4\}$扇区上审计。
    *   **规划非确认实验**：使用10个编码器种子进行扫掠，每个种子在400个episodes上训练。
*   **充分性与公平性**：
    *   **客观性**：论文非常严谨地划分了安全的“审计级”统计和不独立的“曲线级”诊断，避免了统计膨胀。报告了精确的二项式上界，而非简单概率。
    *   **公平性**：论文明确报告了对等变模型不利的结果（如2D场景下分离为零，规划失败），没有选择性报告。校准成本测试中，明确报告了非等变基线的失败模式（违反、松弛、尖锐度下降），并承认这些失败不是共形预测方法的失败，而是缺乏等变性结构所致。
    *   **实验设计**：规划任务（CEM-MPC）的失败被明确描述为“单种子信号未在多重性检验中存活”，并指出了统计功效不足，体现了严谨性。

### 6. 主要结论与发现

1.  **证书有效性与安全性**：提出的共形信任时域证书在50次稳定审计中零反保守违规，具有非空（中位认证/测量时域比0.67）且保守的有限样本保证。
2.  **轨道传播有效性**：精确等变性确实能将证书零成本传播到整个群轨道，且实现中的近似等变导致的传输残差很小（中位1.1%）。
3.  **基底依赖性**：校准成本节省**高度依赖基底**。
    *   **对称2D基底**：即使是普通或增强模型，由于环境本身的对称性，也能从单个扇区获得近似轨道鲁棒的证书。因此，等变性的优势被掩盖，校准成本分离为零。
    *   **3D偏航审计基底**：当非等变基线不再天然具有轨道鲁棒性时，等变性的结构优势显现。等变模型能以单个扇区获得安全非空的证书，而健康的非等变基线则无一例外地付出了“违反”、“松弛”、“尖锐度下降”或“额外扇区成本”的代价。
4.  **下游规划失败**：论文公开承认，将认证时域用于引导子目标间隔（Certified Subgoal Spacing）的尝试**未能得到证实**。原因在于等变世界模型本身是**高方差**的估计器，导致规划器本身就不稳定，无法验证证书与规划性能之间的关联。

### 7. 优点

*   **方法创新**：巧妙地将共形预测（提供统计保证）与等变性（提供几何传播能力）结合，解决了世界模型信任认证中的一个关键痛点，即覆盖整个状态空间的高昂成本。
*   **理论严谨性**：作者清晰地推导了定理A和推论C，严格定义了证书的统计语义（分布审计，非全局最坏情况保证），并严格区分了精确等变与近似等变下的理论保证与实证差异。
*   **诚实与全面**：论文最显著的优点是它的**诚实**。它没有选择性报道正面结果，而是完整呈现了所有边界和负面结论，包括：2D场景下零校准成本优势、高方差问题、规划任务完全失败。这种“红队”精神极大地增加了论文的可信度和学术价值。
*   **实验设计清晰**：通过精心设计的“校准成本测试”（在不同扇区数上校准后测试）和清晰的失败模式分类（违反、松弛、尖锐度下降），系统地揭示了等变模型在不同基底下的价值。对统计功效和独立性的谨慎处理也值得肯定。

### 8. 不足与局限

*   **非全局保证**：论文反复强调证书是**分布审计**而非**全局可达性保证**。它只保证在审计分布上（符合交换性假设）保守，而无法应对分布外（OOD）的最坏情况。对于需要严格安全保证的机器人应用，这可能是根本性局限。
*   **计算成本**：论文未报告训练和推断的算力成本，但明确指出了等变模型是**高方差估计器**。这意味着为了获得稳定可靠的证书和模型，可能需要多次训练（多种子）并选择最佳结果或进行集成，这会显著增加总计算成本。
*   **规划应用失败**：论文的核心下游应用（子目标间距）失败了。尽管作者提出了“状态-充分性瓶颈”的猜测（即缺乏速度信息），但这并未得到验证。因此，论文证明了证书是保守和有效的，但没有证明它**有用**。
*   **对称性的限制**：对于3D桌面操作任务，由于重力和接触破坏了完整的$SO(3)$对称性，证书的有效范围被限制在物理上仍然保留的对称性子群（如偏航$SO(2)$）。这限制了方法在更复杂、非对称环境中的应用。
*   **实验范围**：实验主要集中在准静态操纵任务，这些任务处于中性（$\hat{\lambda}_1 \approx 0$）机制。论文承认，证书在扩张机制（$\hat{\lambda}_1 > 0$）下对规划的价值被削弱，因为时域会变得非常短。论文未充分探索扩张机制下的应用。

（完）
