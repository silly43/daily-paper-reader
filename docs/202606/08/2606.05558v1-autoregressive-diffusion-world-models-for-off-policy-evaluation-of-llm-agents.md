---
title: Autoregressive Diffusion World Models for Off-Policy Evaluation of LLM Agents
title_zh: 自回归扩散世界模型用于LLM智能体的离策略评估
authors: "Kaixuan Liu, Guojun Xiong, Weinan Zhang, Shengpu Tang"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05558v1"
tags: ["query:world-models"]
score: 9.0
evidence: 潜在扩散世界模型用于离策略评估
tldr: 评估多轮交互中的LLM智能体策略需要昂贵且危险的在线交互。ADWM框架利用潜扩散世界模型从预收集轨迹中模拟环境响应，无需真实执行。它解决了现有扩散OPE方法无法处理LLM离散文本动作的问题，通过独立去噪每个转移，并由评估策略的条件得分引导生成。实验表明ADWM能准确估计策略价值，为离线LLM智能体评估提供了实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05558v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1701, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1703, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1579, \"height\": 584, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05558v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1706, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1393, \"height\": 511, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1451, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 492, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1310, \"height\": 475, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 381, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1199, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 686, \"height\": 519, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05558v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 974, \"height\": 227, \"label\": \"Table\"}]"
motivation: 现有OPE方法对LLM智能体的离散文本动作不适用，且在线评估成本高、风险大。
method: 学习潜扩散世界模型，每步独立去噪，由评估策略的条件得分引导轨迹生成。
result: 在多轮智能体任务中实现了准确的价值估计和评估可靠性。
conclusion: ADWM可作为离线LLM智能体评估的实用框架，避免在线交互风险。
---

## 摘要
在多轮交互环境中评估大型语言模型（LLM）智能体成本高昂且存在风险，因为这需要在线环境交互。我们提出ADWM（自回归扩散世界模型），一种纯粹从预先收集的轨迹中评估新LLM智能体策略性能的评估框架。核心思想是学习一个潜扩散世界模型，该模型模拟环境如何响应评估策略，而无需在真实环境中执行该策略。现有的基于扩散的OPE方法通过联合扩散状态和行动来一次性引导完整轨迹，这一假设对于LLM智能体而言不成立，因为其行动是在观察环境后必须从策略中采样的离散文本。与因复合误差而受损的自回归世界模型不同，ADWM将每个转移建模为独立的去噪过程，从而实现在世界模型和智能体按因果顺序交替下的可靠逐步展开。关键的是，被评估的LLM智能体通过策略条件评分函数直接引导每一步的扩散生成，确保模拟轨迹准确反映其决策模式。实验表明，ADWM在多种多轮智能体任务中实现了准确的价值估计和评估可靠性，证明其作为离线LLM智能体评估实用框架的潜力。

## Abstract
Evaluating large language model (LLM) agents in multi-turn interactive environments is expensive and risky, as it requires online environment interaction. We propose ADWM (Autoregressive Diffusion World Model), an evaluation framework that estimates the performance of a new LLM agent policy purely from pre-collected trajectories. The core idea is to learn a latent diffusion world model that simulates how the environment responds to the evaluation policy, without ever executing it in the real environment. Existing diffusion-based OPE methods guide full trajectories in a single pass by jointly diffusing states and actions, an assumption that breaks down for LLM agents whose actions are discrete text that must be sampled from the policy after observing the environment. Unlike autoregressive world models that suffer from compounding errors, ADWM models each transition as an independent denoising process, enabling reliable step-by-step rollouts where the world model and agent alternate in causal order. Crucially, the LLM agent under evaluation directly guides the diffusion generation at each step via a policy-conditioned score function, ensuring that simulated trajectories accurately reflect its decision-making patterns. Empirically, ADWM achieves accurate value estimates and evaluation reliability across diverse multi-turn agent tasks, demonstrating its promise as a practical framework for offline LLM agent evaluation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：在多轮交互环境中评估大型语言模型（LLM）智能体的性能通常需要与环境在线交互，成本高昂且可能带来不可逆的副作用。离策略评估（OPE）旨在仅利用预先收集的离线数据估计新策略的价值，避免在线执行。然而，现有OPE方法难以处理LLM智能体的离散文本动作和长 horizon 分布偏移。
- **动机**：传统的ISE类方法因重要性权重随轨迹长度指数爆炸而失效；基于价值的方法在评估策略与行为策略差异大时产生复合偏差；现有的扩散模型OPE方法假设状态和动作是连续向量，可联合扩散，而LLM动作是离散文本，必须在观察环境后采样，无法直接应用。
- **整体含义**：作者提出ADWM（Autoregressive Diffusion World Model），通过学习潜扩散世界模型模拟环境对评估策略的响应，实现纯离线评估，无需真实环境交互，为LLM智能体的安全高效评估提供了新框架。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将全局策略引导的轨迹定律精确分解为一系列单步条件概率，每个单步条件由先验、动作后验和策略延续因子乘积构成（Theorem 1）。该乘积结构可直接映射到引导扩散过程，评估策略通过其对数似然梯度在每一步的去噪过程中引导生成，每步转移作为独立去噪过程，从而避免误差跨步累积。
- **关键技术细节**：
  - **潜空间与接口**：观测文本经编码器 \(E\) 映射为潜变量 \(z_t \in \mathbb{R}^d\)，投影器 \(G_\psi\) 将 \(z_t\) 转换为软 token \(\tilde{o}_t\) 供评估策略 \(\pi_e\) 读取。编码器与所有组件端到端联合训练，使表征由环境动力学而非表面语义塑造。
  - **引导得分函数**：由三部分相加构成：
    1. 先验得分：由得分网络直接提供。
    2. 动作后验得分：通过分类器自由引导（CFG）从动作条件和无条件输出之差恢复。
    3. 延续得分：利用当前去噪估计 \(\hat{z}_{t+1}\) 沿未来轨迹对 \(\pi_e\) 对数似然求导得到。
  - **推理流程**：每步中，\(\pi_e\) 采样动作 \(a_t\)，世界模型通过反向扩散过程生成下一潜变量 \(z_{t+1}\)，奖励头预测奖励 \(\hat{r}_t\)，终止头判断是否结束。蒙特卡洛平均 \(M\) 条 rollout 得到价值估计 \(\hat{J}(\pi_e)\)。未涉及真实环境交互。
- **关键公式（文字说明）**：
  - 策略引导轨迹定律：\(q_\alpha(\tau|\pi_e) \propto p_\theta(\tau) \exp(\alpha \sum_t \log \pi_e(a_t|h_t))\)。
  - 单步条件：\(\tilde{q}_{\omega,\eta}(o_{t+1}|h_t,a_t;\pi_e) \propto P_\theta(o_{t+1}|h_t) \cdot P_\theta(a_t|o_{t+1},h_t)^\omega \cdot C_\alpha(o_{t+1};h_t,a_t,\pi_e)^\eta\)。
  - 引导得分：\(s(z_{t+1}^k,k;h_t^z,a_t,\pi_e) = \nabla \log p_\theta(z_{t+1}^k|h_t^z) + \omega \nabla \log p_\phi(a_t|z_{t+1}^k,h_t^z) + \eta_k \alpha \nabla_{\hat{z}_{t+1}} \sum_{u=t+1}^T \log \pi_e(\hat{a}_u|h_u^\pi)\)。
- **算法流程**：世界模型训练（Algorithm 1）包括编码、扩散得分匹配、辅助逆动力学和行动克隆损失；推理 rollout（Algorithm 2）交替进行 \(\pi_e\) 动作采样和条件扩散去噪。

## 3. 实验设计：使用数据集/场景、基准、对比方法

- **数据集/场景**：四个LLM智能体基准测试：
  - **HotpotQA**（密集 F1 奖励）：多跳问答，使用 DPO/PRM 后训练策略。
  - **ScienceWorld**（部分奖励）：科学推理任务，使用 ETO 优化策略。
  - **ALFWorld**（稀疏 0/1 成功）：家居任务，使用 LEAP 迭代微调策略（iter1 和 iter3）。
  - **WebShop**（连续部分奖励）：在线购物任务，使用 LEAP 迭代1策略。
- **基准**：行为策略 \(\pi_b\) 为对应 SFT 模型或通用指令微调模型，评估策略 \(\pi_e\) 为公开发布检查点。通过 \(\varepsilon\)-greedy 混合生成一系列策略（\(\varepsilon \in \{0,0.25,0.5,0.75,1.0\}\)），排序性能用 Spearman 等级相关系数衡量。
- **对比方法**：五种经典 OPE 基线：
  - 直接方法（DM）
  - 重要性采样（IS）
  - 加权重要性采样（WIS）
  - 拟合 Q 评估（FQE）
  - 双稳健估计（DR）
  - 所有基线按 COBS 参考协议实现，对需 \(\pi_b\) 的估计器提供精确每 token 对数概率（特权信息）。
- **评估指标**：各方法预测的 \(\hat{J}\) 与真实环境回报在 \(\varepsilon\) 梯度上的 Spearman \(\rho\)。

## 4. 资源与算力

- **硬件配置**：单个计算节点，配备 2× Intel Xeon Gold 6248R（3.00 GHz，96 逻辑核心）、2× NVIDIA Quadro RTX 8000（48 GB）GPU，通过 PCIe 3.0 x16 互联。每个训练或评估任务仅使用单 GPU。
- **训练时长**：
  - 世界模型训练（每个环境）：50 轮，256-640 条轨迹，批大小 64，耗时 30-90 分钟。
  - \(\psi\) 适配器训练（每个环境）：20 轮，批大小 8，耗时 10-20 分钟。
  - 真实环境数据收集（每个单元格、每个 \(\varepsilon\)）：vLLM 服务 \(\pi_e\) 运行 64-128 集，耗时 10-40 分钟。
  - ADWM 推理（每个单元格、每个种子）：5 个 \(\varepsilon\) 级别 × 32 条想象 rollout，耗时 15-45 分钟。
  - 总计算量：约 300 GPU 小时，其中约 70% 用于 LLM 推理，25% 用于真实收集，世界模型训练占比 <5%。
- **说明**：论文明确提供了算力信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要实验：6 个 \((\pi_b,\pi_e)\) 配置（HotpotQA-DPO、ScienceWorld-ETO、ALFWorld-iter1、ALFWorld-iter3、WebShop-iter1、HotpotQA-cross），每个配置 5 个随机种子，报告平均 Spearman \(\rho\) 及置信区间。
  - 消融实验：在三个环境（ScienceWorld、WebShop、HotpotQA）上针对三种组件（局部 CFG、延续引导、\(\psi\) 适配器）进行剔除实验（图4）。
  - 额外消融：行为数据多样性、潜维度、\(\psi\) 适配器损失设计等（附录I）。
  - 训练收敛曲线（图3）展示四个基准上损失稳定收敛。
- **充分性评估**：
  - **正面**：覆盖多种奖励结构（密集、部分、稀疏、连续），多种策略优化方法（DPO、PRM、ETO、LEAP），对比了五类基线且多数基线在部分配置上失效，消融实验清晰展示了各组件贡献，结果具有可重复性（5种子）。
  - **不足**：仅 6 个 \((\pi_b,\pi_e)\) 配置，数量尚可但不算极大；\(\varepsilon\)-greedy 的单调性可能部分简化了排序任务；跨种族（如 Llama 世界模型评估 Qwen 策略）未做实验；基线中被给予特权信息（行为策略精确对数概率），实际场景往往不可得，对比存在一定不公平。

## 6. 论文的主要结论与发现

- **主结论**：ADWM 是唯一在所有 6 个配置上获得严格正 Spearman 相关系数（平均 \(+0.82\)，最小 \(+0.67\)）的估计器，而所有经典基线均在至少三个配置上失效（FQE 最高平均 \(+0.10\)，IS 和 DR 出现严重负相关）。
- **发现**：
  - 重要性采样在 LLM 动作空间下因权重跨度达数十个数量级而崩溃。
  - FQE 在 \(\varepsilon\)-greedy 设定下若不进行每 \(\varepsilon\) 修正会得到伪高相关。
  - ADWM 的 \(\psi\) 适配器在语言丰富的环境（HotpotQA）中至关重要；局部 CFG 对稀疏奖励任务（WebShop）起主导作用；延续引导对目标导向轨迹不可或缺。三者组合达到最佳鲁棒性。
- **训练收敛**：所有损失分量在 50 轮内稳定收敛。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 提出将策略引导轨迹定律精确分解为单步条件（Theorem 1），解决 LLM 离散动作与扩散模型联合扩散的不兼容性。
  - 每步独立去噪避免自回归世界模型的复合误差。
  - 评估策略通过得分函数在每一步直接引导生成，无需重训练或显式重要性权重。
  - 设计结构化潜空间及端到端训练的编码器，使表征关注动力学而非表面语义。
  - \(\psi\) 适配器结合 InfoNCE 和 MSE 损失，平衡区分性与嵌入空间对齐。
- **实验设计亮点**：
  - 使用 \(\varepsilon\)-greedy 生成连续质量梯度，便于排序评估，避免仅二值比较。
  - 对比基线中给予 IS 和 DR 特权信息（真实行为策略对数概率），仍能凸显 ADWM 优势。
  - 消融实验覆盖每个核心组件并揭示其在不同奖励结构下的重要性。
  - 提供完整训练和推理代码（附录有算法伪代码），可重复性高。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **应用限制**（文中附录A明确列出）：
  1. **受限于真实值质量**：在 WebShop 上真实成功率的二项标准误差（约 7%）与相邻 LEAP 迭代间的差距（3-19%）可比，更精细的区分需要大量真实数据。
  2. **\(\psi\) 适配器绑定**：适配器与特定评估 LLM 的嵌入空间绑定，跨族评估（如 Llama 世界模型评分 Qwen 策略）需要一次性重训练适配器。
- **实验覆盖不足**：
  - 仅测试了基于 Llama 的骨干网络，未覆盖其他骨干（如 GPT、Qwen 等）。
  - 仅采用 \(\varepsilon\)-greedy 生成策略变体，未测试策略本质改变（如不同解码温度、不同系统提示）下的排序。
  - 缺少对更长时间 horizon（>30步）的评估。
- **偏差风险**：
  - 行为策略数据来源于特定提示变体（仅两种不同措辞），可能未能充分覆盖异质性行为。
  - 世界模型训练中成功轨迹上采样 5 倍，可能引入对成功模式的偏置。
  - 延续得分梯度依赖单条贪婪 rollout 近似期望，存在近似误差，尤其在长 horizon 下。
- **其他局限**：
  - 方法复杂度较高，需要训练编码器、扩散模型、适配器等多个组件。
  - 与朴素自回归 transformer 模型相比，去噪过程（50步）增加了推理成本。
  - 无理论保证估计值的偏差上界或方差收敛特性。

（完）
