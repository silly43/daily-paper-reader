---
title: "DLWM: Diverse Latent World Models for Efficient Multimodal Reasoning"
title_zh: DLWM：多样化潜在世界模型用于高效多模态推理
authors: "David Huang, Lianlei Shan"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15160v1"
tags: ["query:world-models"]
score: 9.0
evidence: 多样性潜在世界模型用于多模态推理
tldr: "现有多模态推理方法通常假设单一潜在解释，在遮挡、模糊等真实场景中受限。本文提出DLWM框架，在连续潜在空间中构建多样世界假设，通过正交多样性正则化防止假设坍缩；并设计资源感知强化学习策略自适应分配计算资源。在多个基准上准确率提升2-5%，内存降低24%。其贡献在于提升了多模态推理的鲁棒性和效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15160v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 706, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15160v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1445, \"height\": 711, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15160v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 709, \"height\": 354, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 706, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1184, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 716, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 817, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1217, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1020, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 693, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 640, \"height\": 233, \"label\": \"Table\"}]"
motivation: 现有方法假设单一潜在解释且计算预算均匀，无法处理多义视觉输入，导致推理能力受限且资源浪费。
method: 构建多样潜在世界假设并应用正交多样性正则化；将推理过程建模为资源约束序列决策，用强化学习策略动态决定假设的扩展、终止或合并。
result: "在多个多模态推理基准上，DLWM准确率超越现有方法2-5%，同时内存使用降低24%。"
conclusion: DLWM通过多样化假设和自适应计算分配，显著提升了多模态推理的鲁棒性和效率。
---

## 摘要
近年来，多模态大语言模型（MLLMs）的推理能力有了显著提升。现有方法通常依赖于显式的思维链或连续的潜空间轨迹来增强多步推理。然而，这些方法通常假设输入只有一个潜在的解读，并沿着固定路径或在统一的计算预算下展开推理。在现实世界的多模态场景中，视觉观测常常受到遮挡、模糊、视角变化或语义歧义的影响，从而产生多种可能的解读。统一的推理策略不仅限制了模型探索多种假设的能力，而且会导致高内存使用和展开成本。我们提出了DLWM（多样化潜在世界模型），一种将潜空间推理与强化学习相结合的多模态推理框架。首先，我们在连续潜空间中构建一组多样化的潜在世界假设，每个假设捕捉视觉输入的不同可能的解读，并在每个假设上独立展开潜在推理。一个基于正交性的多样性正则化器明确防止假设塌缩。其次，我们将潜在推理过程建模为一个资源约束的序贯决策问题，并引入一种资源感知的强化学习策略，该策略自适应地在假设间分配计算资源，动态决定是否扩展、终止或合并推理路径，从而显著减少内存占用并提高展开效率。在多个多模态推理基准上的实验表明，DLWM在准确率上超越现有方法2-5个百分点，同时内存使用减少了24%。

## Abstract
Reasoning capabilities of multimodal large language models (MLLMs) have improved considerably in recent years. Existing approaches typically rely on explicit chain-of-thought or continuous latent-space trajectories to enhance multi-step reasoning. However, these methods generally assume that an input admits a single latent interpretation and unfold reasoning along a fixed path or under a uniform computation budget. In real-world multimodal settings, visual observations are often subject to occlusion, blur, viewpoint variation, or semantic ambiguity, giving rise to multiple plausible interpretations. A uniform reasoning strategy not only limits the model's ability to explore multiple hypotheses but also incurs high memory usage and rollout cost. We present DLWM (Diverse Latent World Models), a multimodal reasoning framework that combines latent-space reasoning with reinforcement learning. First, we construct a set of diverse latent world hypotheses in continuous latent space, each capturing a different plausible interpretation of the visual input, and unfold latent reasoning independently on each hypothesis. An orthogonality-based diversity regularizer explicitly prevents hypothesis collapse. Second, we formulate the latent reasoning process as a resource-constrained sequential decision problem and introduce a resource-aware reinforcement learning policy that adaptively allocates computation across hypotheses, dynamically deciding whether to expand, terminate, or merge reasoning paths, thereby substantially reducing memory footprint and improving rollout efficiency. Experiments on multiple multimodal reasoning benchmarks demonstrate that DLWM outperforms existing methods by 2-5 points in accuracy while reducing memory usage by 24%.

---

## 论文详细总结（自动生成）

# 详细总结：DLWM（多样化潜在世界模型用于高效多模态推理）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有多模态推理方法（如显式思维链 CoT 或连续潜空间推理）通常假设输入只有一种潜在解读，沿着固定路径或在统一计算预算下展开推理。然而真实世界的视觉输入常受遮挡、模糊、视角变化或语义歧义的影响，存在多种可能解释。统一推理策略不仅限制了模型探索多种假设的能力，而且导致高内存和展开开销。
- **整体含义**：论文旨在解决多模态推理中 **感知不确定性** 和 **计算效率** 之间的矛盾。通过显式建模多个潜在世界假设，并在这些假设间自适应分配计算资源，提升推理鲁棒性和效率。

## 2. 论文提出的方法论

- **核心思想**：结合**多样化潜在世界假设**与**资源感知的强化学习（RL）**。在连续潜空间中构建多个具有语义差异的世界假设，每个假设对应输入的一种可能解释；然后将推理过程建模为预算受限的序贯决策问题，训练一个 RL 控制器动态决定是否扩展、终止或合并推理路径，最后通过加权融合输出答案。

- **关键技术细节**：
  1. **多样化潜在世界生成**：
     - 使用冻结的视觉-语言编码器 Encoder 得到联合表示 $z \in \mathbb{R}^d$。
     - 引入潜世界生成器 $G_\phi$，结合可学习世界嵌入 $e_i$ 产生 $K$ 个世界表示 $w_i = G_\phi(z, e_i)$。
     - 每个世界表示初始化独立的推理轨迹：$h^{(i)}_0 = w_i$, $h^{(i)}_{t+1} = f_\theta(h^{(i)}_t)$，其中 $f_\theta$ 是共享的潜推理模块（单 Transformer 块）。
     - **正交多样性正则化**：防止世界表示坍缩，定义损失 $\mathcal{L}_{\text{orth}} = \sum_{i \neq j} \left( \frac{w_i^\top w_j}{\|w_i\|\|w_j\|} \right)^2$，抑制方向相似性。
  2. **资源感知 RL 计算分配**：
     - 将推理过程建模为马尔可夫决策过程（MDP）。状态 $s_t$ 包含活跃世界表示集合 $A_t$ 和剩余预算 $B_t$。
     - 控制器 $\pi_\omega(a|s_t)$ 可从动作空间选取：$\text{Expand}(i)$（扩展一个世界）、$\text{Stop}(i)$（终止）、$\text{Merge}(i,j)$（合并两个世界）、$\text{Answer}$（终止并输出）。
     - 每次动作有对应成本（$c_{\text{exp}}, c_{\text{stop}}, c_{\text{merge}}$，Answer 为 0）。推理在 $B_t \leq 0$ 或 $A_t = \emptyset$ 或达到最大深度 $T_{\max}$ 时终止。
     - **奖励设计**：$R = R_{\text{acc}} - \lambda_T T - \lambda_K \bar{K}$，同时鼓励准确率和效率（$T$ 为总步骤，$\bar{K}$ 为平均活跃世界数）。
     - 通过策略梯度优化 $\pi_\omega$，目标 $J(\omega) = \mathbb{E}_{\pi_\omega}[R]$。
  3. **训练流程**：两阶段策略。第一阶段监督预训练（固定 rollout 长度）学习稳定表示和基础推理；第二阶段 RL 微调优化计算分配。生成器 $G_\phi$、推理模块 $f_\theta$、评分头 $s_\psi$ 联合优化 $\mathcal{L}_{\text{task}} + \lambda_{\text{orth}}\mathcal{L}_{\text{orth}}$，控制器独立用策略梯度更新。

- **算法流程**（文字说明）：
  - 对每个样本，编码得到 $z$；生成 $K$ 个世界表示 $w_i$；初始化活跃集 $A$、步数 $t=0$、预算 $B_0 = B$。
  - 循环：当 $t < T_{\max}$ 且 $B_t > 0$ 且 $A \neq \emptyset$ 时，控制器依据状态 $s_t$ 选择动作，执行对应操作并更新预算和活跃集。
  - 循环结束后，通过加权融合 $\sum_{i \in A} \alpha_i p_i(y)$（$\alpha_i = \text{softmax}(s_\psi(h^{(i)}_T))$ 输出最终预测。
  - 计算任务损失和正交损失，更新生成器、推理模块、评分头；计算奖励，更新控制器。

## 3. 实验设计

- **数据集与场景**：四个多模态推理基准：
  - **MMVP**：强调细粒度视觉感知和空间推理，视觉歧义高。
  - **GQA**：组合式、多跳关系推理。
  - **VQAv2**：多样日常场景，测试视觉-语言融合。
  - **ScienceQA**：结合视觉、文本和科学知识，需多步跨模态推理。
- **基准方法**：显式 CoT（Wei et al.）、**Coconut**、**CoLaR**、**LVR**、**LaRe**、**Heima**、**Soft Thinking**。所有方法使用相同骨干网络、相同最大计算预算。
- **评估指标**：
  - 准确率（标准准确率或 VQAv2 的共识准确率）。
  - 效率：平均 rollout 步数、平均活跃世界数、GPU 内存、推理吞吐量。

## 4. 资源与算力

- **硬件**：8×A100 GPU。
- **训练配置**：优化器 AdamW，学习率 $2\times10^{-5}$，batch size 64，混合精度训练。
- **训练阶段**：两阶段（监督预训练 + RL 微调），但具体训练时长（epochs 或迭代次数）**未明确说明**。
- **模型规模**：冻结 ViT-L/14 视觉编码器，推理模块 6 层 Transformer（隐藏层 4096），世界生成器为 2 层 MLP，默认 $K=4$，预算 $B=10$，最大深度 $T_{\max}=8$。

## 5. 实验数量与充分性

- **实验组数**：
  - 主结果：4 个数据集，对比 7 个基线，报告准确率和效率（表 1）。
  - **消融实验**：
    - 组件消融（表 2）：去除多世界、正交正则、RL 分配、合并动作、停止动作，共 6 个变体。
    - 世界数量 $K$（表 3a）：$K=1,2,4,6,8$，共 5 个设置。
    - 计算预算 $B$（表 3b）：$B=4,6,8,10,12$，共 5 个设置。
    - 动作空间消融（表 4）：4 种策略变体。
    - 训练策略（表 5）：4 种策略。
    - 聚合方法 + 多样性正则（表 6）：各 3-4 种变体。
  - 附有定性分析、复杂度分析、训练细节等。
- **充分性与公平性**：
  - 所有基线使用相同骨干、数据预处理、评估脚本和推理预算。
  - 报告多次随机种子上的平均值和标准差（附录 B.1.3）。
  - 消融覆盖关键组件（多世界、正则化、RL、动作），且讨论了收益随参数变化的饱和现象。
  - 实验设计客观公平，结论有充分支持。

## 6. 论文的主要结论与发现

- **准确率提升**：DLWM 在所有四个基准上达到最高准确率，超越最强基线（LVR、LaRe）2.4–5.2 个百分点，尤其在 MMVP（高视觉歧义）上提升最大（+5.2%）。
- **效率优势**：平均仅 5.0 步和 2.2 个活跃世界，内存 14.5 GB，吞吐率 8.9 样本/秒，与高效方法 Heima 相当但准确率更高。相比固定 rollout 方法内存降低约 24%。
- **多假设建模有效性**：去掉多世界（$K=1$）导致准确率大幅下降（MMVP 降 >5%），验证了核心贡献。
- **RL 分配有效性**：去掉 RL 分配使步骤回到 8.0、内存升至 18.2 GB，准确率也降低，表明动态分配既提升效率也改善推理质量。
- **动作互补性**：扩展、停止、合并三个动作各有作用，缺一不可。
- **两阶段训练**优于联合训练或单一阶段。

## 7. 优点

- **方法创新**：首次将显式多假设潜建模与资源感知 RL 结合，统一了多路径探索和计算效率优化。
- **正交正则化**：简单有效，防止假设坍缩，使各世界语义可区分。
- **RL 动作设计合理**：扩展、停止、合并三个动作分别控制深度、冗余剪枝和路径压缩，相互补充。
- **实验全面**：覆盖多个难度级别的数据集，消融细致，且公平比较（统一骨干、预算、预处理）。
- **性能-效率权衡出色**：优于单路径方法和固定 rollout 方法，实际计算量与高效方法相当。
- **两阶段训练策略稳定**：先监督预训练再 RL 微调，避免 RL 噪声破坏表示学习。

## 8. 不足与局限

- **多世界开销**：尽管 RL 缓解，但仍需生成 $K$ 个世界，对超大规模模型或实时系统可能仍有压力。
- **任务域限制**：仅在静态图像任务验证，未涉及视频理解或交互式推理（如具身智能）。
- **RL 训练不稳定性**：RL 超参数敏感，可能存在训练方差，文中承认“仍 exhibits 一些不稳定性”。
- **可解释性**：RL 策略的决策过程可能不透明，需要进一步研究可控性和解释性。
- **高利害场景的可靠性**：在自动驾驶、医疗图像等高风险场景下，多假设推理可能引入不确定性，需额外安全机制。
- **计算预算的手动设定**：预算 $B=10$ 是经验选择，可能需针对不同任务调整，缺乏自动预算学习方法。

（完）
