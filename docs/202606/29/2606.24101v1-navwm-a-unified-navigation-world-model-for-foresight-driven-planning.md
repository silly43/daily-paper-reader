---
title: "NavWM: A Unified Navigation World Model for Foresight-Driven Planning"
title_zh: NavWM：面向前瞻规划的通用导航世界模型
authors: "Yanghong Mei, Longteng Guo, Ming-Ming Yu, Guiyu Zhao, Xingjian He, Jing Liu"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24101v1"
tags: ["query:world-models"]
score: 10.0
evidence: 提出统一导航世界模型，集成潜在世界推理、多模态动作预测和可控视觉生成
tldr: 针对传统视觉导航策略的短视决策与模式崩溃问题，现有世界模型将感知、生成和控制分离，未能捕捉共享时空动态。本文提出统一导航世界模型NavWM，通过潜在世界令牌提取几何与语义先验，并引入基于锚点的多模态轨迹预测框架生成多样动作空间，使生成模型作为闭环规划器，利用视觉预视评估最优路径。在多种机器人数据集上，NavWM在高保真未来状态生成和零样本导航成功率上均取得显著提升，为生成式世界模型在导航中的应用提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24101v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 505}, {\"url\": \"assets/figures/arxiv/2606.24101v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1685, \"height\": 1050}, {\"url\": \"assets/figures/arxiv/2606.24101v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1697, \"height\": 990}, {\"url\": \"assets/figures/arxiv/2606.24101v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 807, \"height\": 704}, {\"url\": \"assets/figures/arxiv/2606.24101v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1704, \"height\": 417}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24101v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1584, \"height\": 480}, {\"url\": \"assets/tables/arxiv/2606.24101v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1166, \"height\": 399}, {\"url\": \"assets/tables/arxiv/2606.24101v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1350, \"height\": 320}, {\"url\": \"assets/tables/arxiv/2606.24101v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1390, \"height\": 381}]"
motivation: 传统视觉导航策略存在短视决策和模式崩溃，现有世界模型范式割裂感知、生成与控制，缺乏统一时空建模。
method: NavWM统一潜在世界推理、多模态动作预测与可控视觉生成，通过潜在世界令牌提取先验，基于锚点预测多样轨迹实现视觉预视规划。
result: 在多种机器人数据集上，NavWM在高保真未来状态生成和零样本导航成功率上均取得显著提升，超越现有方法。
conclusion: NavWM通过视觉预视闭环规划，有效克服短视与模式崩溃，为生成式世界模型在导航中实现鲁棒零样本泛化提供了新范式。
---

## 摘要
传统视觉导航策略在复杂环境中常面临短视决策与模式崩塌的问题。尽管世界模型提供了有前景的替代方案，但现有范式通常将感知、生成与控制相隔离，未能捕捉其共享的时空动态。本文提出NavWM——一种统一的导航世界模型，无缝融合了潜在世界推理、多模态动作预测与可控视觉生成。其核心是利用潜在世界标记提取几何与语义先验，赋予智能体鲁棒的结构理解能力。为克服确定性策略的局限，我们引入基于锚点的多模态轨迹预测框架，生成多样化的动作空间。这种内在的多样性明确赋予生成式世界模型作为鲁棒闭环规划器的能力，通过视觉前瞻评估并选择最优路径。在多种机器人数据集上的广泛实验表明，NavWM显著推进了现有技术水平，在高保真未来状态生成与零样本导航成功率方面均实现了显著提升。

## Abstract
Conventional visual navigation policies often struggle with myopic decision-making and mode collapse in complex environments. While world models offer a promising alternative, existing paradigms typically isolate perception, generation, and control, failing to capture their shared spatio-temporal dynamics. In this paper, we propose NavWM, a unified navigation world model that seamlessly integrates latent world reasoning, multimodal action prediction, and controllable visual generation. At its core, NavWM leverages latent world tokens to distill geometric and semantic priors, endowing the agent with robust structural understanding. To overcome the limitations of deterministic policies, we introduce an anchor-based multimodal trajectory forecasting framework that generates a diverse action space. This inherent diversity explicitly empowers the generative world model to act as a robust closed-loop planner, utilizing visual foresight to evaluate and select the optimal path. Extensive experiments across diverse robotics datasets demonstrate that NavWM significantly advances the state-of-the-art, delivering remarkable improvements in both high-fidelity future state generation and zero-shot navigation success.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **传统视觉导航策略**（如GNM、ViNT、NoMaD）学习从观测到动作的直接映射，属于**反应式策略**，缺乏对未来世界状态的显式推理，导致短视决策和对未知环境的泛化能力不足。
- **现有世界模型范式**（如NWM、UniWM）尝试引入未来预测，但通常将**感知、生成与控制分离**，未能捕捉三者共享的时空动态，导致表示次优。
- **具体挑战**：
  - 解耦设计忽略共享动力学；
  - 潜在表示缺乏显式场景抽象（几何/语义），长期预测不稳定；
  - 动作预测通常建模为单模态轨迹，引发模式崩塌，使智能体陷入局部最优。
- **本文目标**：提出**统一导航世界模型NavWM**，通过联合训练潜在世界推理、多模态动作预测和可控视觉生成，实现**前瞻驱动的闭环规划**。

### 2. 论文提出的方法论

#### 核心思想
- 将感知、生成和控制统一在**共享的Bidirectional Mamba骨干网络**中，利用**潜在世界令牌（Latent World Tokens）**提取几何与语义先验，通过**基于锚点的多模态轨迹预测**生成多样动作空间，并借助**视觉前瞻（visual foresight）**评估候选路径，实现生成式世界模型作为闭环规划器。

#### 关键技术细节

1. **潜在世界推理（Latent World Reasoning）**
   - 在编码特征中插入可学习的**潜在世界令牌**，通过CNN解码器预测未来帧的深度图$\hat{D}$和语义特征$\hat{S}$。
   - 深度监督使用**尺度不变损失**：
     $$L_{si} = \frac{1}{P}\sum_{p=1}^P g_p^2 - \frac{\lambda}{P^2}\left(\sum_{p=1}^P g_p\right)^2, \quad g_p = \log\hat{d}_p - \log d_p$$
   - 语义监督使用MSE损失，最终$L_{\text{reason}} = \lambda_{\text{depth}} L_{si} + \lambda_{\text{sem}} L_{\text{mse}}$。

2. **多模态轨迹提议（Multimodal Trajectory Proposal）**
   - 基于锚点的两阶段预测：
     - **目标预测**：离线聚类构建锚点码本$\mathcal{A} \in \mathbb{R}^{K\times 2}$，用MLP预测锚点概率$\hat{\pi}$和偏移$\Delta\hat{A}$，损失包含分类和Huber回归。
     - **轨迹生成**：以场景特征$H$和精炼目标$\tilde{\mathcal{A}}$为条件，直接预测完整轨迹$\hat{\mathcal{T}}_k$，仅对匹配锚点$k^*$计算Huber损失$L_{\text{traj}}$。
   - 总体动作损失：$L_{\text{action}} = L_{\text{anchor}} + \lambda_{\text{traj}} L_{\text{traj}}$。

3. **世界模型与视觉前瞻（World Modeling for Visual Foresight）**
   - 采用**条件扩散Transformer（CDiT）**，将预测动作转化为光流场$F$，与扩散时间步嵌入融合，通过AdaLN和交叉注意力注入场景特征$H$。
   - 使用**连续时间流匹配**训练生成网络$v_\theta$，优化目标：
     $$L_{\text{visual}} = \mathbb{E}_{\tau, z_0, z_1}\left[\|v_\theta(z_\tau, \tau, F, H) - (z_1 - z_0)\|_2^2\right]$$
   - 采样时直接从高斯噪声$z_0$通过常向量场积分得到未来图像。

4. **两阶段训练策略**
   - **第一阶段**：教师强制，用真实轨迹训练所有模块，时长100k步。
   - **第二阶段**：冻结骨干和动作头，微调CDiT，使其适应从预测轨迹中采样（对抗曝光偏差），时长50k步。
   - 多任务损失加权采用**不确定加权**：$\mathcal{L} = \sum_{k\in\mathcal{T}} \left(\frac{1}{2}\exp(-s_k)L_k + \frac{1}{2}s_k\right)$。

### 3. 实验设计

#### 数据集与场景
- **训练集**：Go Stanford、SCAND、RECON、HuRoN（涵盖室内外真实机器人数据）。
- **零样本测试集**：TartanDrive（越野环境，未见）。
- **任务**：图像目标导航（Image Goal Navigation），要求基于起始观测到达目标位置。

#### 对比方法
- **导航策略**：GNM、ViNT、NoMaD、NWM（结合NoMaD执行）、UniWM、Anole-7B。
- **视觉生成**：Diamond、NWM、UniWM。

#### 评估指标
- **导航**：绝对轨迹误差（ATE）、相对位姿误差（RPE）、平均方向误差（AOE）、最大平均方向误差（MAOE）、成功率（SR）。
- **生成质量**：PSNR、SSIM、LPIPS、DreamSim。
- **多样性**：平均成对距离（APD）。

#### 实验组别
1. **离线评估（表1）**：单步预测的导航精度与图像生成质量对比。
2. **导航Rollout（表2）**：在已见和未见环境中滚动执行多步过程，评估成功率和轨迹精度。
3. **模块消融（表3）**：分别移除潜在世界推理、动作预测、世界模型，观察对导航与生成的影响。
4. **训练阶段消融（图4）**：对比单阶段与两阶段训练对生成指标的影响。
5. **多模态方法与规划器有效性（表4）**：对比GMM、扩散、锚点方法的APD、ATE等，并检验世界模型规划器的作用。
6. **候选轨迹数$K$的影响（图5）**：不同$K$下三种多模态方法的成功率和轨迹误差变化。

### 4. 资源与算力

- **GPU**：8×NVIDIA A100（40GB显存）。
- **批量大小**：全局batch size=64。
- **训练步数**：第一阶段100,000步，第二阶段50,000步。
- **模型规模**：1.5B参数。
- **学习率**：预热500步至1e-4，余弦衰减至1e-5。
- **优化器**：AdamW。
- 论文明确报告了上述配置，但未给出总训练时长（小时）。

### 5. 实验数量与充分性

- **实验全面性**：覆盖离线单步、在线滚动、多消融、多模态方法比较、超参数分析；在5个数据集上评价，包含零样本设置。
- **客观性**：与多个SOTA方法（扩散、自回归、端到端）公平对比，指标涵盖低层（PSNR）和感知层（LPIPS, DreamSim）及导航标准指标。
- **公平性**：所有方法在相同数据划分和评估协议下比较，消融实验独立控制变量。
- **潜在偏差**：
  - 离线评估中NavWM仅随机采样多模态轨迹，未使用世界模型规划，而其他方法（如NWM）也使用类似策略，但未完全对齐控制变量。
  - 零样本仅TartanDrive，场景数量较少，泛化结论需更多数据支撑。
  - 对比方法（如Diamond）原本为Atari设计，迁移至导航可能表现欠佳，但论文仍引用公平对比。

### 6. 论文的主要结论与发现

- NavWM在**离线单步预测**中取得SOTA：ATE从0.302降至0.207，PSNR从14.17提升至17.34，LPIPS从0.282降至0.243。
- **导航Rollout**在已见环境成功率从66%提升至72%，未见环境从36%提升至44%，且轨迹误差大幅下降。
- 潜在世界推理、动作预测、世界模型三者**联合训练带来协同增益**，相互提升（表3）。
- **两阶段训练**显著改善图像生成质量（图4）。
- **锚点多模态轨迹预测**比GMM和扩散方法在多样性（APD）和精度上均更优（表4）。
- 世界模型作为**规划器**能有效提升导航性能，特别是对于锚点方法（表4）。
- 增加候选轨迹数$K$可提升导航效果，锚点方法对更大$K$鲁棒（饱和点约7个）。
- 定性结果（图3）显示NavWM能更好对齐真实轨迹并维持目标指向。

### 7. 优点

- **统一架构**：首次将潜在世界推理、多模态动作预测和视觉生成融合在单一模型，利用共享时空动态实现相互增强。
- **结构化场景理解**：通过潜在世界令牌显式学习几何和语义先验，避免黑箱特征学习，提升长期预测稳定性。
- **多模态动作设计**：锚点方法自然生成多样且物理合理的轨迹，避免模式崩塌，并为前瞻评估提供丰富候选集。
- **生成式闭环规划**：世界模型不用于开环模拟，而是作为规划器评估候选路径，实现视觉前瞻驱动决策。
- **生成质量高**：流匹配框架实现高质量快速采样，PSNR等指标显著领先。
- **零样本泛化**：在未见数据集TartanDrive上仍保持可观成功率，表明学到广泛适用的动力学。

### 8. 不足与局限

- **计算开销**：1.5B参数模型，8×A100训练，推理时需运行扩散采样和多轨迹评估，实时性可能受限，论文未讨论部署效率。
- **时间窗口限制**：仅使用4帧上下文预测4帧未来，对于需要长时间依赖的任务（如复杂城市场景）可能不够。
- **伪标签依赖**：深度和语义来自预训练模型（Depth Anything V2, SAM），这些模型的错误会传播到NavWM，影响鲁棒性。
- **零样本评估局限性**：仅用TartanDrive一个数据集，未见环境代表性不足；城市、狭窄走廊等场景未覆盖。
- **对比不够全面**：未与近期基于大语言模型的导航规划器（如NavGPT、LLaVA-Nav）对比；也未评估在完全未知环境中的物理机器人部署。
- **失败分析缺失**：未分析无法到达目标的原因（障碍物、运动模式错误、生成失败），可能隐藏特定偏差。
- **超参数敏感性**：候选数$K$、损失权重等对性能有影响，但仅消融了$K$，其他超参数固定，通用性待验证。

（完）
