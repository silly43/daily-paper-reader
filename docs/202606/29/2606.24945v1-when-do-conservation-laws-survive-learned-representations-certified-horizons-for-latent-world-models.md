---
title: When Do Conservation Laws Survive Learned Representations? Certified Horizons for Latent World Models
title_zh: 守恒定律何时在学习到的表示中幸存？潜在世界模型的认证视野
authors: Hongbo Wang
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24945v1"
tags: ["query:world-models"]
score: 10.0
evidence: 潜在世界模型，认证视界
tldr: 针对物理世界模型学习表示后守恒律的认证问题，提出证书视界方法，通过解码物理不变量并分解表示、读出和潜在动力学缺陷预算，预认证滚动步数。实验表明硬辛结构在已知相坐标下有效但无法跨学习图，软Lipschitz对齐的弱守恒在表示学习中存活，像素认证需读出任稳定子管。核心贡献是解码物理不变量而非潜变量，其鲁棒性可测量、认证和证伪。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24945v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1161, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24945v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1359, \"height\": 656, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24945v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24945v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1514, \"height\": 1016, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24945v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1552, \"height\": 1079, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24945v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24945v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1765, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24945v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1717, \"height\": 496, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24945v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1717, \"height\": 540, \"label\": \"Table\"}]"
motivation: 现有模型可能维持守恒但真实能量漂移，需可认证的守恒律及表示学习下的生存条件。
method: 提出证书视界，通过解码物理不变量并分解表示、读出和潜在动力学缺陷预算，预认证模型滚动步数。
result: 硬辛结构在已知相坐标有效但跨图差，软Lipschitz对齐在表示学习中存活，像素需读出任稳定子管。
conclusion: 核心是解码物理不变量而非潜变量，其鲁棒性可测量、认证和证伪。
---

## 摘要
我们问一个关于物理世界模型的表示学习问题：当一个模型学习到潜在表示后，守恒定律何时仍然可认证？一个认证视野从可测量的模型缺陷中预先界定了展开步骤在物理不变量的水平集上可证明地保持的步数。关键设计选择是认证什么：不是学习到的潜在哈密顿量或学习到的标量见证（模型可以在真实的能量中漂移的同时守恒这两者），而是通过解码潜在状态并评估已知不变量获得的解码物理不变量。围绕这个对象，我们推导出壳层-视野认证，其预算分解为表示、读出和潜在动力学缺陷，并有一个单调对齐桥，通过它一个软的学习见证为解码不变量产生认证视野，并在保守系统上的状态、学习提升和像素观测上测试它们。保守认证可以幸存于学习到的表示，但并非所有几何先验都同样幸存：硬典范辛结构在已知相坐标中产生最长的视野，却不跨过学习到的图表，而受控Lipschitz对齐的软不变量在我们测试的学习表示设置中幸存；像素认证在读出行稳定的子管上恢复；开普勒问题暴露了一个几何边界。因此中心对象不是潜在哈密顿量，而是解码物理不变量，其对表示学习的鲁棒性可以被测量、认证和证伪。

## Abstract
We ask a representation-learning question about physical world models: when does a conservation law remain certifiable after a model learns a latent representation? A certified horizon bounds -- in advance, from measurable model defects -- how many steps a rollout provably stays on a physical invariant's level set. The key design choice is what is certified: not a learned latent Hamiltonian or a learned scalar witness (a model can conserve either while drifting in true energy), but the decoded physical invariant obtained by decoding the latent state and evaluating the known invariant. Around this object we derive shell-horizon certificates whose budget decomposes into representation, readout, and latent-dynamics defects, with a monotone alignment bridge through which a soft learned witness yields a certified horizon for the decoded invariant, and test them across state, learned-lift, and pixel observations on conservative systems. Conservation certificates can survive learned representation, but not all geometric priors survive equally: hard canonical symplectic structure yields the longest horizons in known phase coordinates yet does not cross a learned chart, whereas a controlled-Lipschitz-aligned soft invariant survives in the learned-representation settings we test; pixel certification is recovered on a readout-stable sub-tube; and the Kepler problem exposes a geometric boundary. The central object is therefore not a latent Hamiltonian, but a decoded physical invariant whose robustness to representation learning can be measured, certified, and falsified.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当物理世界模型学习到潜在表示后，守恒定律（如能量守恒）是否仍然可被认证？即能否在模型展开之前，从可测量的模型缺陷中预先界定模型步数，使得展开后的预测值仍然保持在物理不变量的水平集内？
- **动机**：现代世界模型通过在潜在空间滚动来获取计算效率，但这也带来了信任难题——模型可能内部“守恒”一个标量，而真实的物理能量却在漂移。因此，需要一种独立于模型内部表示的、可证明的认证方法。
- **关键洞察**：认证的对象不应是学习的潜在哈密顿量$H_\theta$或学习到的标量见证$C_\omega$，而应是**解码的物理不变量**$\Psi(z) = H^\star(\Pi D_\psi z)$——即通过解码器$D_\psi$和读出映射$\Pi$从潜在状态恢复物理坐标后，评估已知物理不变量$H^\star$。
- **整体含义**：守恒认证可以在学习到的表示下幸存，但并非所有几何先验都同等幸存：硬典范辛结构在已知相坐标中最优，但无法跨学习图；而受控Lipschitz对齐的软不变量则在表示学习设置中更强健。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 建立一个**认证视野**（certified horizon）$T$，即模型在容忍度$\epsilon$内安全滚动的最大步数，其通用形式为：
  $$ T = \left\lfloor \frac{m_{c,\rho}\,\epsilon - \text{初始化缺陷}}{\text{每步漂移}} \right\rfloor $$
  其中$m_{c,\rho}$是壳层灵敏度（能量壳附近梯度下确界）。
- 不同表示下，初始化缺陷和每步漂移的来源不同，论文将其分解为：**表示缺陷**（嵌入误差）$\epsilon_0$、**读出缺陷**$\epsilon_{\Pi,\text{num}}$、**潜在动力学漂移**$\delta_{\text{tube phys}}$。

### 关键技术细节

#### 1. 状态壳认证（Proposition A）
- 直接使用物理坐标，一步状态缺陷$\delta_{\text{state}} = \sup_K |H^\star(f_\theta x) - H^\star(x)|$。
- 认证视野：
  $$ T_{\text{state shell}} = \left\lfloor \frac{m_{c,\rho}\,\epsilon - \epsilon_{\text{state}}^0}{\delta_{\text{state}}} \right\rfloor $$

#### 2. 潜在解码能量壳认证（Proposition B）
- 解码后的轨迹$\hat{x}_n = \Pi D_\psi F_\theta^n E_\phi(o_0)$。
- 缺陷分解：$\epsilon_0$（初始嵌入）、$\epsilon_{\Pi,\text{num}}$（解码/读出误差）、$\delta_{\text{tube phys}}$（潜在动力学漂移）。
- 认证视野：
  $$ T_{\text{latent shell}} = \left\lfloor \frac{m_{c,\rho}\,\epsilon - \epsilon_0 - \epsilon_{\Pi,\text{num}}}{\delta_{\text{tube phys}}} \right\rfloor $$

#### 3. 对齐桥（Proposition C）
- 软见证$C_\omega$必须与解码物理不变量对齐。引入校准函数$g_C$（Lipschitz连续），对齐缺陷$\eta_D = \sup_z |\Psi(z) - g_C(C_\omega z)|$。
- **负载条件**：$0 < \kappa \leq g'_C \leq L_g$（双Lipschitz），确保水平集双向对应。
- 认证视野：
  $$ T_{\text{align}} = \left\lfloor \frac{m_{c,\rho}\,\epsilon - 2\eta_D}{L_g \delta_C} \right\rfloor $$
  其中$\delta_C = \sup |C_\omega(Fz) - C_\omega(z)|$。
- 强调：单调性不够（等张拟合因Lipschitz常数过大使视野为零），必须受控Lipschitz。

#### 4. 联合不变量壳（Proposition D）
- 针对多不变量系统（如开普勒：能量和角动量），使用标准化向量$\tilde{I}$，每步漂移$\delta_I$，最坏情况灵敏度$\sigma_*$（需秩条件）。
- 认证视野：
  $$ T_{\text{joint}} = \left\lfloor \frac{\sigma_* \epsilon - \epsilon_0}{\delta_I} \right\rfloor $$
- 引入**合法重图防作弊**：任何重图$\varphi(H,L)$不得通过坍缩$\sigma_*$来膨胀视野（需$\sigma_*^J \geq 0.3\sigma_*^{A0}$，条件数$\leq 50$）。

### 算法流程
1. 训练：编码器$E_\phi$、潜在动力学$F_\theta$、解码器$D_\psi$。软见证$C_\omega$通过自监督训练（不变性+反坍缩项），不使用$H^\star$。
2. 校准：在保留的壳层上拟合校准函数$g_C$（受控Lipschitz样条），并估计缺陷（$\epsilon_0, \delta_{\text{tube phys}}, \eta_D$等）。
3. 认证：对给定容忍度$\epsilon$，计算视野$T$，并限制在管道$K_{c,\rho}$内（排除读出任不稳定或秩不足的点）。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

### 实验系统
- **简单摆**（单不变量）、**谐振子**（单不变量）、**开普勒二体问题**（双不变量，且唯一奇点系统）。
- 所有系统都有已知的物理不变量$H^\star$（以及开普勒的角动量$L$），但模型在训练和评估中从未使用$H^\star$——仅用于最终评估。

### 表示层次
论文按**表示抽象度**滑动：状态（物理坐标直给）→ 提升（低维观测，学习编码）→ 像素（高维图像，学习编码+解码+读出）。
- **状态**：模型直接接收$(q,p)$，读出为恒等，$\epsilon_{\Pi,\text{num}}=0$。
- **提升**：编码器从低维观测学习潜在图；解码器+读出准确恢复$(q,p)$，$\epsilon_{\Pi,\text{num}}\approx0$，认证受动力学限制。
- **像素**：观测是高维图像，解码器/读出是主要误差源，$\epsilon_{\Pi,\text{num}}$可能占主导。

### 对比方法（三种潜在动力学先验）
1. **Plain**（无结构基线）：无约束残差潜在映射。
2. **Hard symplectic**（硬辛结构）：使用分离的Störmer–Verlet步骤或SympNet，标准哈密顿世界模型的归纳偏置。
3. **Soft (B-self)**（软自监督见证）：自监督训练的标量（或向量）见证$C_\omega$，训练时不访问$H^\star$，通过对齐桥支持认证。

额外：一个特权控制器（使用真实$H,L$）仅作为参考消融。

### Benchmark/评价指标
- **非空认证**（non-vacuous）：$T \geq 1$（对某个容忍度$\epsilon$）。
- **种子级胜率**$P(T_B > T_{\text{plain}})$。
- **结构增益比**$R_{\text{shell}} = T_{\text{soft}}/T_{\text{plain}}$。
- **对齐审计**$R^2$：$g_C \circ C_\omega$与$\Psi$的相关性。
- **排除比例**（像素子管）$\leq 2.3\%$。

## 4. 资源与算力

论文没有明确说明使用的GPU型号、数量或训练时长。仅在附录中提及：
- 环境：macOS-arm64（Python 3.11.15, PyTorch 2.12.0）和 Linux-x86_64（Python 3.12.3, PyTorch 2.8.0+cu128, NumPy 2.1.2）。
- 使用确定性核和线程固定（`use_deterministic_algorithms=True`，BLAS/OMP线程固定）。
- 未报告GPU型号、GPU数量或总训练时间。这属于实验信息不透明的局限。

## 5. 实验数量与充分性

### 实验数量
- **状态开普勒**：3个种子，plain vs symplectic。
- **提升摆**：8个种子，软见证（B-self） vs plain vs symplectic（含Verlet和SympNet）。
- **对齐桥（WS2）**：8个种子（校准审计），加上10个种子（见证建立审计）。
- **像素恢复（WS1）**：10个种子，软见证 vs plain。
- **开普勒提升救济阶梯**：容量实验（$d_z\in\{8,16,32\}$）、合法重图实验（4种重图）。
- **消融实验**：像素恢复的消融（移除真实时间不变性、移除方差项、使用堆栈解码器），对齐桥的消融（样条 vs 等张 vs 仿射，以及多种控制伪影）。

### 充分性评估
- **充分**：实验覆盖三个表示层次（状态、提升、像素），两个系统（摆、开普勒），多种种子数量（8~10），并提供消融研究验证关键组件。
- **公平**：使用不相交的种子三连（训练、评估、校准），确定性核，预注册的冻结认证规则（禁止调参通过）。对比方法间数据和超参数匹配。
- **客观**：报告种子级统计（非空计数、胜率），而非单点值。开普勒负面结果通过阶梯式归因（排除容量、重图等解释）。
- **局限**：系统仅三个（摆、谐振子、开普勒），未拓展到更多自由度或耗散系统。认证视野本身较短（开普勒$T_{\text{joint}} \leq 7$）。像素认证仅在读出任稳定子管上（非全局管道）。

## 6. 论文的主要结论与发现

1. **状态表示**下，硬辛结构明显胜出：开普勒联合认证，symplectic 3/3种子非空 vs plain 0/3，每步漂移$\delta_I$约7.6倍改善。
2. **提升表示**下，硬辛结构失效（Verlet和SympNet与plain几乎无差异），而软(B-self)见证生存，结构增益比约1.30，$P(\text{soft} > \text{plain}) = 0.88$（8种子）。
3. **对齐桥**需要**受控Lipschitz单调**校准：样条($\kappa=0.242, L_g=0.606$)产生非空视野$T_{\text{align}}=5.1$（$\epsilon=1.0$），而等张单调校准（$L_g\sim10^2$）使视野为零——仅单调性不够。
4. **像素认证**在读出任稳定子管上恢复：软见证在9/10种子非空（plain仅4/10），排除比例$\leq 2.3$%。
5. **开普勒提升**暴露了**几何边界**：近中心点附近$\partial H/\partial r \sim r^{-2}$放大了小误差，该缺陷对容量扩大（1.21×改善）和合法重图（2.8×改善）都鲁棒，且始终$T_J=0$。这是由系统反平方奇点决定的几何限制，而非表示学习失败。

## 7. 优点：方法或实验设计上的亮点

- **清晰的认证对象定义**：明确区分“解码物理不变量”与“学习到的潜在哈密顿量/见证”，并坚持认证前者，避免了潜在模型内部自洽但真实能量漂移的陷阱。
- **统一的认证视野框架**：通过三个预算项（表示、读出、动力学）层叠，使得状态、提升、像素三个层次可以公平比较，且任何差距都能归因到具体组件。
- **严格的实验纪律**：
  - 使用不相交的种子三连（训练、评估、校准）。
  - 认证规则在最终运行前冻结，避免调参通过。
  - 报告种子级非空计数和胜率，而非单点最佳值。
  - 对负面结果（开普勒提升）进行阶梯式归因，排除容量、重图等替代解释。
- **软见证训练**完全自监督（不使用$H^\star$），且通过对齐桥转换为可认证对象，控制实验（打乱、随机标量、轨迹索引、新鲜头）全部倒塌，证明对齐真实。
- **合法重图防作弊**引入条件数$\leq 50$和$\sigma_*$下限，确保重图无法通过数值技巧膨胀视野。

## 8. 不足与局限

- **实验系统规模小**：只测试了三个低维保守系统（摆、谐振子、开普勒），未拓展到多自由度或非保守系统（如耗散或受控系统）。
- **认证视野短**：即使最佳情况下，认证步数也只有个位数（开普勒$T_{\text{joint}}\leq 7$，摆$T_{\text{align}}=5.1$），实际部署仍需更长视野。
- **像素认证是局部的**：仅在读出任稳定子管上成立（排除比例$\leq 2.3\%$），不是全局管道上的认证。
- **资源信息不透明**：未报告GPU型号、数量或训练时长，难以评估方法的计算成本。
- **理论部分有未依赖结果**：Proposition D（反向误差/影子哈密顿对齐）和F（阴影）仅作为条件性/程序级陈述，未在实际实验中验证，限制了理论贡献的全面性。
- **开普勒几何边界是系统特定的**：不适用于所有提升设置，论文也承认这一点，但读者可能误认为提升认证普遍困难。
- **未测试更高维观测或部分可观测系统**：论文明确限定“小系统”，并指出向实际系统的扩展不是本文目标，但这是实际应用的必要方向。

（完）
