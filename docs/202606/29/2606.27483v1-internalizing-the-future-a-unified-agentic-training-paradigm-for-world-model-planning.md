---
title: "Internalizing the Future: A Unified Agentic Training Paradigm for World Model Planning"
title_zh: 内化未来：面向世界模型规划的统一智能体训练范式
authors: "Xuan Zhang, Zhijian Zhou, Lingfeng Qiao, Yulei Qin, Ke Li, Xing Sun, Xiaoyu Tan, Chao Qu, Yuan Qi"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27483v1"
tags: ["query:world-models"]
score: 10.0
evidence: 直接提出智能体中的世界模型规划
tldr: 标准LLM agent在长程任务中缺乏内部世界模型进行前瞻性规划。为此提出统一训练范式：先通过世界模型代理中间训练注入预测能力，再通过格式引导SFT结构化能力，最后通过前瞻条件强化学习校准模拟。在搜索和数学推理任务上，该方法显著优于其他基线。结果表明，有效内部世界建模需要能力先行的训练流程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27483v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1377, \"height\": 777, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27483v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1705, \"height\": 815, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27483v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1524, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27483v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1400, \"height\": 1078, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1253, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 899, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1605, \"height\": 1448, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1218, \"height\": 409, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1300, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 617, \"height\": 540, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1297, \"height\": 409, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 617, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1549, \"height\": 565, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1714, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1553, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 1332, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27483v1/table-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 1550, \"height\": 585, \"label\": \"Table\"}]"
motivation: 标准LLM agent缺乏内部世界模型进行前瞻性规划，仅能反应式决策。
method: 三阶段训练：WM-AMT注入预测能力，FE-SFT结构化能力，FC-RL校准模拟。
result: 在搜索和数学推理任务上，该方法优于标准微调、SFT和RL基线。
conclusion: 有效内部世界建模需要能力先行的训练流程，实现有根据的前瞻性规划。
---

## 摘要
大语言模型智能体在序贯决策中展现出强大能力，但在长时域任务中仍本质上是反应式的。与人类在行动前运用“假设”推理评估潜在计划不同，标准智能体缺乏用来模拟未来结果的内部世界模型。因此，我们提出通过训练单一自回归模型来内化面向未来的规划能力，使其能够同时表述预期的状态推演和基于计划成功率的估计——一种Q值的文本对应物。关键的是，我们识别出格式-能力差距：在后期训练中仅对前瞻轨迹进行微调会导致对未来迹象的表层模仿，而缺乏真正的预测基础。为弥补这一差距，我们引入一个三阶段训练范式：(i) 世界模型智能体中训练，将潜在预测能力注入策略；(ii) 格式引发监督微调，构建此注入能力的结构；(iii) 前瞻条件强化学习，优化生成模拟的校准与效用。在搜索和数学推理任务上的评估表明，我们的方法一致优于其他训练基线。结果表明，在LLM智能体中实现有效的内部世界建模需要以能力为先的训练流程，以实现扎实且校准的前瞻能力。

## Abstract
Large language model (LLM) agents have demonstrated strong capability in sequential decision-making, yet they remains fundamentally reactive in long-horizon tasks. Unlike humans who employ "what-if" reasoning to evaluate potential plans before commitment, standard agents lack an internal world model to simulate future outcomes. Therefore, we propose to internalize future-aware planning by training a single autoregressive model to verbalize both a prospective state rollout and a plan-conditioned success estimate-a textual analogue of the Q-value. Crucially, we identify a format-capability gap: simply fine-tuning agents on look-ahead traces during post-training leads to superficial mimicry of foresight without genuine predictive grounding. To bridge this gap, we introduce a three-stage training paradigm: (i) World Model Agentic Mid-Training (WM-AMT) to inject latent predictive capabilities into the policy; (ii) Format-Eliciting SFT (FE-SFT) to structure this injected capability; and (iii) Foresight-Conditioned Reinforcement Learning (FC-RL) to refine the calibration and utility of the generated simulations. Evaluated on search and mathematical reasoning tasks, our approach consistently outperforms other training baselines. Our results demonstrate that effective internal world modeling in LLM agents requires a capability-first training pipeline to achieve grounded and calibrated foresight.

---

## 论文详细总结（自动生成）

```markdown
# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**研究动机**：
- 标准LLM智能体在长时域任务中本质上是反应式的，缺乏类似人类的“假设推理”（what-if reasoning）能力——即在行动前模拟和评估未来可能的结果。
- 人类决策依赖于内部世界模型（small-scale model of external reality），允许在脑中预演和比较不同计划后再执行。当前LLM agent缺乏这种内部化的前瞻规划能力。

**核心问题**：
- 如何让LLM agent通过单自回归模型内化世界模型，使其能够口头表达前瞻性状态推演和基于计划的成功估计（Q值的文本对应物），而不使用独立的价值头或外部模拟器。

**识别关键瓶颈**：
- 作者识别出“格式-能力差距”（format-capability gap）：仅仅在后期训练（post-training）中让模型学习包含前瞻块（look-ahead traces）的格式数据，模型会学会表面上的结构模仿（产生看似合理的模拟文本），但缺乏真正的预测基础，甚至可能产生误导性噪声。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：
- 提出一种统一的三阶段训练范式，将世界模型规划能力内化进单一自回归策略中。三个阶段分别为：
  1. **世界模型智能体中训练（WM-AMT）**：通过在大规模智能体轨迹中插入压缩的未来经验摘要（包括轨迹预测和Q值估计），注入潜藏的“预测-行动”能力。
  2. **格式引发监督微调（FE-SFT）**：结构地引出已注入的能力，让模型学会在特定标记模式下生成结构化前瞻块（imaginary → keyword → analysis → confidence → think → action）。
  3. **前瞻条件强化学习（FC-RL）**：通过组合奖励（任务成功、前瞻基础性、置信度校准）同时优化策略和世界模型的质量。

**关键技术细节**：

- **WM-AMT**：
  - 对每个轨迹随机选择一些步骤t，使用总结函数$\Phi(\cdot)$生成结构化世界模型块$z_t = [z_t^{(traj)} \oplus z_t^{(eval)}]$，其中$z_t^{(traj)}$是抽象的未来路径摘要（里程碑），$z_t^{(eval)}$包含语义差距分析和Q值估计$q\in[0,100]\%$（表示按此路径成功解决原始任务的置信度）。
  - 然后注入$z_t$到原始轨迹中形成增强轨迹$\tilde{\tau}$，通过标准自回归负对数似然损失优化策略$\theta$：
    $$
    \mathcal{L}_{\text{WM-AMT}}(\theta) = -\mathbb{E}_{\tilde{\tau} \sim D_{\text{WM-AMT}}}\left[\sum_{i=1}^{|\tilde{\tau}|} \log P_\theta(u_i | u_{<i})\right]
    $$
  - 该阶段强制模型在给定历史到$s_t$的条件下，自回归地重建复合块$z_t$，从而隐式地注入世界模型能力。

- **FE-SFT**：
  - 构建任务特定的指令调优数据集$D_{\text{FE-SFT}}$，包含结构化前瞻块（用标签包围：`<imaginary>...</imaginary>`, `<keyword>...</keyword>`, `<analysis>...</analysis>`, `<confidence>...</confidence>`），然后生成实际动作。
  - 同样使用标准自回归损失进行优化。由于WM-AMT已建立因果先验，FE-SFT避免了从头开始学习世界模型时的幻觉。

- **FC-RL**：
  - 智能体生成的完整轨迹结构：世界模型跨度（imaginary → keyword → analysis → confidence）和策略跨度（think → action）。
  - **世界模型优化**包括两个奖励：
    - 基础奖励$R_{\text{ground}}$：验证预测的关键词是否在后续实际执行中出现（$R_{\text{ground}} = R_{\text{match}}(K_t^{(i)}, \tau_{>t}^{(i)})$）。
    - 校准奖励$R_{\text{calib}}$：使用Brier得分惩罚置信度估计与最终结果之间的偏差：
      $$
      R_{\text{calib}} = I - (q_t^{(i)} - I)^2
      $$
      其中$I$是二元成功指示符。
    - 组合的世界模型奖励$R_{\text{WM}}$，然后进行组内归一化得到优势$A(\tau^{[\text{world_model}]}_{ti})$。
  - **策略优化**：使用全局优势$A_E(\tau_i)$和基于置信度的步骤级优势$A_S(\tau_{ti})$的组合：
    $$
    A(\tau^{[\text{policy}]}_{ti}) = A_E(\tau_i) + \omega \cdot A_S(\tau_{ti})
    $$
    其中$\omega$是平衡权重（实验中搜索任务取0.2，数学推理取0.1）。通过组相对策略优化（GRPO）更新策略。
  - 所有优化都在单一因果语言模型中进行，不引入额外价值头。

## 3. 实验设计

**数据集/场景**：
- **搜索任务**：7个搜索增强QA数据集，包括单跳（NQ, TriviaQA, PopQA）和多跳（HotpotQA, 2Wiki, MuSiQue, Bamboogle）。使用2018年维基百科转储作为知识源，E5作为检索器，检索top-3段落。
- **数学推理任务**：AIME 2024, 2025, 2026。评估指标为mean@30和pass@30（每个问题采样30次）。

**基准方法**：
- 基础模型：Youtu-LLM-2B的一个中间检查点（预训练后，mid-training前）。对照两个mid-training变体：Youtu-LLM-2B-Base（标准智能体中训练200B tokens，不修改结构）和Youtu-LLM-2B-Base (WM-AMT)（使用相同数据但插入世界模型块）。
- 对比方法包括：
  - Post-Training Only*：仅在标准Base上进行SFT/RL。
  - State-Only Prediction (Post-Training Only)† 和 (With Mid-Training)‡：预测未来状态但省略Q值。
  - IWM [15]：应用现有世界模型数据生成方法。
  - 不同组合：SFT vs FE-SFT，SFT+RL vs FE-SFT+RL vs FE-SFT+FC-RL，以及不同mid-training基座的对比。
- 额外在Llama-3.2-3B-Instruct和Qwen2.5-7B-Instruct上进行初步实验验证格式-能力差距的普遍性。

**超参数**：
- WM-AMT：AdamW优化器，学习率5e-5，余弦调度，最小学习率1e-7，1 epoch，批量大小752，数据最大长度32768。
- FE-SFT：学习率5e-5（搜索）/1e-5（数学），余弦调度，2 epoch（搜索）/6 epoch（数学），批量大小256，最大长度18000（搜索）/20000（数学）。
- FC-RL：策略学习率1e-6，批量大小256（搜索）/128（数学），每组样本数5（搜索）/16（数学），温度1.0。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或训练小时数。
- 仅提及模型规模为Youtu-LLM-2B（~2B参数），mid-training数据为200B tokens（其中WM-AMT使用约62B+21B的增强数据）。FE-SFT和RL的数据规模较小（~2k-4k轨迹）。具体算力成本未报告。

## 5. 实验数量与充分性

**实验数量**：
- 搜索任务：在7个数据集上报告结果，包括不同训练阶段（SFT/RL前和最终性能），对比多种基线（不同mid-training、不同后期训练方法、有无状态预测、有无置信度）。
- 数学推理：3个AIME年份，每个报告mean@30和pass@30，同样多种基线对比。
- 消融实验：敏感性分析（$\omega$在FC-RL中的影响），基础奖励$R_{\text{ground}}$的消融，响应长度分析，以及RL-only基线分析。
- 额外在另外两个基座模型上验证格式-能力差距的普遍性。

**充分性与公平性**：
- 对比了多种方法，包括仅状态预测、IWM等，涵盖不同训练阶段（SFT、RL）的组合，较为全面。
- 控制mid-training数据量相同（200B tokens），仅改变结构（WM-AMT vs 标准）。
- 实验设置详细（检索参数、评估指标、超参数），但未报告多次重复实验的方差。
- 数学推理采用30次采样，减少随机性影响；搜索使用贪心解码（greedy sampling）。
- 总体充分且客观，但缺乏跨多个随机种子的统计显著性检验。

## 6. 论文的主要结论与发现

- **三阶段范式有效性**：完整的WM-AMT + FE-SFT + FC-RL在搜索和数学推理任务上一致优于所有其他基线。最终模型在搜索任务上平均得分50.6，数学推理上mean@30 29.5，pass@30 60.0。
- **格式-能力差距确实存在**：仅在后期训练中使用前瞻格式获得高格式遵从率（>95%），但性能提升有限，甚至可能下降（如标准Base上FE-SFT + RL vs SFT + RL）。
- **WM-AMT的必要性**：替换标准mid-training为WM-AMT后，在相同后期训练方法下显著改善性能（如SFT 38.2→39.9，FE-SFT 37.4→41.8）。
- **FE-SFT的作用**：在WM-AMT基础上，FE-SFT优于标准SFT（41.8 vs 39.9）。
- **FC-RL的增益**：在FE-SFT基础上，FC-RL优于标准RL（50.6 vs 49.1），且优于状态预测基线（48.7）。
- **特别在复杂多跳任务上改进显著**：如MuSiQue从20.6提升到34.2，2Wiki从31.2提升到46.2（对比Base+标准SFT+RL）。
- **定性分析**：置信度与任务成功正相关（正确时高，错误时低），且前瞻块能保持长时域规划连贯性。

## 7. 优点

1. **创新性地识别并解决格式-能力差距**：首次明确指出现有世界模型训练中的关键问题，并设计能力先行的训练流程。
2. **统一的自回归框架**：无需独立价值头或外部模拟器，所有能力内化于单一LLM中，保持架构简洁。
3. **三阶段逐步递进**：先注入能力（WM-AMT），再结构化引出（FE-SFT），最后通过奖励优化校准（FC-RL），逻辑清晰且有效。
4. **前瞻块设计合理**：包含轨迹预测（关键字里程碑）和Q值估计（置信度），使模型产生可验证、可校准的模拟。
5. **FC-RL的奖励设计**：结合基础性验证（关键词匹配）和Brier得分校准，鼓励真实预测和准确置信度，同时避免奖励黑客。
6. **实验验证充分**：涵盖两个代表性任务（搜索和数学推理），多个数据集，多种基线，以及消融和敏感性分析。
7. **定性案例展示**：提供了具体例子说明模型如何在内化世界模型后表现出长程规划和校准置信度，增强说服力。
8. **方法可迁移**：初步在Llama-3.2-3B和Qwen2.5-7B上验证了格式-能力差距的普适性，暗示方法具有通用潜力。

## 8. 不足与局限

1. **算力信息缺失**：未报告具体GPU型号、数量、训练时长等资源消耗，难以评估工程可复现性和扩展成本。
2. **仅使用2B参数模型做主要实验**：虽然选择合理，但未在更大模型（如7B、13B、70B）上验证三阶段范式的有效性，普适性存疑。
3. **数学推理任务仅使用AIME**：AIME是竞赛级难度，缺少与其他常见基准（如GSM8K, MATH）的对比，覆盖面不足。
4. **搜索任务检索固定为top-3**：没有探讨不同检索数量的影响，可能与实际应用灵活性不符。
5. **未见方差或显著性检验**：结果以单次数值或平均报告，缺乏多次重复的置信区间或统计检验，可能受随机性影响。
6. **FC-RL中$\omega$的选取依赖手动调优**：敏感性分析显示$\omega=0.2$最优，但未见自适应或理论推导。
7. **定性案例分析有限**：仅提供了少数示例，未系统量化人类评估或自动化评估前瞻块的质量。
8. **应用限制**：方法依赖大规模轨迹数据（200B tokens）的mid-training，对仅做后期训练或数据有限的场景可能不适用。此外，前瞻块增加了推理时token数（平均增加3-18%），在延迟敏感场景需要注意。
9. **未见错误分析**：未讨论失败案例的类型或原因（如什么样的任务仍会失败，前瞻块何时失效）。

（完）
```
