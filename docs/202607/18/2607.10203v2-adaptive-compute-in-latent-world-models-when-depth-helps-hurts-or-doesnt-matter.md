---
title: "Adaptive Compute in Latent World Models: When Depth Helps, Hurts, or Doesn't Matter"
title_zh: 潜在世界模型中的自适应计算：深度何时有帮助、有害或无关紧要
authors: Achyuthan Sivasankar
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10203v2"
tags: ["query:world-models"]
score: 10.0
evidence: 潜在世界模型自适应计算深度分析
tldr: 自适应计算的世界模型通常假设深度能提升预测精度。本研究通过预注册的浅层惩罚ρ，在9个DeepMind Control任务上评估深度在自回归展开中的实际效果。结果发现三种情况：深度有帮助（6/9任务），浅层反而更好（2/9，ρ低至0.85），以及一个平坦任务。反转现象由训练监督方式造成，形成逻辑困境。该发现揭示了深度收益的非单调性，为自适应路由设计提供了关键指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10203v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1220, \"height\": 683, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1397, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 634, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 657, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 717, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 628, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1228, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1315, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1413, \"height\": 1162, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v2/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1425, \"height\": 559, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10203v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 965, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10203v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 186, \"label\": \"Table\"}]"
motivation: 验证自适应计算世界模型中深度每步提升能否在自回归展开中保持，识别深度何时有益、有害或无关。
method: 使用预注册的浅层惩罚ρ（浅层退出与全深度展开的错误比），在9个DeepMind Control任务上对比单步和多步训练。
result: 6/9任务深度有益（ρ最高4.7倍），2/9浅层反转（ρ降至0.85），1个平坦；反转由早期退出监督方式导致。
conclusion: 深度收益取决于任务和训练，部分可预测；规划中深度优势与ρ符号一致，需关注度量空间和编码器影响。
---

## 摘要
自适应计算世界模型——提前退出或深度混合预测器，每步使用可变深度——假设深度能带来更好的预测，并且可以自适应路由。在自回归展开中，第一个假设要求深度每步的精度能够经受组合。我们使用预先注册的工具——浅层惩罚ρ = err(最浅退出展开)/err(全深度展开)——在九个DeepMind Control任务上进行了测试，匹配单步(K=1)和多步(K=4)训练，每个设置三个随机种子。我们发现了三种情况：在6/9的任务中，深度有助于展开（内在，ρ高达4.7倍）；在2/9的任务中，浅层退出击败了全深度（反转，ρ低至0.85倍）；还有一个任务是无差异的。稳健的反转（cheetah）不是动力学的属性，而是由训练造成的：仅在第一展开步监督早期退出的消融实验消除了反转（ρ: 0.87→1.18, n=8, Δ=+0.31），而内在权衡任务则不受影响——这是一种双重分离，我们称之为可路由性第22条军规，因为使退出可路由的监督正是训练它们比全深度更优展开的因素。这种情况部分可先验预测：观测/动作维度与单步模型误差与ρ的相关性|斯皮尔曼|≈0.75 (n=9)。在CEM规划器中，ρ的符号预测规划是否受益于深度，在反转任务上最为明显，其中浅层规划胜过了深度规划。最后，三个注意事项：任务的情况取决于度量空间、展开步数和编码器。所有阈值和门控在计算活动之前就已固定，包括针对激发本研究假设的预先注册的否定结果。

## Abstract
Adaptive-compute world models -- early-exit or mixture-of-depths predictors that spend variable depth per step -- assume depth buys better predictions and can be routed adaptively. In autoregressive rollouts, the first assumption requires depth's per-step precision to survive composition. We test this with a pre-registered instrument, the shallow penalty $ρ=\mathrm{err}(\text{shallowest-exit rollout})/\mathrm{err}(\text{full-depth rollout})$, across nine DeepMind Control tasks under matched single-step ($K=1$) and multi-step ($K=4$) training, three seeds each. We find three regimes: on 6/9 tasks depth helps rollouts (intrinsic, $ρ$ up to $4.7\times$), on 2/9 the shallow exits beat the full stack (inversion, $ρ$ down to $0.85\times$), and one is flat. The robust inversion (cheetah) is not a property of the dynamics but is created by training: an ablation supervising early exits only at the first rollout step erases it ($ρ: 0.87\to1.18$, $n=8$, $Δ=+0.31$), while an intrinsic-tradeoff task is unaffected -- a double dissociation we call the routability catch-22, since the supervision that makes exits routable is what trains them to out-roll the full stack. The regime is partly predictable a priori: observation/action dimensionality and one-step model error correlate with $ρ$ at $|\text{Spearman}|\approx0.75$ ($n=9$). Inside a CEM planner, $ρ$'s sign predicts whether planning benefits from depth, most sharply on the inversion task, where shallow planning beats deep. Finally, three cautions: a task's regime depends on the metric space, the rollout horizon, and the encoder. All thresholds and gates were fixed before the compute campaign, including a pre-registered negative for the hypothesis that motivated the study.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：自适应计算世界模型（如早期退出或混合深度网络）假设每步使用更深预测器能获得更好的下一个潜在状态，并且一个路由器可以选择每步需要的深度。然而，在自回归展开（如规划中的多次调用）中，这个假设是否成立？深度在单步上的高精度能否在组合后保持？
- **整体含义**：如果深度在自回归展开中并不总是有益，那么基于深度的自适应路由在规划器中可能失效甚至有害。论文通过预注册的单一指标$\rho$，系统性地识别了三种深度-组合行为模式（内在、反转、平坦），揭示了深度收益的非单调性，并为自适应计算世界模型的设计提供了必须考虑的条件。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：定义一个**浅层惩罚**$\rho = \frac{\mathrm{err}(\text{最浅退出展开})}{\mathrm{err}(\text{全深度展开})}$，其中$\mathrm{err}$是平均潜在空间误差。$\rho > 1$表示深度有益（内在），$\rho \approx 1$表示平坦（路由无意义），$\rho < 1$表示浅层更好（反转）。该指标在固定检查点上一次计算，并用于分类任务。
- **关键技术细节**：
    - 使用**块堆叠潜在预测器**，$d_{\text{model}}=256$，MLP比例为2，共有12个残差块，每2块设一个早期退出头（深度2,4,...,12）。
    - 训练使用**JEPA损失**（对EMA目标进行潜在MSE）、**深度监督**（每个早期退出头）、**蒸馏**（蒸馏权重0.5）和**VICReg方差/协方差正则化**。
    - **多步训练对比**：训练孪生模型，仅在训练步数$K$上不同：$K=1$（单步）和$K=4$（潜在超射，通过4步全深度展开反向传播）。两者使用相同的预收集数据、相同的批次顺序，因此仅在损失函数上不同。
- **算法流程**（文字说明）：
    1. 收集每个任务80条长度为1000的随机轨迹，作为固定数据集。
    2. 训练$K=1$和$K=4$两个孪生模型，每个模型在早期退出头上进行深度监督。
    3. 对每个训练完成后的检查点，编码256个保持窗口的潜在状态$z_0$，以每个固定退出深度$d$进行10步自回归展开，测量平均潜在误差$e_d$（与EMA编码的真实未来比较）。
    4. 计算$\rho = e_{d_{\min}} / e_{\text{full}}$，其中$d_{\min}$为最浅退出深度。
    5. 根据预注册的阈值（见表3）将任务分类为内在、反转、平坦或边界。
- **公式**：
    - 浅层惩罚：$\rho = \frac{\mathrm{err}(\text{shallowest-exit rollout})}{\mathrm{err}(\text{full-depth rollout})}$
    - 阈值（以$\rho$值判断）：
        - 内在：$\rho_{K1} \ge 1.25$
        - 反转：$\rho_{K1} < 1.15 \land \rho_{K4} < 0.90$
        - 平坦：$\rho_{K1} < 1.15 \land 0.90 \le \rho_{K4} < 1.25$
        - 创建：$\rho_{K1} < 1.15 \land \rho_{K4} \ge 1.25$
        - 边界：其他

## 3. 实验设计：使用哪些数据集/场景、基准、对比方法

- **数据集/场景**：使用**DeepMind Control Suite (DMC)** 中的9个连续控制任务：`finger-turn_hard`, `acrobot-swingup`, `reacher-easy`, `cartpole-balance`, `walker-walk`, `hopper-stand`, `humanoid-run`, `cheetah-run`, `quadruped-walk`。每个任务从随机策略收集80条1000步轨迹作为固定数据集。
- **基准**：没有明确的外部基准，但每个任务自身有一个**持续性基线**（persistence baseline），即假设未来状态与当前状态相同。所有训练模型均优于该基线，没有坍缩。
- **对比方法**：主要对比$K=1$与$K=4$（单步与多步训练）对$\rho$的影响。此外，还对反转任务进行了**消融实验**：仅在第一步（$t=0$）监督早期退出（`deep_sup_first_step_only`），对比标准全步骤深度监督。还对比了**潜在空间**与**观测空间**（通过线性探针重构）的度量影响，以及**状态输入（MLP编码器）**与**像素输入（CNN编码器）**的差异。
- **规划器验证**：使用**CEM规划器**（256个候选，步长10，2次迭代）闭循环运行在真实环境中，对比固定深度2、固定深度12以及自适应路由深度的回报。

## 4. 资源与算力

- 论文明确提到：原始、广度、Gate-B（cheetah/walker）和像素实验使用**H100/H200 GPU**进行（共78次训练运行）；四足消融和cheetah种子扩展实验使用**Apple Silicon MPS设备**进行（13次运行）。训练超参数：60k步（humanoid 80k），批次256，学习率$3\times10^{-4}$，权重衰减$10^{-4}$。
- **未详细说明**：没有给出具体GPU数量、训练总时间或显存消耗。但指出总训练运行数为91（主实验）+ 后续扩展，涵盖了9个任务的细致对比。

## 5. 实验数量与充分性

- **实验数量**：
    - 9个任务，每个任务$K=1$和$K=4$两个孪生模型，各3个种子：共$9\times2\times3=54$个训练运行（但文中实际列出了36个（6个额外任务×2×3）加上原始18，共54个？需要核对：原文说"36 runs"（6 tasks×2×3=36）加上原始3 tasks×2×3=18，共54个训练运行）。另外还有像素实验18个运行、消融实验（cheetah/walker/quadruped）以及cheetah扩展（增加5个种子），总计约91个运行。
    - 每个任务都进行了多次评估：256个保持窗口，10步展开；进行了种子间变异分析。
- **充分性**：
    - **分类充分**：9个任务覆盖了三种明确归类（6内在、2反转、1平坦），种子间一致性高（仅quadruped有一个种子跨越边界）。
    - **消融实验充分**：对cheetah进行了8个种子的消融验证（$\Delta=+0.31$，分布不重叠），表明机制可靠；对quadruped和walker（控制）也进行了消融。
    - **规划验证部分充分**：4个有可用奖励模型的任务中，P1在3个任务上成立（cartpole、acrobot、cheetah），humanoid平坦回报无差别；quadruped（边界）和walker（弱奖励模型）未通过。P2固定阈值失败，校准路由在acrobot上成功，在cartpole上部分成功。
    - **跨条件对比**：比较了潜在空间vs观测空间、短步长vs长步长、状态输入vs像素输入，全面揭示了度量依赖性。
- **客观性与公平性**：实验采用**预注册**设计，所有阈值和决策门在计算活动前已提交到git历史（包括失败的原始假设Gate 1），脚本自动化执行一次审计，避免手动调整。这保证了结果客观、可重复。

## 6. 论文的主要结论与发现

- **三大深度-组合模式**：
    1. **内在（intrinsic）**：深度在6/9任务中改善展开质量（$\rho$高达4.7倍），且单步训练即可体现，无需多步训练。
    2. **反转（inversion）**：在2/9任务（cheetah-run, quadruped-walk）中，浅层退出比全深度更好（$\rho$低至0.85）。
    3. **平坦（flat）**：1/9任务（humanoid-run）深度与浅层几乎无差异。
- **反转机制：可路由性第22条军规**：
    - 通过因果消融证明，cheetah上的反转是由每步深度监督（使早期退出可用于路由的训练方式）造成的。移除第一步骤后的深度监督后，反转消失（$\rho$从0.87升到1.18），而内在任务walker不受影响。这形成了一个双重分离：使早期退出可路由的训练本身训练它们比全深度表现更好，从而消除了深度优势。
    - 该机制是任务特定的：quadruped上的边界反转不受同样消融影响，暗示不同起因。
- **部分可先验预测性**：
    - 观测维度（斯皮尔曼$\rho=-0.77$）、动作维度（$-0.73$）和全深度单步误差（$-0.77$）与$\rho_{K4}$有强相关性。高维、难建模的任务更倾向于反转或平坦。
- **规划验证中$\rho$的预测性**：
    - 在内在任务（cartpole、acrobot）中，深度规划回报优于浅层；在反转任务（cheetah）中，浅层规划回报优于深度（平均6.58 vs 7.24）。$\rho$作为规划器深度收益的诊断工具是有效的。
- **三个评估注意事项**：
    1. **度量空间依赖性**：humanoid在潜在空间平坦，但在观测空间反转（$\rho=0.78$），表明标准潜在误差可能掩盖计算收益。
    2. **展开步长依赖性**：cheetah的反转在短步长（H=10）下明显，但随步长增加恢复（$\rho\to1.07$ at H=50）。
    3. **编码器依赖性**：状态空间中的内在任务（cartpole、walker）在像素编码下变为平坦，即使模型质量匹配。深度收益是（任务、训练、编码器）三元组的属性。

## 7. 优点

- **预注册方法论**：所有阈值、决策门、失效标准在计算活动前已提交并冻结，脚本自动执行一次审计，极大增强了结果的可信度和可重复性。
- **因果消融设计**：通过仅移除第一步骤后的深度监督，直接验证了反转的训练起因，并使用了内在任务作为对照，形成了稳健的双重分离。
- **跨设置验证**：不仅限于状态空间，还检验了像素编码、展开步长、度量空间、规划器回报等多个维度，全面揭示了深度收益的条件性。
- **简单而有效的诊断指标**：单个$\rho$值即可分类任务模式，并预测规划器收益，实用性强。
- **诚实报告负面结果**：原始假设（多步训练创建深度-质量权衡）在3个任务上失败后，没有掩盖，而是转而提出描述性分类，这种学术诚实值得认可。

## 8. 不足与局限

- **实验覆盖**：仅限DMC连续控制任务（状态空间为主），且模型为固定架构（块堆叠、MLP编码器）。像素实验只做了三个任务且部分受限于训练不足（cartpole K=4）。类别数少（$n=9$），相关性分析（第8节）仅为描述性，未进行统计显著性检验。
- **可路由性第22条军规的普适性**：该机制在cheetah上稳健，但在quadruped上无效，说明反转有多种原因，本文只找到了一种。不能将反转笼统归因于训练。
- **规划验证不完整**：P1在两个任务上未通过（quadruped边界，walker奖励模型弱）；P2固定阈值失败，校准后在acrobot通过，在cartpole上仅部分通过。规划器仅针对$K=4$模型，未测试$K=1$或其它训练。
- **资源与复现细节不足**：未提供训练花费的具体GPU小时数，像素实验的细节也较少。虽然代码和预注册承诺开源，但当前论文仅提及脚本。
- **可预测性的局限性**：第8节的相关性基于$n=9$，且预测器共线（维度和模型误差），结论推论性较强。论文预注册了进一步的6个任务外推测试，但结果在本文中尚未给出。
- **其他局限**：
    - 只有1个任务属于平坦类别（humanoid）且其标签依赖于度量空间；四足反转边界不稳定。
    - 模型质量（如EMA目标）可能影响$\rho$，但未深入分析。
    - 没有研究更宽/更深骨干网络对模式的影响。

（完）
