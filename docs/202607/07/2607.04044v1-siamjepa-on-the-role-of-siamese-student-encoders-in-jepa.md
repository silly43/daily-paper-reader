---
title: "SiamJEPA: On the Role of Siamese Student Encoders in JEPA"
title_zh: "SiamJEPA: 关于孪生学生编码器在JEPA中的作用"
authors: Makoto Yamada
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.04044v1"
tags: ["query:ssl"]
score: 9.0
evidence: 使用孪生编码器的JEPA自监督学习
tldr: 联合嵌入预测架构（JEPA）在自监督学习中表现优异，但现有方法依赖单编码器。本文提出SiamJEPA，引入孪生学生编码器与指数移动平均教师网络，可视为脑启发模型PhiNet的JEPA形式。在ImageNet线性探测实验中，孪生编码器作为正则化器提升了表示可分性，加速了早期训练，并在有限预算下优于单编码器JEPA和MAE。研究表明，孪生学生架构是预测表示学习的重要归纳偏置，为JEPA设计提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04044v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1283, \"height\": 588, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04044v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1303, \"height\": 473, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04044v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1019, \"height\": 583, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1091, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 972, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1262, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1237, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1225, \"height\": 282, \"label\": \"Table\"}]"
motivation: 探索孪生学生编码器在JEPA中的作用，其更符合脑启发学习框架，但尚未被深入研究。
method: 提出SiamJEPA，使用孪生学生编码器配合EMA教师网络，预测掩码区域嵌入。
result: 在ImageNet线性探测中，SiamJEPA收敛更快、精度更高，有限预算下超越单编码器JEPA和MAE。
conclusion: 孪生学生编码器作为有效归纳偏置，能提升JEPA表示学习性能。
---

## 摘要
最近，联合嵌入预测架构（JEPA）作为自监督表征学习的一种有前景的框架，吸引了计算机视觉和机器学习界的广泛关注。与重建像素的掩码自编码器不同，JEPA模型通过预测掩码区域的潜在嵌入来学习表征。现有的基于JEPA的方法，如I-JEPA和V-JEPA，通常在学生网络中使用单个编码器。相反，使用孪生编码器作为学生网络更自然地与受大脑启发的表征学习框架相一致，但它们在JEPA模型中的作用尚未得到充分探索。在本文中，我们研究了孪生学生编码器在基于JEPA的表征学习中的影响。为此，我们提出了SiamJEPA，即配备指数移动平均（EMA）教师网络的掩码孪生学生编码器。SiamJEPA也可以被视为受大脑启发的表征学习模型PhiNet的JEPA形式化。通过在ImageNet线性探测上的大量实验，我们证明孪生编码器作为JEPA目标的有效正则化器，提高了表征的可分离性并加速了训练早期的学习。此外，在有限的训练预算下，SiamJEPA始终优于可比的单编码器JEPA变体，并且比需要更长训练的掩码自编码器（MAE）获得更高的线性探测准确率。我们的发现表明，孪生学生编码器不仅仅是一种架构选择，而是预测性表征学习的重要归纳偏置。这些结果为基于JEPA模型的设计提供了新见解，并表明整合孪生学生架构为改进自监督表征学习提供了一种简单而有效的方法。

## Abstract
Recently, Joint Embedding Predictive Architectures (JEPAs) have attracted significant attention in the computer vision and machine learning communities as a promising framework for self-supervised representation learning. Unlike masked autoencoders that reconstruct pixels, JEPA models learn representations by predicting latent embeddings of masked regions. Existing JEPA-based methods, such as I-JEPA and V-JEPA, typically employ a single encoder in the student network. In contrast, using Siamese encoders for student network is more naturally aligned with brain-inspired representation learning frameworks, yet their role in JEPA models remains largely unexplored. In this paper, we investigate the effect of Siamese student encoders in JEPA-based representation learning. To this end, we propose SiamJEPA, masked Siamese student encoders equipped with an exponential moving average (EMA) teacher network. SiamJEPA can also be viewed as a JEPA formulation of the brain-inspired representation learning model PhiNet. Through extensive experiments on ImageNet linear probing, we demonstrate that Siamese encoders act as an effective regularizer for the JEPA objective, improving representation separability and accelerating learning during the early stages of training. Furthermore, SiamJEPA consistently outperforms comparable single-encoder JEPA variants under limited training budgets and achieves higher linear probing accuracy than Masked Autoencoders (MAE) which requires longer training. Our findings reveal that Siamese student encoders are not merely an architectural choice but constitute an important inductive bias for predictive representation learning. These results provide new insights into the design of JEPA-based models and suggest that incorporating Siamese student architectures offers a simple yet effective approach for improving self-supervised representation learning.

---

## 论文详细总结（自动生成）

## 论文《SiamJEPA: On the Role of Siamese Student Encoders in JEPA》详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在联合嵌入预测架构（JEPA）中，**孪生学生编码器**（即两个共享权重的编码器，分别处理不同掩码视图）究竟扮演什么角色？现有JEPA模型（如I-JEPA、V-JEPA）均采用**单学生编码器**，而脑启发表征学习框架（如PhiNet）天然使用了孪生架构，二者的联系尚未被系统研究。
- **研究动机**：理解孪生结构是否能作为JEPA的有效**归纳偏置**，从而提升表征质量、加速训练，并避免表示坍塌。作者希望为JEPA设计提供新的理论指导和实用建议。
- **背景**：JEPA通过在潜在空间预测掩码区域来学习表征，避免了像素重建的高成本。然而，JEPA面临表示坍塌和训练不稳定等挑战，现有解决方案依赖stop-gradient和EMA。孪生编码器可能提供额外的正则化，但目前缺乏系统分析。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**SiamJEPA**，将**孪生学生编码器**引入JEPA框架，配合**EMA教师编码器**。通过两个独立的掩码视图进行潜在预测，并引入KL正则化项来增强两个学生编码器输出之间的一致性，从而获得更丰富、更稳健的表征。

- **关键架构**：
  - **学生编码器 $f$** ：共享权重的两个Vision Transformer（ViT），各自处理独立掩码后的 patch 序列 $X$。输出 $H^{(1)}$ 和 $H^{(2)}$。
  - **教师编码器 $f_{ema}$** ：参数由学生编码器的EMA更新，处理完整（或未掩码）图像，输出 $Y$。
  - **预测器 $h$** ：线性层，用于对齐两个学生编码器的CLS token表征，输出 $h(H) = HW$，其中 $W \in \mathbb{R}^{d \times d}$。
  - **预测器 $g$** ：Transformer模型，根据 $h(H)$ 和**潜在变量 $Z$** 预测所有token的潜在表征 $\hat{Y}$。
  - **潜在变量 $Z$** 采用概率形式：后验 $q(Z|h(H_{cls}^{(1)}), H_{cls}^{(2)})$ 利用两个视图信息；先验 $p(Z|h(H_{cls}^{(1)}))$ 仅依赖单个视图。
    - 这允许模型建模预测的不确定性。

- **损失函数**：
  - **Sim-1（KL项）**：最小化后验与先验之间的KL散度，促使单视图预测能近似于双视图一致的表征。公式（以分支1为例）：
    $$KL_{sg}^{(1)} = KL\left( q(Z|[h(H_{cls}^{(1)}), H_{cls}^{(2)}]) \;\|\; sg(p(Z|h(H_{cls}^{(1)}))) \right)$$
    其中 $sg$ 为stop-gradient，防止先验被快速拉近，提升训练稳定性。
  - **Sim-2（NMSE项）**：预测被掩码区域（$\bar{M}$）的patch表征，使用归一化均方误差：
    $$MSE^{(1)} = \frac{1}{|\bar{M}|} \left\| \text{Mask}\left( Y_{patch}^{(1)} - \hat{Y}_{patch}^{(1)},\, \bar{M} \right) \right\|_{Frob}^2$$
    其中 $Y^{(1)}$ 来自教师编码器对全图的编码。
  - **总损失**：$L = \frac12 (MSE^{(1)} + MSE^{(2)}) + \lambda_{KL} \cdot \frac12 (KL_{sg}^{(1)} + KL_{sg}^{(2)})$。
    - 当 $\lambda_{KL} = 0$ 时退化为类似单编码器JEPA；作者选取 $\lambda_{KL}=10^{-4}$ 作为“类JEPA”基线。

- **掩码策略**：两个学生分支的掩码集 $M_1$ 和 $M_2$ **不重叠**（$M_1 \cap M_2 = \emptyset$），避免捷径学习。

### 3. 实验设计

- **数据集**：ImageNet-1K（约128万张图片），用于预训练和线性探测评估。
- **Benchmark**：**ImageNet线性探测**（Top-1准确率），即固定预训练编码器，训练线性分类器。
- **对比方法**：
  - **MAE**（Masked Autoencoder）——像素重建基线
  - **CAE**（Context Autoencoder）——改进的掩码自编码器
  - **I-JEPA**——官方JEPA基线（注：I-JEPA使用600 epoch训练，本文为400 epoch，间接比较）
  - **SiamJEPA**的多个变体（不同 $\lambda_{KL}$、free-bit、掩码策略等）。

- **基础配置**：
  - 骨干网络：ViT-Base（patch size 16，12层Transformer）
  - 图像分辨率：224×224
  - 预训练epochs：400（消融实验部分使用300 epoch节省计算）
  - 优化器：AdamW，base学习率 $1.5\times10^{-4}$
  - 有效batch size：8192
  - EMA动量：从0.99到0.9999分段调度
  - 掩码比例：0.75（块掩码为主）
  - 预测器深度：1层Transformer

### 4. 资源与算力

- 文中明确说明：**使用NVIDIA V100、A100或H100 GPU**，但**未给出具体GPU数量或训练总时长**。有效batch size为8192，推测多卡分布式训练。预训练400 epoch，考虑到ImageNet-1K规模和ViT-Base，训练开销较大（但未量化）。
- 消融实验（300 epoch）进一步缩减计算量。

### 5. 实验数量与充分性

- **实验数量**：共提供**7个表格**、**2个图**，覆盖：
  - 主结果对比（表2）
  - KL权重 $\lambda_{KL}$ 消融（表3）
  - free-bit阈值消融（表4）
  - 掩码策略消融（表5）：随机 vs 块掩码，三种掩码比例（0.70/0.75/0.80）
  - 权重衰减消融（表6）：0.05 vs 0.1，观察400 epoch
  - 预测目标消融（表7）：CLS token vs mean pooling（10层 vs 12层）
  - 学习曲线（图2）：展示不同 $\lambda_{KL}$ 下损失变化
- **充分性评价**：实验设计**系统且全面**，对不同关键超参数和设计选择均进行了控制变量分析。但存在以下局限：
  - 仅使用ImageNet-1K单一数据集，未在更大规模数据（如ImageNet-21K、JFT）或下游任务（如目标检测、分割）上验证。
  - 与I-JEPA的比较不直接（不同代码库、不同epoch数）。
  - 未对更大模型（ViT-L/H）进行测试。
  - 预测器深度的消融仅提及实验困难，未提供完整数据。

### 6. 论文的主要结论与发现

1. **孪生学生编码器作为有效正则化器**：引入KL一致性损失 $\lambda_{KL}$ 后，线性探测性能显著提升，尤其在训练早期加速收敛。例如，$\lambda_{KL}=0.01$ 在100 epoch时已达63.72% Top-1，而 $\lambda_{KL}=0.0001$ 仅57.89%。
2. **块掩码优于随机掩码**：与I-JEPA一致，块掩码（block masking）在SiamJEPA中表现更好，但随机掩码在精细调参后也可接近。
3. **较大权重衰减有利于长训练**：0.1 weight decay 在400 epoch时达到70.15%，优于0.05的69.33%，表明强正则化有助于避免过拟合。
4. **中间层mean pooling更适合线性探测**：第10层mean pooling优于CLS token和第12层mean pooling，提示中间层保留了更多通用语义信息。
5. **训练高效性**：SiamJEPA在400 epoch达到70.2%，超过MAE 400 epoch的61.9%，且接近I-JEPA 600 epoch的72.9%（直接对比不公平）。表明潜在预测加孪生编码器可在更少计算资源下获得竞争力。

### 7. 优点

- **创新性**：首次系统研究孪生编码器在JEPA中的作用，提供了新的理论视角。
- **简洁有效**：仅增加一个共享权重的编码器和KL正则项，结构简单但效果显著。
- **实验设计规范**：控制变量法清晰，消融实验覆盖主要设计维度，结论可信。
- **实用指导**：为后续JEPA研究提供了实用超参数选择建议（如块掩码、大权重衰减、中间层特征）。
- **开源友好**：基于官方MAE代码库实现，便于复现和扩展。

### 8. 不足与局限

- **对比不直接**：与I-JEPA的对比因训练设置（epochs、代码库）不同而难以公平量化。作者也承认不追求SOTA，但这一局限减弱了性能优势的说服力。
- **数据集单一**：仅在ImageNet-1K图像分类上验证，未涉及视频、分割、检测等任务，也未验证模型规模扩展性。
- **预测器深度未充分优化**：作者指出预测器深度影响表征层次，但因计算资源限制未能系统探索，可能错过最佳配置。
- **KL项引入额外超参数**：$\lambda_{KL}$ 和 free-bit 需要调优，增加了实际应用复杂度。
- **脑启发连接有限**：虽声称受PhiNet启发，但未深入探讨生物机制与实验结果的对应关系，理论深度稍弱。
- **开源代码未见**：论文未提供代码链接，可复现性待确认。

（完）
