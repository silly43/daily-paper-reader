---
title: "TextSLIP: Text Self-Supervised CLIP for Medical Report Generation"
title_zh: TextSLIP：用于医学报告生成的文本自监督CLIP
authors: "Haoyu Jiang, Ziping Cong"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.21970v1"
tags: ["query:ssl"]
score: 9.0
evidence: 对文本对进行对比学习以改进CLIP报告生成
tldr: 自动化放射报告生成对临床工作流至关重要，但标准CLIP仅优化跨模态对齐，缺乏细粒度语义监督。为此提出TextSLIP，在CLIP基础上引入模态内文本对比学习，通过自监督增强文本对提升文本嵌入判别性，从而为视觉编码器提供更优语言监督。在7百万脑MRI图像-文本对预训练后，报告生成指标一致优于CLIP基线。消融实验表明文本侧自监督是增益关键。文本级对比学习为改善医学视觉-文本对齐提供了新方向，但仍需跨领域验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21970v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1242, \"height\": 716, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21970v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 548, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21970v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1269, \"height\": 547, \"label\": \"Table\"}]"
motivation: 标准CLIP仅优化跨模态对齐，未显式结构化文本嵌入空间，导致细粒度语义监督不足，难以支持复杂报告生成。
method: 提出TextSLIP，在CLIP中增加模态内文本对比学习，利用自监督增强文本对提高文本嵌入判别性，为视觉编码器提供更细粒度语言监督。
result: 在7百万脑MRI数据上预训练并微调，TextSLIP在报告生成指标上持续优于CLIP基线，消融实验证实文本自监督贡献显著。
conclusion: 文本级对比学习是改进医学视觉-文本对齐的有效方向，未来需在更多领域验证其泛化性。
---

## 摘要
自动化放射学报告生成对于提高报告一致性和临床工作流程至关重要。虽然对比语言-图像预训练（CLIP）推动了医学视觉语言建模的发展，但现有的CLIP风格方法可能仍无法为复杂的报告生成提供足够细粒度的语义监督。标准CLIP主要优化跨模态对齐，而没有显式地构建引导视觉表征学习的文本嵌入空间。为解决这一局限，我们提出了TextSLIP，一种通用的医学视觉-语言预训练框架，它通过模态内文本对比学习增强CLIP。通过自监督增强文本对提高文本嵌入的判别性，TextSLIP旨在为视觉编码器提供更细粒度的语言监督。作为初步验证，我们在700万脑部MRI图像-文本对的数据集上预训练TextSLIP，并在报告生成架构中微调预训练的视觉编码器。在与CLIP风格基线的控制比较中，TextSLIP在报告生成指标上表现出一致的改进。消融研究进一步表明，文本侧自监督对观察到的提升有贡献。这些结果表明，文本级对比学习是改善医学视觉-文本对齐的一个有前景的方向，而在更多医学领域的广泛验证仍是重要的下一步。

## Abstract
Automating radiology report generation is important for improving reporting consistency and clinical workflows . While Contrastive Language--Image Pretraining (CLIP) has advanced medical vision language modeling, existing CLIP-style approaches may still provide insufficient fine-grained semantic supervision for complex report generation. Standard CLIP primarily optimizes cross-modal alignment, without explicitly structuring the textual embedding space that guides visual representation learning. To address this limitation, we propose TextSLIP, a general medical vision-language pretraining framework that augments CLIP with intra-modal text contrastive learning. By improving textual embedding discriminability through self-supervised augmented text pairs, TextSLIP is designed to provide finer-grained linguistic supervision to the visual encoder. As an initial validation, we pretrain TextSLIP on a curated dataset of 7 million brain MRI image-text pairs and fine-tune the pretrained visual encoder within a report generation architecture. In controlled comparisons with CLIP-style baselines, TextSLIP shows consistent improvements on report generation metrics. Ablation studies further suggest that text-side self-supervision contributes to the observed gains. These results indicate that text-level contrastive learning is a promising direction for improving medical visual-textual alignment, while broader validation across additional medical domains remains an important next step.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：自动化放射学报告生成有助于减轻放射科医生负担并提高报告一致性。对比语言-图像预训练（CLIP）在医学视觉-语言建模中取得了进展，但标准CLIP只优化跨模态图像-文本对齐，缺乏对文本嵌入空间的显式结构约束。这使得视觉编码器无法从文本侧获得细粒度的语义监督，导致生成的报告质量不足，尤其在处理语义相似但表述多样的医学描述时效果不佳。
- **整体含义**：作者提出通过引入模态内文本对比学习来增强CLIP，使文本嵌入更具判别性，从而为视觉编码器提供更细粒度的语言监督，最终提升医学报告生成质量。

## 2. 方法论

### 核心思想
在标准CLIP的图像-文本跨模态对比学习基础上，增加文本自监督对比学习分支（采用ESimCSE方法）。通过构造文本增强正样本对（词重复策略）和负样本（动量编码器+队列），迫使文本编码器学习更丰富的语义结构，进而间接提升视觉编码器的表示质量。

### 关键技术细节
- **模型架构**：
  - 图像编码器：ViT-B-16-quickgelu
  - 文本编码器：BioMed-BERT
  - 动量文本编码器：与文本编码器结构相同，参数通过指数移动平均更新：$\theta_m \leftarrow \alpha \theta_m + (1-\alpha)\theta$
- **损失函数**：
  - 跨模态对比损失 $L_{CLIP}$：双向InfoNCE损失，包括图像到文本和文本到图像两个方向。
  - 文本自监督对比损失 $L_{ESimCSE}$：正样本为经过词重复（Word Repetition）增强的文本对，负样本来自FIFO队列（存储动量编码器输出）。
  - 总损失：$L = L_{CLIP} + \lambda L_{ESimCSE}$，其中$\lambda$为平衡超参数。
- **训练流程**：先在大规模脑MRI图像-文本对上预训练视觉和文本编码器，然后在报告生成任务（R2Gen框架）中固定或微调视觉编码器，生成报告。

## 3. 实验设计

### 数据集 / 场景
- **预训练数据集**：从MedTrinity-25M中提取约**700万**脑MRI图像-文本对（脑部相关样本）。
- **微调/测试数据集**：BRATS2023-GLI（包含1251例多中心术前胶质瘤MRI研究，四个模态：T1n, T1c, T2w, T2f）。提取2D切片（沿三个轴向），使用AutoRG Brain+GPT-4将临床报告结构化并翻译为英文，形成图像-印象对。划分为7:1:2的训练/验证/测试集。

### Benchmark 与对比方法
- 基准模型：标准R2Gen（原始视觉提取器为ResNet-101从头训练）、CLIP基线（在相同700万数据上预训练）、UniMed-CLIP、BioMed-CLIP。
- 评估指标：BLEU-1/2/3/4、ROUGE-L。

## 4. 资源与算力

- **预训练阶段**：单节点，6张NVIDIA A100 80GB GPU，训练20个epoch，学习率4e-5，batch size 1536，warm-up 5000步，总训练约**80小时**。
- **微调阶段**：单张NVIDIA A100 80GB GPU，训练30个epoch。
- 论文明确说明了GPU型号、数量、训练时长和超参数。

## 5. 实验数量与充分性

- **主要实验**：在四个MRI模态（T1n, T1c, T2w, T2f）上对比TextSLIP与UniMed-CLIP、BioMed-CLIP（表1），以及对比CLIP基线和ResNet-101（表2，消融实验）。
- **消融实验**：通过控制预训练目标（仅CLIP vs. TextSLIP）和视觉初始化（ResNet-101 vs. 预训练编码器），隔离文本自监督的贡献。
- **充分性分析**：实验设计公平（所有基线在同一数据上预训练或同一架构），但**实验数量相对较少**：仅在一个下游数据集（BRATS2023-GLI）上测试，未跨其他医学领域验证；消融仅对比了有无文本自监督，未深入分析$\lambda$、词重复率、队列大小等超参数的影响；评估仅使用n-gram指标，缺少临床相关指标（如精确率、召回率或专家评估）。

## 6. 主要结论与发现

- **TextSLIP在BLEU和ROUGE-L上持续优于所有CLIP基线**：在四个MRI模态上，BLEU-1提升约1-2%，BLEU-4提升约1-3%，ROUGE-L也有小幅提升。
- **消融实验证实文本自监督是提升关键**：在相同预训练数据下，TextSLIP（含文本对比损失）比单纯CLIP预训练效果更好，且优于从头训练的ResNet-101。
- **文本级对比学习是改善医学视觉-文本对齐的有效方向**。

## 7. 优点

- **方法新颖**：将文本自监督对比学习与CLIP跨模态学习巧妙结合，解决了标准CLIP文本嵌入空间结构缺失的问题。
- **实验控制严谨**：所有预训练基线均使用相同数据（700万脑MRI对）和相同架构（ViT-B-16+BioMed-BERT），公平比较。
- **代码开源**：提供GitHub仓库，可复现。
- **初步验证有效**：在医学报告生成任务上取得了可复现的改进。

## 8. 不足与局限

- **领域覆盖窄**：仅验证了脑MRI一个领域，未在胸部X光、CT等其他模态上测试，泛化性未知。
- **下游报告质量依赖结构化处理**：报告中提及报告是使用GPT-4从原始临床文本结构化而来，可能存在噪声或偏差。
- **评估指标局限**：仅使用BLEU和ROUGE等词汇重叠指标，它们与临床相关性弱，可能无法反映诊断准确性。
- **消融不充分**：未探讨$\lambda$、词重复概率、队列大小等超参数的影响，也未对比其他文本增强方法。
- **实验数量少**：仅一组下游数据集、一个基线（R2Gen），缺少与更多最新方法的对比。
- **临床实用性未验证**：未进行专家评审或临床效应分析。

（完）
