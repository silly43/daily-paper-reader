---
title: Unbiased Open World Regularization for Fair Self-Supervised Learning
title_zh: 面向公平自监督学习的无偏开放世界正则化
authors: "L{é}o Nicollier, Marc Pic, Pablo Mus{é}, Enric Meinhardt-Llopis, Gabriele Facciolo"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22149v1"
tags: ["query:ssl"]
score: 9.0
evidence: 提出用于公平自监督学习的无偏开放世界正则化方法
tldr: 自监督学习模型容易学习数据集中的虚假偏差，现有全局正则化仅约束全局分布，无法防止特征空间中的偏差纠缠。本文提出无偏开放世界正则化 (UOWReg)，将全局目标分布改为条件目标分布，强制表示与属性统计独立。实验表明，UOWReg在CelebA上降低等化几率违规，保持分类精度；在合成雕刻任务中成功隔离微细信号，防止亚群塌缩。该方法为公平自监督学习提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22149v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1671, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22149v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1622, \"height\": 933, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22149v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1621, \"height\": 1444, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22149v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1464, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22149v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1299, \"height\": 774, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22149v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1067, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22149v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 642, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22149v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 843, \"height\": 324, \"label\": \"Table\"}]"
motivation: 现有SSL模型易受虚假偏差影响，全局正则化不足以防止偏差纠缠，需显式条件分布匹配。
method: 提出UOWReg，通过条件目标分布（高斯或球面均匀）强制表示与属性统计独立，实现无偏开放世界正则化。
result: 在CelebA上UOWReg降低等化几率违规，保持分类精度；在合成雕刻任务中有效隔离微细信号，防止标准SSL的亚群塌缩。
conclusion: UOWReg通过条件正则化实现公平性，优于全局正则化，对公平SSL具有重要价值。
---

## 摘要
尽管近期取得了进展，自监督学习（SSL）模型和联合嵌入预测架构（JEPAs）仍然容易学习数据集中的虚假偏差。这些技术依赖于正则化，通过强制全局目标分布（如多元高斯分布或球面上的均匀分布）来防止表示坍缩。然而，这些全局约束不足以防止偏差纠缠，因为任务无关的特征仍然可以将潜在空间分割成不同的子区域。虽然最近的方法（如纠缠与解纠缠（EnD）和公平监督对比学习（FSCL））在经验上去除潜在空间的偏差，但我们表明它们只是条件分布匹配的部分近似。为了显式地实现这种匹配，我们提出了无偏开放世界正则化（UOWReg），一种仅编码器框架。我们表明，从全局目标转向条件目标能够保证学习表示与目标属性之间的统计独立性，无论选择何种目标分布。我们在高斯和球形潜在空间上使用统计度量来强制这些目标分布，从而经验验证了该框架。尽管条件匹配在使用两种分布时都能有效缓解偏差，但我们证明，在球面上强制执行条件均匀性会产生更低的线性探测分类误差。经验上，与现有的仅编码器基线相比，UOWReg在CelebA基准上减少了均等几率违规，同时保持了具有竞争力的分类准确率。此外，我们引入了合成雕刻任务——一种新颖的设置，其中主要的宏观结构掩盖了细粒度的微观特征。我们表明，UOWReg有效防止了标准SSL中观察到的子群体坍缩，即使微观特征与全局结构严重纠缠，也能成功将其隔离。

## Abstract
Despite recent advances, self-supervised learning (SSL) models and Joint-Embedding Predictive Architectures (JEPAs) remain susceptible to learning spurious biases in the dataset. These techniques rely on regularization, which prevents representation collapse by enforcing a global target distribution such as a multivariate Gaussian or a uniform distribution on the sphere. However, these global constraints are insufficient to prevent bias entanglement, as task-irrelevant features can still segregate the latent space into distinct sub-regions. While recent approaches like Entangling and Disentangling (EnD) and Fair Supervised Contrastive Learning (FSCL) empirically debias the latent space, we show that they act as partial approximations of conditional distribution matching. To enforce this matching explicitly, we propose Unbiased Open World Regularization (UOWReg), an encoder-only framework. We show that this shift from a global to a conditional objective guarantees statistical independence between the learned representations and the targeted attributes, regardless of the chosen target distribution. We empirically validate this framework across both Gaussian and spherical latent spaces, using statistical measures to enforce these target distributions. While conditional matching successfully mitigates bias with both distributions, we demonstrate that enforcing conditional uniformity on the sphere yields a lower linearprobing classification error. Empirically, UOWReg reduces Equalized Odds violations on the CelebA benchmark while maintaining competitive classification accuracy compared to existing encoder-only baselines. Furthermore, we introduce the Synthetic Engraving Task-a novel setting in which a dominant macro-structure masks a fine-grained micro-signature. We show that UOWReg effectively prevents the subpopulation collapse observed in standard SSL, successfully isolating micro-signatures even when heavily entangled with the global structure.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

自监督学习（SSL）模型，特别是联合嵌入预测架构（JEPAs），在防止表示坍缩方面已取得进展，但全局正则化（如强制高维高斯分布或球面均匀分布）仍不足以应对数据集中的虚假偏差。这类偏差导致任务无关的特征（如背景色、敏感人口属性）将潜在空间分割为不同的子区域，使模型依赖这些“捷径”而非因果语义特征。这种偏差纠缠既损害公平性（如性别与吸引力属性的相关性），也影响分布外泛化。现有的解纠缠方法（如EnD、FSCL）虽然经验有效，但作者证明它们仅是对条件分布匹配的部分近似，且依赖标签或存在不稳定问题。因此，本文提出**无偏开放世界正则化（UOWReg）**，一种仅编码器框架，通过显式强制条件分布匹配来保证学习表示与目标属性之间的统计独立性，从根本上消除偏差纠缠。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
将SSL中的正则化目标从全局分布匹配替换为**条件分布匹配**：对每个已知的虚假属性类别 $b$，强制其条件表示分布 $X_b = X \mid B = b$ 与目标随机变量 $Y$（如标准高斯 $N(0,I)$ 或球面均匀分布 $U(S^{d-1})$）相同。根据命题1，若对所有 $b$ 有 $X_b \sim Y$，则 $X \sim Y$ 且 $X \perp B$（统计独立）。

### 关键技术细节
1. **基线：开放世界正则化（OWReg）**  
   结合两个目标：
   - **不变性损失**：最小化各增强视图与全局原型（global prototype）之间的欧氏距离，保留语义特征。  
     $$
     \mathcal{L}_{\text{inv}} = \frac{1}{V}\sum_{v=1}^{V}\|\mu_n - \tilde{z}_{n,v}\|_2^2
     $$
   - **全局分布匹配**：通过统计差异度量 $\mathcal{D}$（如MMD或KL散度）强制整体表示匹配目标分布 $Y$。  
     $$
     \mathcal{L}_{\text{OWReg}} = \frac{1}{V}\sum_{v=1}^{V} \mathcal{D}(X^{(v)}, Y)
     $$

2. **无偏开放世界正则化（UOWReg）**  
   在全局匹配基础上加入条件匹配项，并引入平衡参数 $\alpha$ 以稳定小样本估计方差：  
   $$
   \mathcal{L}_{\text{UOWReg}} = \frac{1}{V}\sum_{v=1}^{V}\left[(1-\alpha)\mathbb{E}_b[\mathcal{D}(X_b^{(v)}, Y)] + \alpha\mathcal{D}(X^{(v)}, Y)\right]
   $$
   其中 $\alpha \in [0,1]$，默认0.5（连续数据用0.25）。条件项通过小批量内同属性样本分组估计。

3. **目标分布的选择**  
   - **高斯目标**：使用MMD+核函数 $k_N$，来自KerJEPA，避免随机投影。  
   - **球面均匀目标**：使用基于热核（heat kernel）的MMD或KL散度闭合表达式，直接在高维球面上计算，无需欧氏投影。  
   理论证明球面均匀目标更有利于下游任务（k-NN、岭回归的最坏情况性能）。

4. **统一先前方法**  
   作者证明：  
   - EnD的解纠缠项等价于条件MMD的矩匹配近似（仅匹配二阶矩）。  
   - FSCL的负样本排斥项等价于条件KL-KDE估计。  
   UOWReg通过灵活选择最佳差异度量（连续/聚类数据分别用KL/MMD）实现了更一般化的条件匹配。

## 3. 实验设计：数据集/场景、基准、对比方法

| 场景 | 数据集 | 衡量指标 | 对比方法 |
|------|--------|----------|----------|
| 视觉解纠缠可视化 | Colored MNIST（CMNIST） | k-NN目标分类准确率、偏差属性预测准确率 | OW基线（高/球面）、FSCL、UOWReg（两种目标） |
| 公平表示学习 | CelebA（目标属性：Attractive/Big Nose；虚假属性：Male） | 分类准确率（Acc.↑）、均等几率（EO↓，公平性度量） | 监督基线（CE，FSCL+）、无监督OW基线（高斯/球面）、自监督去偏FSCL†、UOWReg（高斯/球面） |
| 细粒度局部扰动敏感性 | 合成雕刻任务（Synthetic Engraving） | 条件平均精确度（mAP\|c）：在同一全局结构（DataMatrix码）内检索不同微印记（micro-signature）的能力 | OW基线（KL正则化）、UOWReg（KL，α=0.25），不同签名数 m∈{2,4,128} |
| 平衡参数α消融 | 雕刻任务（连续数据，KL）+ CelebA（聚类数据，MMD） | 根据场景用mAP\|c或EO+Acc | 不同α值（0, 0.25, 0.5, 0.75, 1.0） |

**基准设置**  
- CMNIST：小卷积网络+两个MLP投影头，100 epoch，λ=0.05。  
- CelebA：ImageNet预训练ResNet-18+投影头，30 epoch，λ=0.05，学习率0.05，batch 256。  
- 雕刻任务：灰度ConvNeXt（DINOv3）骨架+投影头，40 epoch，λ=1/3，热核温度t=2/256。

## 4. 资源与算力

论文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。仅提及所有模型使用AdamW优化器、权重衰减5e-4、线性预热后恒定学习率。对于工业化部署或大规模实验的可重复性，这部分信息相对缺失。

## 5. 实验数量与充分性

### 实验数量
- **CMNIST**：一组定量比较（表1）+ t-SNE可视化（图3）。
- **CelebA**：两组属性组合（Attractive/Male, Big Nose/Male），各用3个随机种子平均。
- **雕刻任务**：三个m值（2,4,128），各用3个种子平均，报告均值±标准差。
- **α消融**：雕刻任务和CelebA各一组，共5个α值。
- **统一先前方法的理论分析**未单独设置实验，但通过公式推导表明UOWReg更优。

### 充分性与公平性
- **充分性**：覆盖了合成数据（验证原理）、真实世界公平基准（CelebA）、对抗性强纠缠场景（雕刻任务），并做了关键超参数消融。但缺少对大模型（如ViT-L）或大规模数据（如ImageNet）的验证。
- **公平性**：对比了同类仅编码器方法（FSCL†、OW基线），并调整超参数报告最优结果。但未与基于生成模型（如扩散数据增强）或对抗训练的方法直接比较（作者明确限定为仅编码器方法）。未提供标准偏差或统计显著性检验。

## 6. 论文的主要结论与发现

1. **条件分布匹配可保证统计独立性**：命题1严格证明，若每个虚假属性类别的表示分布均与目标分布相同，则全局表示与属性独立。
2. **UOWReg有效去除已知虚假偏差**：在CMNIST上将偏差k-NN准确率从100%降至69.9%，同时提升目标准确率。
3. **球形均匀目标优于高斯目标**：在CelebA上球形UOWReg的EO仅2.8%，同时保持75.6%准确率；高斯UOWReg的EO虽低（8.7%），但准确率大幅下降至59.6%。
4. **在强纠缠场景中防止子群体坍缩**：雕刻任务中，m=2时UOWReg的mAP|c从OW的76.48%提升至90.69%，成功隔离微观特征。
5. **混合全局/条件正则化（0<α<1）优于纯条件或纯全局**：消融实验表明，α=0.25/0.5可在稳定性和解纠缠之间取得最佳平衡。

## 7. 优点：方法或实验设计上的亮点

- **方法论统一性**：首次将EnD、FSCL等经验方法统一为条件分布匹配的近似，奠定了理论框架。
- **分布无关性**：UOWReg可灵活使用MMD或KL散度，适配不同数据结构（聚类/连续）。
- **仅有编码器，免对抗**：避免了GAN或变分法的训练不稳定和超参数敏感性，易于集成到现有SSL管道。
- **新颖的对立任务（合成雕刻）**：设计了一个宏观结构（DataMatrix码）与微观信号高度纠缠的控制设定，有效评估了方法在极端偏差下的鲁棒性。
- **只要求虚假属性标注**：无需任务标签，符合自监督精神；对于实际应用（如工业认证）非常实用。
- 实验报告了多次重复的平均值和标准差，可信度较高。

## 8. 不足与局限

- **虚假属性必须离散且已知**：论文假设b是离散类别（如性别、码编号），未处理连续或未知偏差。
- **完美相关无法解决**：当宏观结构与微观特征完全一一对应（m=1）时，仅编码器方法无法解纠缠，属于开放问题。
- **CelebA上分类准确率低于监督方法**：球形UOWReg的EO虽低，但准确率（75.6%）低于FSCL+（82.5%）甚至低于无偏置的OW基线（80.1%），可能存在权衡问题。
- **规模验证不足**：仅在小型（CMNIST、CelebA）和中型（雕刻64×64）数据集上实验，未在ImageNet等大规模真实场景上测试。
- **算力未报告**：缺乏GPU型号、训练时间和能耗数据，不利于公平比较和可复现性评估。
- **未与生成类去偏方法对比**：如基于扩散模型的数据重采样或后处理（nullspace projection），限制了结论的覆盖面。

（完）
