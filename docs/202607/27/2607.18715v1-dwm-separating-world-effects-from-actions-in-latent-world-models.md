---
title: "DWM: Separating World Effects from Actions in Latent World Models"
title_zh: DWM：在潜在世界模型中将世界效应与动作分离
authors: "Yi-Ge Zhang, Tianqi Du, Qi Zhang, Yisen Wang"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18715v1"
tags: ["query:world-models"]
score: 9.0
evidence: 在潜在世界模型中分离世界效应与动作
tldr: "潜在世界模型将动作驱动与动作不变的世界效应（如重力、惯性）混合在单一预测目标中，导致归因困难和可迁移性差。本文提出DWM，在监督层面引入辅助world head，通过归一化世界对比损失实现动作不变性，并与原始预测头施加正交约束，实现预测过渡的显式加法分解。在PushT-W、Reacher-W、TwoRoom-W三个带持久世界效应的基准上，DWM在CEM规划成功率上平均提升13.1%，且不牺牲平坦环境性能。该方法无需修改架构或推理流程，为分离世界动态与动作效应提供了简洁有效的框架。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18715v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1403, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18715v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1429, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18715v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18715v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 603, \"height\": 491, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18715v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1219, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1103, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 798, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1254, \"height\": 435, \"label\": \"Table\"}]"
motivation: 现有潜在世界模型将动作驱动与动作不变的世界效应混合为一个学习信号，导致归因困难和可迁移性差。
method: DWM在潜在世界模型预测器中添加辅助world头，通过归一化世界对比损失正则化为动作不变，并与原始预测头通过正交约束实现显式加法分解。
result: "在PushT-W、Reacher-W、TwoRoom-W三个W变体基准上，DWM的CEM规划成功率平均提升13.1%，且平坦环境性能持平。"
conclusion: DWM通过监督级分解有效分离动作驱动与动作不变的世界效应，提升模型泛化与规划能力，且不增加架构复杂度。
---

## 摘要
潜在世界模型构成了现代基于模型的控制的基础，然而当前的动作条件公式用一个单一的、无差别的目标来监督下一个潜在状态的转移，迫使一个整体的学习信号吸收状态变化的每一个来源。然而，在现实世界中，转移源于两个异质来源：由智能体引起的动作驱动成分，以及动作不变的世界效应——即在空动作下仍会发生的、由环境内在动力学（例如，重力驱动的滑动、惯性、接触反弹和持续漂移）所决定的变化。将它们融合成一个单一目标会在潜在转移中纠缠两者，阻止模型将观察到的变化归因于其根本原因，并削弱学习到的动力学的可迁移性。我们引入了DWM（分解的世界模型），这是一个监督级框架，实现了这种分解。DWM用一个辅助的世界头来增强潜在世界模型的预测器，该世界头通过归一化的世界对比目标正则化以保持动作不变性，而原始的预测头通过正交性约束与之耦合；共同地，这两个信号诱导出预测转移的显式加性分解，分为一个动作不变组成部分和一个互补的动作驱动组成部分，而不改变底层架构或推理流程。为了在持续的世界效应下评估DWM，我们构建了三个标准控制基准的W变体——PushT-W、Reacher-W和TwoRoom-W——每个都实例化了一个独特的动作不变动态。DWM在平坦对应基准上匹配了强基线，并在W变体上的CEM规划成功中实现了平均绝对改进13.1%。

## Abstract
Latent world models underpin much of modern model-based control, yet current action-conditioned formulations supervise   the next-latent transition with a single, undifferentiated target, forcing a monolithic learning signal to absorb every   source of state change. In real world, however, transitions arise from two heterogeneous sources: an action-driven   component induced by the agent, and an action-invariant world effect -- the change that would still occur under a null   action, dictated by the environment's intrinsic dynamics (e.g., gravity-driven sliding, inertia, contact rebound, and   persistent drift). Fusing them into a single target entangles the two inside the latent transition, prevents the model   from attributing observed changes to their underlying causes, and undermines the transferability of the learned   dynamics. We introduce DWM (Decomposed World Model), a supervision-level framework that operationalizes this   decomposition. DWM augments the predictor of a latent world model with an auxiliary world head, regularized by a   normalized world-contrastive objective to be action-invariant, while the original pred head is coupled to it via an   orthogonality constraint; together, the two signals induce an explicit additive decomposition of the predicted   transition into an action-invariant and a complementary action-driven component, without altering the underlying   architecture or inference pipeline. To evaluate DWM under persistent world effects, we construct W-variants of three   standard control benchmarks -- PushT-W, Reacher-W, and TwoRoom-W -- each instantiating a distinct action-invariant   dynamic. DWM matches strong baselines on the flat counterparts and delivers a mean absolute improvement of 13.1% in CEM   planning success across the W-variants.

---

## 论文详细总结（自动生成）

# 论文总结：DWM：在潜在世界模型中将世界效应与动作分离

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有动作条件潜在世界模型（如LeWM）将状态转移中所有变化来源（动作驱动和世界效应）融合为一个无差别的单一预测目标。这导致模型无法区分“智能体的动作改变了什么”和“环境本身会如何演化（如重力、惯性、漂移）”，从而损害多步滚动的准确性，并削弱动力学迁移至新环境（如不同重力）的能力。
- **研究动机**：在现实物理控制任务中，世界效应（action-invariant）与动作效应（action-driven）经常共存且源自动态的不同机制。作者指出，仅通过增加零动作数据无法迫使模型分离两者，问题根源在于训练目标本身。因此，他们提出了一种监督层次的分解框架。
- **整体含义**：DWM不仅提升带世界效应基准的规划成功率（平均+13.1%），还保持平坦任务性能不变，且不改变推理架构，提供了一种轻量级但有效的解耦方案，为更真实场景下的世界模型设计提供新思路。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将潜在状态转移分解为两个加性成分：动作不变的世界效应 $\Delta z_t^{\text{world}}$ 和互补的动作驱动效应 $\Delta z_t^{\text{action}}$。
- 通过添加一个训练时的辅助“world head”和一个正交约束，在监督层面诱导这种分解，而不改变骨干网络或推理流程。

### 关键技术细节
1. **架构**（图2）：
   - 保持LeWM的编码器 $\phi$、动作条件预测器 $g$ 和预测头 $h_p$ 不变。
   - 附加一个平行的世界头 $h_w$，从相同的中间表示 $r_t$ 读入，输出 $\hat{z}^w = h_w(r_t)$。
   - 动作驱动成分定义为残差 $\hat{z}^a = \hat{z} - \hat{z}^w$，其中 $\hat{z} = h_p(r_t)$ 是完整的下一潜在预测（推理时唯一使用）。
2. **损失函数**（公式6）：
   - **世界对比损失 $L_{wc}$**：对同一历史，替换当前动作为随机采样动作，要求 $h_w$ 的输出 $\hat{z}^w$ 保持不变。采用InfoNCE目标（公式7）增强动作不变性，同时通过负样本防止崩塌。
   - **正交损失 $L_{orth}$**：$L_{orth} = |\cos(\hat{z}^w, \hat{z}^a)|$，强制世界头输出与动作驱动成分正交，避免信息混叠。
   - 总损失 $L = L_{WM} + \lambda_{wc} L_{wc} + \lambda_{orth} L_{orth}$。
3. **训练与推理分离**：world head仅在训练时使用，推理时丢弃，因此无额外计算开销。

### 公式（文字描述）
- 定义动作不变分量：$\Delta z^{\text{world}}_t = \mathbb{E}[z_{t+1} | \text{history}, a_t=0] - z_t$
- 加性分解恒等式：$\Delta z_t = \Delta z^{\text{world}}_t + \Delta z^{\text{action}}_t$。
- 预测分解：$\hat{z} = \hat{z}^w + \hat{z}^a$。

## 3. 实验设计：数据集/场景、基准、对比方法

### 数据集与场景
- **主要基准**：三个标准控制任务的W-variant版本，每个引入一种持久动作不变世界效应：
  - **PushT-W**：水平推方块变体，加入重力（45°,25°），方块在无动作时仍会滑动。
  - **Reacher-W**：DMC Reacher变体，加入垂直平面重力（60°倾斜），机械臂受重力下拉。
  - **TwoRoom-W**：2D导航变体，加入恒定漂移偏置 $b=(4,2)$ 像素/步。
- **平坦对照**：原始PushT、Reacher、TwoRoom（无世界效应），用于验证不破坏已有性能。
- **额外实验**：**Ball-in-Cup**（DMC标准任务），具有振荡性状态依赖的世界效应，更复杂。

### 对比方法
- 主要基线：**LeWM**（LeWorldModel，单头潜在世界模型，即DWM去掉world head和两个辅助损失的版本）。
- 消融中对比不同 $\lambda_{wc}, \lambda_{orth}$ 取值。
- 未与其他复杂解耦模型（如Iso-Dream）对比，论文解释因为二者架构差异大且DWM作为监督级方法可即插即用。

### 评估指标
- **规划性能**：CEM规划成功率（3随机种子，每种子50个起始-目标对）。
- **预测质量**：单步预测损失和20步自回归滚动损失（MSE或qpos误差）。
- **解耦诊断**：世界头和预测头对动作扰动的方差和最大-最小跨度。
- **OOD泛化**：在PushT-W上训练于单一重力，测试于不同重力。

## 4. 资源与算力

- **GPU型号**：NVIDIA H20 GPU。
- **训练配置**：batch size 128，bfloat16精度，AdamW优化器（学习率5e-5，权重衰减1e-3），线性预热+余弦退火学习率调度。
- **训练epoch数**：因任务而异：PushT-W 5 epochs，Reacher-W 20 epochs，TwoRoom-W 100 epochs，Ball-in-Cup 20 epochs。
- **未明确信息**：未提及使用的GPU数量、总训练时间或能耗。仅说明所有实验在H20上运行。

## 5. 实验数量与充分性

| 实验类型 | 数量/覆盖 | 充分性与公平性评价 |
|----------|-----------|-------------------|
| **主实验（规划成功率）** | 3个W-variant × 3随机种子 × 50对；3个平坦对照 | 充分。覆盖三种不同世界效应，有统计基准（标准差）。平坦对照验证方法不破坏原有性能。 |
| **预测质量** | 3个W-variant，单步与滚动预测 | 覆盖所有主任务，显示大幅提升（如PushT-W单步降低89.8%）。 |
| **解耦诊断** | 3个W-variant，方差和跨度比率 | 一致性较好，世界头方差比预测头低2-3个数量级。 |
| **OOD泛化** | PushT-W，2个OOD重力+ID重力，CEM成功率和余弦相似度 | 保持了14pp提升，余弦相似度接近1，表明分解泛化性强。 |
| **消融研究** | PushT-W，改变 $\lambda_{wc}$ 和 $\lambda_{orth}$（0,0.1,0.2,0.3等） | 展示了两个损失的必要性，趋势单调合理。 |
| **额外复杂环境** | Ball-in-Cup，使用各自成功头评估，避免混淆 | 提升6pp，滚动损失降低一个量级。 |
| **总体评价** | 实验数量充足，覆盖多任务、多视角（规划、预测、表示、泛化、消融）。对比方法单一（仅LeWM），但论文明确指出LeWM是当前最强单头潜在世界模型，且其他解耦方法架构差异大不易直接比较。消融和诊断增强了归因可靠性。 | 公平性：训练协议、评估预算与基线完全一致；Flat对照避免偏向性；Ball-in-Cup使用各自成功头消除作弊。 |

## 6. 论文的主要结论与发现

1. **分离有效**：在含持续世界效应的三个W-variant任务上，DWM平均提升CEM规划成功率13.1%（+12.0, +10.7, +16.7 pp），且平坦任务性能保持不变。
2. **解耦准确**：世界头的动作方差比预测头低2-3个数量级，正交约束防止信息混叠。
3. **滚动质量提升**：20步自回归预测损失显著低于基线（如PushT-W降低66.7%，Ball-in-Cup降低一个数量级）。
4. **OOD泛化强**：在不同重力下，动作驱动成分余弦相似度0.9991，CEM成功率保持44%（基线30%）。
5. **两个损失互补**：消融显示单独去掉 $L_{wc}$ 或 $L_{orth}$ 均无法达到完整增益。
6. **方法通用**：DWM无需改变骨干架构，可应用于现有动作条件世界模型，且对状态依赖的世界效应（如Ball-in-Cup）仍有效。

## 7. 优点：方法或实验设计上的亮点

- **监督级创新**：不同于以往架构级解耦（如分离编码器/分支），DWM在损失函数层面诱导分解，保持骨干不变，易于集成至现有框架。
- **即插即用**：推理时忽略world head，无额外计算开销，部署友好。
- **实验设计全面**：构建W-variant基准（公开控制任务添加真实世界效应）、OOD测试、解耦诊断、消融，形成了完整的归因链。
- **消除评估偏倚**：Ball-in-Cup中为每个模型训练独立的成功头，避免评估器偏好。
- **清晰的形式化**：明确动作不变分量的定义，并基于此设计训练目标（InfoNCE + 正交约束）。

## 8. 不足与局限

- **实验覆盖有限**：对比方法仅LeWM，缺少与其他解耦世界模型（如Iso-Dream、SLiDe）的定量比较，论文虽给出理由但仍显不足。
- **非线性耦合简化**：加性分解假设在复杂物理交互（如摩擦与推力耦合）下可能不精确，残差会吸收非线性交互，理论上不会完全干净。
- **任务规模较小**：仅测试4个任务（3个W-variant + 1个Ball-in-Cup），缺乏在更复杂、更长程或真实机器人数据上的验证。
- **OOD测试单一**：仅PushT-W进行了OOD重力测试，其他任务OOD泛化未评估。
- **未报告训练成本**：缺乏GPU数量、训练时间等算力细节，难以评估实用性。
- **潜在偏差**：World head只受InfoNCE约束，但若动作扰动太简单（如高斯噪声可能不与环境交互），可能无法学得真正的世界效应。另外，为零动作定义（null action）依赖人为设计，在连续动作空间中不一定明确。
- **结论的强声称**：“world-effect”和“action-effect”在真实世界不一定严格可加，但论文以恒等式回避此问题，实际耦合仍存在于共享表示中。

（完）
