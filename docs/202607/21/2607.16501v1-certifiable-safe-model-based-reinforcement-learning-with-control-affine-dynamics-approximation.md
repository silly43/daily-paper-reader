---
title: Certifiable Safe Model-Based Reinforcement Learning with Control-Affine Dynamics Approximation
title_zh: 基于控制仿射动力学逼近的可认证安全模型强化学习
authors: "Hao Zhou, Yanze Zhang, Cameron Reid, Wenhao Luo"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16501v1"
tags: ["query:world-models"]
score: 9.0
evidence: 提出一个可证明安全的基于模型的强化学习框架，学习控制仿射动力学作为世界模型。
tldr: 基于模型的安全强化学习常忽视残差不确定性中的控制仿射结构，导致保守或无效保证。本文提出控制仿射动力学近似框架，采用控制仿射随机傅里叶特征高效建模，结合自适应共形预测量化不确定性，并利用控制障碍函数实现可证明安全策略。在倒立摆和四旋翼仿真中验证了有效性。贡献在于提升了安全保证的严谨性与任务效率的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16501v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 652, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16501v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1797, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16501v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1789, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16501v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1798, \"height\": 344, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16501v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 1017, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16501v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 904, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16501v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 710, \"height\": 127, \"label\": \"Table\"}]"
motivation: 现有安全强化学习方法因忽略残差模型不确定性的控制仿射结构而导致过于保守或安全保证失效。
method: 利用控制仿射随机傅里叶特征建模动力学，结合自适应共形预测量化不确定性，并用控制障碍函数合成安全策略。
result: 在倒立摆和三维四旋翼仿真中，该框架实现了安全探索与高效任务执行，优于对比方法。
conclusion: 学习控制仿射结构并量化不确定性可提升安全强化学习的实用性和安全保证的严格性。
---

## 摘要
安全模型强化学习通常将控制理论分析与强化学习相结合，使机器人在安全探索（部分）未知系统动态的同时，推导出任务效率的控制动作。控制性能和安全保证通常依赖于部分建模的名义系统动态的先验知识，以及补偿剩余模型不确定性的数据驱动模型。然而，现有方法往往忽略剩余模型不确定性的结构（例如，控制仿射分量），可能导致过于保守的机器人行为或基于安全学习控制器的无效安全保证。本文提出了一种安全强化学习框架，使用控制障碍函数（CBF）学习控制仿射动态，并具有可认证的数据驱动安全策略。具体来说，我们首先使用控制仿射随机傅立叶特征（ARFF）以控制仿射形式对机器人动态进行建模，这提供了随数据集大小扩展的计算效率，并减少了模型基强化学习的潜在模型偏差。然后，应用一种基于自适应共形预测（ACP）的无模型、高效的不确定性量化方法来量化由学习到的控制仿射动态产生的安全约束中的不确定性。这允许使用CBF进行有原则且高效的控制器综合，从而实现数据驱动的安全保证。在车杆和3D四旋翼平台上的仿真结果证明了所提出框架的有效性。

## Abstract
Safe model-based reinforcement learning (RL) often bridges control-theoretic analysis and RL for robots to safely explore (partially) unknown system dynamics while deriving control actions for task efficiency. The control performance and safety assurance typically rely on prior knowledge of partially modeled nominal system dynamics and the data-driven models that compensate for residual model uncertainties. However, existing methods often overlook the structure of residual model uncertainties (e.g., components affine in control), which could lead to overly conservative robot behaviors or invalid safety guarantees under the safe learning-based controllers. This paper proposes a safe reinforcement learning framework that learns control-affine dynamics with a certifiable data-driven safe policy using control barrier functions (CBF). Specifically, we first use Control-Affine Random Fourier Features (ARFF) to model robot dynamics in a control-affine form, which offers computational efficiency that scales with dataset size and reduces potential model bias for model-based reinforcement learning. Then, a model-free, efficient uncertainty quantification method using adaptive conformal prediction (ACP) is applied to quantify the uncertainty in the safety constraint arising from the learned control-affine dynamics. This allows for data-driven safety assurance amenable to principled and efficient controller synthesis with CBF. Simulation results on the cartpole and the 3D quadrotor platforms demonstrate the effectiveness of the proposed framework.

---

## 论文详细总结（自动生成）

# 基于控制仿射动力学逼近的可认证安全模型强化学习——论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在安全模型强化学习（safe MBRL）中，机器人需要同时学习未知系统动力学并确保探索与部署过程中的安全性。现有方法通常假设残差模型不确定性（即未建模的动态）仅依赖于状态而忽略其控制仿射结构，这可能导致（1）过于保守的机器人行为；（2）基于学习模型的安全保证失效（例如使用控制障碍函数CBF时）。此外，常用高斯过程（GP）建模虽能提供不确定性估计，但计算复杂度随数据量增加而剧增；而随机傅立叶特征（RFF）近似虽计算高效，却往往低估不确定性，导致过度自信的安全约束。
- **研究动机**：为了在不牺牲安全保证的前提下提高模型学习与策略合成的效率与严谨性，需要一种能够保留控制仿射结构、高效计算且提供可校准不确定性量的数据驱动建模方法，并由此构建可认证的安全策略。
- **整体含义**：论文提出了一套完整的框架——SafeCARL，将控制仿射随机傅立叶特征（ARFF）与自适应共形预测（ACP）相结合，在安全强化学习中学习控制仿射动力学，并利用CBF合成可带不确定性感知的安全策略。该框架旨在同时实现计算效率、样本效率与可证明的安全保证。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 使用**控制仿射随机傅立叶特征（ARFF）** 对未知动力学 $d(x_k, u_k)$ 进行建模，显式保持其控制仿射形式 $d(x_k, u_k) = \tilde{f}(x_k) + \tilde{g}(x_k) u_k$，从而避免模型偏差并便于后续CBF优化。
- 采用**自适应共形预测（ACP）** 在线量化预测的不确定性，得到可校准的置信区间，并据此将概率安全约束转化为确定性约束，克服了RFF不确定性低估问题。
- 将上述组件集成到基于乐观探索（Thompson Sampling + MPPI）的MBRL框架中，实现安全高效探索。

### 关键技术细节
- **ARFF建模**：
  将未知动力学表示为 $d(x_k, u_k) = \omega^\top \phi(x_k, u_k)$，其中
  $$\phi(x_k, u_k) = [\varphi_1(x_k), \cdots, \varphi_{m+1}(x_k)] \begin{bmatrix} u_k \\ 1 \end{bmatrix}$$
  $\varphi_i(x_k)$ 为基于状态的随机傅立叶函数（RFF），$\omega$ 通过岭回归解析求解（式(13)-(15)），计算复杂度不随数据集大小增加而增加。
- **ACP不确定性量化**：
  定义预测的屏障证书值 $G(x_k, u_k) = h(\hat{f}(x_k)+\hat{g}(x_k)u_k+d(x_k,u_k)) - (1-\gamma)h(x_k)$，真实值为 $G^*(x_k, u_k)$。ACP动态更新非一致性得分的分位数 $S^{(r)}_k$，保证
  $$P(-S^{(r)}_k + G(x_k,u_k) \le G^*(x_k,u_k) \le S^{(r)}_k + G(x_k,u_k)) \ge 1-\alpha$$
  从而通过 $-S^{(r)}_k + G(x_k,u_k) \ge 0$ 作为充分条件实现概率意义上的安全约束（定理1）。
- **安全策略合成**：
  在每个时间步，由MPPI产生参考控制 $u^{ref}_k$，然后求解二次规划（式(17)）：
  $$u_k = \arg\min \|u_k - u^{ref}_k\|^2$$
  满足 CBF 约束：$h(x_{k+1}) - h(x_k) \ge -\gamma h(x_k) + S^{(r)}_k$，以及控制限。由于ARFF显式解耦了控制输入，该优化是凸的，可高效求解。
- **算法流程**（Algorithm 1）：
  每个 episode：初始化状态；通过 Thompson Sampling 采样 $\tilde{\omega}_t \sim \mathcal{N}(\omega_t, (\Sigma_{\omega_t})^{-1})$；在 horizon 内用 MPPI 基于采样动力学生成参考控制，再使用式(17)得到安全控制，执行并收集数据；更新岭回归的 $\Sigma_{\omega_t}$ 和 $\omega_t$。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **测试场景**：
  - **Cartpole（倒立摆）**：来自 Gymnasium，状态 $x=[p, \dot{p}, \theta, \dot{\theta}]$，任务使摆保持竖直（goal状态）；注入未知动力学（参数不准确：名义质量 $m_c^{nom}=1.5, m_p^{nom}=0.05, l^{nom}=0.4$），真实参数 $m_c=1.0, m_p=0.1, l=0.5$。
  - **3D四旋翼稳定控制**：基于[10]的微分平坦动力学，状态为位置、线速度、欧拉角；注入未知动力学（名义质量 $m^{nom}=0.8$，实际$m=1.0$）与正弦风扰动；任务悬停至目标位置 $p_{goal}=[1.5, 2.0, 3.0]$。
  - **3D四旋翼轨迹跟踪**：跟踪8字轨迹，安全集定义为关于 $p_z, \dot{p}_z$ 的高阶约束。
- **Benchmark**：使用的是仿真环境，无真实数据集。
- **对比方法**：
  - MPPI-GT：基于真实动力学（上界）。
  - LC³（MPPI-RFF）：用普通RFF学习动力学，无控制仿射结构，也无安全约束。
  - MPPI-ARFF：本文方法但**无CBF安全约束**。
  - MPPI-ARFF-CBF：本文方法用CBF约束但**无ACP不确定性量化**。
  - **Ours (SafeCARL)**：完整方法（ARFF+ACP+CBF）。
  另在计算效率比较中对比了“MPPI-RFF-CBF-ACP”（用RFF代替ARFF加CBF-ACP）。

## 4. 资源与算力

- **文中未明确提及**具体GPU型号、数量或训练时长。仅说明计算实验在AMD Ryzen 7960X 24核处理器上完成，用于测量求解式(17)的平均时间。未说明大规模并行训练资源。

## 5. 实验数量与充分性

- **每个实验独立重复100次**（Cartpole和UAV stabilization均如此），以获得统计结果（奖励曲线、安全概率、最小h值等）。这提供了充分的随机性抵消。
- **消融实验**：对比了MPPI-ARFF（无安全）、MPPI-ARFF-CBF（无ACP）、Ours（完整），在Cartpole和UAV上均执行。此外有探索轨迹可视化与多起始点泛化测试（UAV stabilization）。
- **安全性评估**：记录100次实验中各方法的最小h值及安全率（h>0比例），并报告均值标准差。
- **计算效率**：在Cartpole和UAV上对比了ARFF vs RFF在式(17)上的平均求解时间（10次以上统计）。
- **充分性评价**：实验较充分，覆盖了主要性能指标（奖励、安全率、探索效率、泛化、计算速度）。但缺少真实机器人实验或与更多先进方法（如基于GP的CBF方法）的直接对比。

## 6. 论文的主要结论与发现

1. **控制仿射结构重要性**：使用ARFF学习控制仿射动力学比普通RFF（LC³）获得更好的任务奖励与更快的收敛速度（在Cartpole和UAV上）。
2. **ACP不确定性量化的必要性**：仅有CBF而无ACP时，安全率大幅下降（Cartpole 19%，UAV 0%）；加入ACP后安全率达到100%（Cartpole）和98%（UAV），满足预设失败概率$\alpha=0.02$。
3. **计算效率优势**：因式(17)变为凸优化，ARFF方法比RFF方法求解快约8倍。
4. **安全探索与泛化**：SafeCARL能在探索过程中保持状态在安全集内，且学习到的动力学可泛化到不同初始位置。
5. **跟踪性能**：在四旋翼8字轨迹跟踪中，SafeCARL相比LC³获得更精确的跟踪轨迹。

## 7. 优点

- **方法论创新**：首次将ARFF（原本用于学习控制Lyapunov函数）引入安全强化学习框架，并联合ACP形成可认证的安全策略，解决了GP计算昂贵、RFF不确定性低估的弊病。
- **理论严谨性**：提供了定理1将概率约束转化为确定性约束，基于ACP的非参数保证，不依赖模型假设。
- **优化易处理**：由于ARFF保持控制仿射结构，CBF约束成为凸约束，可使用CVXPY高效求解（实数规划）。
- **实验充分且可重复**：100次重复实验，报告均值和标准差，消融设计合理。
- **可视化清晰**：展示了探索轨迹、安全集边界、多起点泛化等。

## 8. 不足与局限

- **未能真实机器人验证**：所有实验在仿真中完成，未考虑真实传感器噪声、执行器延迟、模型失配等现实挑战。
- **假设控制仿射结构**：方法要求系统动力学本质上是控制仿射的（或能用控制仿射逼近），对于非控制仿射系统（如带摩擦非线性的系统）可能失效。
- **ACP超参数依赖**：ACP需要预设学习率$\delta$和初始失败概率$\alpha$，在仿真中手工设置，未讨论在线自适应调参的影响。
- **缺乏与更多基线对比**：未对比基于GP的CBF方法（如[7,10]）、其他不确定性量化方法（如dropout、ensemble）或近期基于分布鲁棒优化的方法。
- **安全保证的概率性**：ACP提供的保证是边际概率，不保证逐时步或最坏情况安全，在一些安全关键系统中可能不够强。
- **计算资源信息缺失**：未报告训练总时长、GPU使用情况，限制了可重复性评估。

（完）
