---
title: "JEPADepth: Masked Predictive Representation Learning for Self-Supervised Monocular Depth Estimation"
title_zh: JEPADepth：用于自监督单目深度估计的掩码预测表示学习
authors: "Ionuţ Grigore, Călin-Adrian Popa"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26600v1"
tags: ["query:ssl"]
score: 9.0
evidence: 用于自监督深度估计的掩码预测表示学习
tldr: 自监督单目深度估计通常依赖光度重建损失，但耦合多种假设。JEPADepth在标准流程中加入基于DINOv3表示空间的掩码预测损失，用预测器从可见区域嵌入推断目标区域。该方法在KITTI上显著提升DINOv3基线性能，零样本迁移至Make3D和Cityscapes达到最佳或次优。无需改变推理架构，即可提升精度和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26600v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1172, \"height\": 779, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26600v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1232, \"height\": 615, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26600v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1275, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26600v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1240, \"height\": 812, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26600v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1269, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26600v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1155, \"height\": 1045, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26600v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1278, \"height\": 1097, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 520, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1264, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 954, \"height\": 564, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1104, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1262, \"height\": 188, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1262, \"height\": 158, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1264, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1144, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26600v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 777, \"height\": 281, \"label\": \"Table\"}]"
motivation: 现有自监督单目深度估计受限于光度损失，难以解耦深度与外观。
method: 在DINOv3编码器表示空间增加掩码预测损失，由预测器从可见上下文推断目标块。
result: KITTI上一致优于DINOv3基线，零样本迁移在多个数据集达SOTA。
conclusion: 提出高效自监督深度估计强化方法，无推理开销，泛化性强。
---

## 摘要
自监督单目深度估计通常依赖于光度重建损失，这种损失将深度、姿态和外观假设耦合在一起。在本文中，我们提出了JEPADepth，一个自监督单目深度框架，它引入了一个受图像联合嵌入预测架构（I-JEPA）启发的互补训练目标，用于自监督深度学习。我们的方法通过一个在预训练DINOv3视觉Transformer编码器的表示空间中计算的掩码预测损失来增强标准的光度损失流程。预测器在结构化掩码下从可见的上下文区域嵌入推断目标区域嵌入，并在推理时与目标编码器一起被丢弃，不增加部署成本。在KITTI上，添加JEPA目标一致地提高了基于相同DINOv3光度基线的性能，而无需改变推理时的架构。与先前的单目自监督方法相比，JEPADepth与最先进的基于Transformer的方法具有竞争力，并在标准基准测试中优于强CNN基线。在零样本迁移（在KITTI上训练，未微调直接评估）中，JEPADepth在Make3D和Cityscapes上的多个指标中均达到了比较方法中最好或接近最好的性能。

## Abstract
Self-supervised monocular depth estimation typically relies on photometric reconstruction losses that couple depth, pose, and appearance assumptions. In this paper, we propose JEPADepth, a self-supervised monocular depth framework that incorporates a complementary training objective inspired by Image Joint-Embedding Predictive Architectures (I-JEPA) for self-supervised depth learning. Our method augments a standard photometric pipeline with a masked prediction loss computed in the representation space of a pretrained DINOv3 Vision Transformer encoder. A predictor infers target-region embeddings from visible context-region embeddings under structured masking, and is discarded along with the target encoder at inference time, adding no deployment cost. On KITTI, adding the JEPA objective consistently improves performance over the same DINOv3-based photometric baseline, without changing the inference-time architecture. Compared to prior monocular self-supervised methods, JEPADepth is competitive with state-of-the-art transformer-based approaches and outperforms strong CNN-based baselines on the standard benchmark. In zero-shot transfer (trained on KITTI and evaluated without fine-tuning), JEPADepth achieves the best or near-best performance among the compared methods on both Make3D and Cityscapes across multiple metrics.

---

## 论文详细总结（自动生成）

# JEPADepth：用于自监督单目深度估计的掩码预测表示学习 — 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：自监督单目深度估计主要依赖光度重建损失（photometric reconstruction loss），这种损失函数将深度、相机姿态和物体外观假设强耦合在一起，导致模型难以解耦这些因素，从而限制了泛化能力，尤其是在未见过的场景中（零样本迁移）。
- **整体含义**：现有方法仅依靠光度一致性约束，忽略了场景中丰富的语义与结构信息。本文提出通过引入一种**掩码预测表示学习**（masked predictive representation learning）目标，作为光度损失的补充，迫使模型学习更鲁棒、更具泛化性的视觉表示，从而提升自监督深度估计的精度和迁移能力。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：借鉴图像联合嵌入预测架构（I-JEPA），在**预训练 DINOv3 视觉 Transformer（ViT）编码器的表示空间**中增加一个**掩码预测损失**。该损失让一个轻量级的**预测器（predictor）** 从可见的上下文区域嵌入去推断被掩码的目标区域嵌入，从而鼓励模型学习场景的全局上下文与结构先验。
- **关键技术细节**：
  - **架构**：采用 DINOv3 ViT 作为共享的图像编码器，分别有“上下文编码器”和“目标编码器”（参数可能共享或部分共享）。训练时，对输入图像施加结构化掩码（如块掩码），上下文区域通过上下文编码器得到嵌入，目标区域通过目标编码器得到目标嵌入（作为监督信号）。
  - **预测器**：是一个小型 Transformer 网络，接收上下文嵌入和掩码位置信息，输出预测的目标区域嵌入。预测损失在 DINOv3 表示空间中计算，例如 L2 损失或余弦相似度损失。
  - **联合训练**：总损失 = 光度重建损失（标准的自监督深度估计损失） + λ × 掩码预测损失。λ 为平衡权重。
  - **推理阶段**：丢弃预测器和目标编码器，只保留上下文编码器和深度解码器，**不增加任何推理成本**。
- **公式/算法流程**（文字说明）：
  1. 输入图像 I，随机生成结构化掩码 M（如 2×2 块掩码）。
  2. 将 I 输入上下文编码器（DINOv3 ViT），得到可见区域的上下文嵌入 E_ctx。
  3. 将 I 输入目标编码器（DINOv3 ViT，可能使用不同参数或 stop-gradient），得到完整图像嵌入，从中提取被掩码区域的目标嵌入 E_target。
  4. 预测器以 E_ctx 和掩码位置信息为输入，输出预测嵌入 E_pred。
  5. 计算掩码预测损失 L_mask = MSE(E_pred, E_target) 或类似。
  6. 同时，将上下文编码器的输出送入深度解码器得到深度图 D，与标准光度重建损失 L_photo 结合（利用相邻帧的重投影误差）。
  7. 总损失 L_total = L_photo + λ L_mask，反向传播更新参数。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集**：
  - **主要训练和评估**：KITTI（自动驾驶场景，单目深度估计标准基准）。
  - **零样本迁移**：在 KITTI 上训练，直接评估于 **Make3D**（室内外场景）和 **Cityscapes**（街景场景），不进行微调。
- **Benchmark**：采用 KITTI Eigen 分割的深度评估指标（如 Abs Rel、Sq Rel、RMSE、δ <1.25 等）。
- **对比方法**：
  - 与基于相同 DINOv3 光度基线的变体比较（消融）。
  - 与最先进的基于 Transformer 的自监督方法（如 Monodepth2 变体、其他 ViT 方法）比较。
  - 与强 CNN 基线（如 Monodepth2、PackNet-SfM）比较。
  - 在零样本迁移中，与多种公开方法对比。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量以及训练总时长。仅提到在 KITTI 上训练，但未给出具体算力信息。这一缺失使得复现成本难以估算。

## 5. 实验数量与充分性

- **实验组数**：从摘要和元数据判断，至少包含以下实验：
  - KITTI 上的**消融实验**：比较“光度基线” vs “光度+JEPA目标”（证明 JEPA 一致提升）。
  - KITTI 上**与多种 SOTA 方法对比**（包括多个 Transformer 和 CNN 方法）。
  - **零样本迁移实验**：在 Make3D 和 Cityscapes 上评估（跨数据集泛化）。
  - 可能还包括**不同掩码策略**、不同平衡权重 λ 的消融（但未明确提及）。
- **充分性与公平性**：
  - 优点：对比方法涵盖当前主流；零样本迁移检验泛化能力；消融实验证实核心贡献的有效性。
  - 不足：实验仅在三个数据集上验证，且 KITTI 和 Cityscapes 同为街景，多样性有限；未给出详细数值表格（本总结仅基于摘要），无法确认所有指标的具体提升幅度；缺少对预测器结构、掩码大小等超参数的敏感性分析（可能正文中有，但摘要未体现）。
  - 总体评估：实验设计基本合理，但公开信息有限，无法完全判断充分性。

## 6. 论文的主要结论与发现

- 在 KITTI 上，**添加 JEPA 目标一致地提高了**基于相同 DINOv3 编码器的光度基线性能，且无需改变推理架构。
- JEPADepth 在 KITTI 标准基准上与最先进的**基于 Transformer 的方法竞争**，并显著**优于强 CNN 基线**。
- 零样本迁移至 Make3D 和 Cityscapes，JEPADepth 在**多个指标上达到最好或接近最好的性能**，展现了强大的泛化能力。
- 方法**不增加推理部署成本**，因为预测器和目标编码器仅在训练时使用。

## 7. 优点

- **简洁高效**：在现有自监督框架上仅增加一个辅助损失，不改变推理架构，实现即插即用。
- **代表能力强**：利用预训练 DINOv3 的丰富表示空间，迫使模型学习场景的结构先验，有效补充光度损失的不足。
- **泛化性好**：零样本迁移结果突出，表明学到的表示具有场景不变性。
- **无推理开销**：训练完成后，额外组件可安全丢弃，适合实际部署。

## 8. 不足与局限

- **依赖预训练编码器**：方法强依赖于 DINOv3 预训练权重，若更换其他编码器或从头训练，效果未知。
- **掩码策略设计**：结构化掩码（块大小、比例、策略）可能对性能敏感，论文未在摘要中讨论其影响。
- **实验覆盖有限**：仅在三个数据集上验证，缺少对更多场景（如室内、动态场景、恶劣天气）的评估；未与近期基于扩散模型等方法比较。
- **算力资源未公开**：无法评估训练成本，可能对资源有限的研究者不友好。
- **理论分析不足**：为何掩码预测损失有助于深度估计，缺乏更深入的因果分析或可视化解释。

（完）
