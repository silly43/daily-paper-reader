---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：一种统一的世界与动作建模扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:world-models"]
score: 9.0
evidence: 统一扩散Transformer用于世界与动作建模
tldr: 针对机器人策略依赖大型预训练VLM作为动作骨干的问题，提出WorldDiT统一扩散Transformer架构，同时实现动作生成与视觉世界建模。该架构无需大型VLM，通过单一扩散Transformer输出连续动作并预测未来图像块。在LIBERO四个套件上，WorldDiT以sub-billion参数位于帕累托前沿，兼顾模型大小与成功率，为未来扩展研究提供强基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 避免依赖大型预训练VLM动作骨干，同时增强动作生成与视觉世界建模的耦合。
method: 采用统一扩散Transformer，联合生成连续动作块与预测未来相机帧的归一化RGB patch。
result: 在LIBERO四个套件上，以sub-billion参数达到帕累托最优模型大小与成功率。
conclusion: 提供无需大型VLM的高效通用机器人动作与世界建模基线，适合未来扩展。
---

## 摘要
许多最新的机器人策略通过使用大规模预训练视觉-语言模型（VLM）作为动作骨干来追求更强的控制。我们提出了WorldDiT，一种统一的扩散变换器架构，它将动作生成与视觉世界建模相结合，在没有大型预训练VLM动作骨干的情况下实现了强劲的性能。在训练过程中，单个扩散变换器生成连续的动作块，并从未来摄像头帧中预测归一化的RGB块目标。在四个LIBERO仿真套件中，WorldDiT位于报告的总模型参数和平均成功率帕累托前沿上。这些结果为未来的缩放研究提供了一个强大的低于十亿参数的基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：当前许多机器人策略采用“大型预训练视觉-语言模型（VLM）作为动作骨干”的范式，虽然带来了强大的感知和语言理解能力，但难以区分是模型规模、架构设计还是预训练数据带来的控制性能提升。本文的核心问题是：**能否在不使用大型预训练VLM作为动作骨干的前提下，通过统一架构同时实现连续动作生成与视觉世界建模，并保持强控制性能？**
- **整体含义**：WorldDiT证明，一个统一扩散Transformer（DiT）足以同时完成动作预测与未来视觉帧的RGB块预测，且性能可与依赖大型VLM的方法竞争。这为缩小模型规模、降低计算开销提供了新方向，同时为扩展研究提供了低于10亿参数的强基线。

### 2. 论文提出方法论

- **核心思想**：利用单个共享DiT骨干网，联合建模连续机器人动作和未来相机帧的归一化RGB块。训练时同时预测动作和未来视觉块，推理时仅使用动作分支，视觉预测头被移除。
- **关键技术细节**：
  - **输入编码**：冻结的视觉编码器（MAE）和文本编码器（CLIP）处理多视图图像和语言指令；可训练的机器人状态编码器分别编码臂和夹爪状态，拼接后投影为状态token。
  - **输出目标**：动作目标为7步连续动作块$A_i \in \mathbb{R}^{7 \times 7}$；未来视觉目标为从主相机和腕相机帧中选取的128个归一化RGB块（每个相机64个块），每个块768维。
  - **训练**：基于流匹配（Flow Matching）损失。对于动作目标y和RGB块目标y_rgb，定义噪声路径$x_\tau = (1-\tau)\epsilon + \tau y$，损失函数为：
    $$\mathcal{L}_{\text{flow}} = \mathbb{E}_{\epsilon, y, \tau} \left[ \| v_\theta(x_\tau, \tau, c) - (y - \epsilon) \|_2^2 \right]$$
    总损失为动作损失和RGB块损失的加权和：$\mathcal{L}_{\text{total}} = w_{\text{action}} \mathcal{L}_{\text{action}}^{\text{flow}} + w_{\text{rgb}} \mathcal{L}_{\text{rgb}}^{\text{flow}}$，权重分别设为0.1和0.001。
  - **推理**：从高斯噪声初始化动作token，通过20步欧拉积分生成完整动作块，仅执行前3个动作，然后滑动窗口重新规划。不依赖任何未来视觉目标。
  - **骨干结构**：DiT深度4、隐藏维度1024、16个注意力头、4个寄存器token（register tokens）。上下文窗口包含3步历史观测，总窗口长度为10步。

### 3. 实验设计

- **数据集与场景**：使用LIBERO仿真基准（共4个下游套件）：**LIBERO Spatial**、**LIBERO Object**、**LIBERO Goal**、**LIBERO Long**。此外，使用大型多任务套件**LIBERO 90**进行预训练。
- **Benchmark**：LIBERO是机器人操作的标准benchmark，包含多个任务，评估成功率和模型参数量的权衡。
- **对比方法**：报告了24种方法，分为两类：
  - **使用大型预训练VLM动作骨干**：如ACoT VLA (98.5%)、MMaDA VLA (98.0%)、VLANeXt (97.4%)、π0.5 (96.9%)、OpenVLA OFT (97.1%)等。
  - **不使用大型VLM动作骨干**：如DreamVLA (92.6%)、FlowVLA (88.1%)、DiT Policy (82.4%)、Octo (75.1%)、Diffusion Policy (72.4%)等。

### 4. 资源与算力

- **训练配置**：单节点8块**RTX Pro 6000 GPU**。预训练在LIBERO 90上进行30个epoch，每GPU批次40，梯度累积2；微调每GPU批次32，梯度累积2，有效批次512。使用bf16混合精度，学习率1e-4（余弦调度，1个epoch预热）。
- **算力估计**：未明确给出总训练时长，但配置属于中等规模（8块高端消费级GPU），训练效率较高。

### 5. 实验数量与充分性

- **实验数量**：每个套件测试500个episode（其中300个用于阶段性检查点选择，200个用于最终报告）。共4个套件，总计2000个模拟episode。
- **充分性分析**：
  - **优点**：覆盖了LIBERO所有四个下游套件，报告了每个套件的成功率及平均成功率，与24种方法对比，实验量在同类工作中常见。
  - **不足**：
    - 仅使用**一个配置**（399M参数，固定上下文长度3、动作块长度7、采样步数20），未进行消融研究（如不同上下文长度、块大小、采样步数、损失权重等）。
    - 报告的平均成功率（94.9%）包含了300个用于检查点选择的episode，因此“并非完全独立测试集估计”，存在过拟合风险。
    - 仅评估LIBERO模拟环境，未在真实机器人或更复杂的基准（如CALVIN、MetaWorld）上验证。
    - 对比方法中许多结果来自其原始报告，而非统一复现，公平性受限。

### 6. 论文主要结论与发现

- **核心发现**：WorldDiT以**399M总参数**（其中训练参数135.1M）在LIBERO各套件上分别取得98.0%、97.0%、92.8%、91.8%的成功率，平均**94.9%**。在参数-成功率帕累托前沿上，所有更高成功率的方法都使用了更大参数集，而参数相近或更小的方法成功率更低。
- **结论**：统一扩散Transformer架构可以很好地耦合动作生成与视觉世界建模，且无需大型VLM骨干，为未来研究提供了紧凑的可扩展基线。

### 7. 优点

- **架构简洁高效**：无需大型预训练VLM，降低了计算和存储开销，易于部署。
- **联合训练+推理解耦**：视觉预测仅在训练时提供监督，推理时完全移除，不增加推理负担。
- **帕累托前沿位置**：表明模型大小与性能之间取得了良好平衡，适合资源受限场景。
- **方法透明**：提供了完整的模型配置、训练损失、采样步数等细节，易于复现。

### 8. 不足与局限

- **实验覆盖面窄**：仅基于LIBERO模拟环境，缺乏真实机器人物理实验和多任务泛化性测试。
- **消融实验缺失**：未分析各组件（如RGB预测损失权重、上下文长度、采样步数、编码器选择）的贡献，难以理解关键设计因素。
- **报告偏差**：最终成功率混合了检查点选择阶段的数据，可能高估了真实泛化性能。
- **对比公平性**：由于公共权重和代码不完整，不同方法的结果来自不同报告，协议可能不一致。
- **可扩展性未验证**：作者仅测试了单一小模型（399M），未展示模型规模增大时的行为（实际规模扩展实验是未来工作）。
- **方法限制**：预测未来RGB块仅从单一时间步（i+H）采样，而非完整视频序列，世界模型能力有限；且未与强化学习或在线学习结合。

（完）
