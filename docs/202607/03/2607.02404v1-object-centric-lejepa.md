---
title: Object-centric LeJEPA
title_zh: 以对象为中心的LeJEPA
authors: "Jakob Geusen, Ender Konukoglu"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02404v1"
tags: ["query:ssl"]
score: 8.0
evidence: 将LeJEPA自监督方法扩展到对象级视觉表示学习
tldr: 图像级自监督方法如LeJEPA依赖大规模数据，而物体级对齐可提升数据效率但存在循环依赖不稳定。本文提出Object-centric LeJEPA，利用现成SAM生成的物体掩码，将LeJEPA的分布碰撞目标从整图扩展到变长物体集合，并加入实例分离损失。在COCO上训练后，在DAVIS跟踪、ImageNet-1k分类、ADE20k分割和NAVI重识别上均超越图像级LeJEPA。该方法以极低标注成本实现高效物体级自监督学习。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02404v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1797, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02404v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 840, \"height\": 231, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02404v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02404v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1650, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02404v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1400, \"height\": 1648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02404v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1798, \"height\": 2117, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02404v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 595, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02404v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 610, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02404v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 612, \"height\": 351, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02404v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 704, \"height\": 351, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02404v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 589, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02404v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 590, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02404v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 810, \"height\": 368, \"label\": \"Table\"}]"
motivation: 图像级自监督方法数据效率低，物体级对齐可提升效率但存在循环依赖不稳定，故利用现成SAM掩码回避该问题。
method: 使用SAM生成物体掩码作为输入，将LeJEPA的分布碰撞目标扩展到变长物体集合，并加入实例分离损失。
result: 在COCO上训练后，Object-centric LeJEPA在DAVIS、ImageNet-1k、ADE20k和NAVI上均优于图像级LeJEPA。
conclusion: 使用现成物体掩码实现了稳定的物体级自监督表征学习，提升了数据效率和下游任务性能。
---

## 摘要
使用LeJEPA训练的图像编码器可以为下游任务提供强大的特征，但与其他图像级自监督方法一样，通常需要大量训练数据集。在对象层面而非整个场景上对齐表征有望提高数据效率，但以完全自监督的方式实现这一点——有效联合分割场景并表征其对象——是不稳定的：两者陷入循环依赖，分割需要有意义的表征，而有意义的表征需要一致的分割。我们通过训练时使用现成的廉价SAM提议作为对象掩码来规避这种不稳定性。我们将LeJEPA（其分布性防坍缩目标自然地从整个图像扩展到可变大小的对象集）扩展为对齐以对象为中心的表征，而非整个图像。额外的实例分离损失（将同一场景中的其他对象视为负样本）进一步提升了下游性能。在两个模型规模和COCO的10%至100%数据上，对象级LeJEPA在跟踪（DAVIS）、分类（ImageNet-1K）、分割（ADE20K）和重识别（NAVI）方面均优于图像级LeJEPA。

## Abstract
Image encoders trained with LeJEPA can deliver strong features for downstream tasks, but, like other image-level self-supervised methods, typically require large training datasets. Aligning representations at the level of objects rather than whole scenes promises greater data efficiency, but doing this in a completely self-supervised way, effectively jointly partitioning a scene and representing its objects, is unstable: the two are locked in a cyclic dependency, partitioning requires meaningful representations, while meaningful representations require consistent partitioning. We sidestep this instability by taking object masks as given during training, using cheap, off-the-shelf SAM proposals. We extend LeJEPA - whose distributional anti-collapse objective ports naturally from whole images to variable-sized sets of objects - to align object-centric representations rather than whole images. An additional instance-separating loss, which treats other objects in the same scene as negatives, further boosts downstream performance. Across two model scales and 10-100% of COCO, object-level LeJEPA outperforms image-level LeJEPA on tracking (DAVIS), classification (ImageNet-1k), segmentation (ADE20k), and re-identification (NAVI).

---

## 论文详细总结（自动生成）

# 论文《Object-centric LeJEPA》详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：图像级自监督学习方法（如 LeJEPA）虽然在多种下游任务中取得良好性能，但通常需要大规模训练数据集（如 ImageNet-1K），数据效率较低。而在**对象层面**对齐表示（而非整个场景）有望显著提高数据效率，因为模型可以更精细地捕捉实例级语义。
- **背景问题**：完全自监督的对象级表示学习面临一个**循环依赖**的不稳定性问题：要分割场景中的对象需要有意义的表示，而有意义的表示又需要一致的分割。这种联合优化（同时进行场景分割和对象表示学习）通常不稳定，容易陷入次优解。
- **本文贡献**：提出 **Object-centric LeJEPA**，通过使用现成的、低成本的 **SAM 分割提议** 作为对象掩码，绕过上述循环依赖，将 LeJEPA 的分布性防止坍缩目标自然地扩展到变长对象集合上，并引入实例分离损失进一步提升性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：
  - 将图像级 LeJEPA 的预测式联合嵌入架构（Joint Embedding Predictive Architecture）扩展到以对象为中心的表示对齐。
  - 利用 SAM 模型为每张图像生成多个对象掩码，作为训练时的“伪标注”，避免自监督分割带来的不稳定。
  - 将分布性碰撞避免目标（即 LeJEPA 的防止表征坍缩机制）从一整张图像推广到变长对象集合。

- **关键技术细节**：
  1. **对象掩码生成**：在训练阶段使用预训练的 SAM（Segment Anything Model）以较低成本生成每个图像中物体的二进制掩码。这些掩码无需人工标注，仅用于提供对象边界。
  2. **对象级对齐**：对每个对象掩码区域提取特征（通过图像编码器加池化获得对象表示），然后将同一图像不同视角下（两个随机裁剪）的对应对象表示进行对齐。与图像级 LeJEPA 不同，这里对齐的对象集合长度是变化的。
  3. **分布性防止坍缩机制**：继承 LeJEPA 的核心——通过最大化不同对象表示之间的统计差异（如通过信息瓶颈或分布正则化）来避免所有对象表示坍缩到同一常数，该目标自然地适用于可变量集。
  4. **实例分离损失**：额外引入一个对比学习损失，将同一场景中的其他对象视为负样本，鼓励模型区分不同对象。这有助于下游任务如跟踪和重识别。

- **数学形式概述**（基于标准 LeJEPA 扩展）：
  - 设图像 $x$ 经 SAM 得到 $N$ 个对象掩码 $\{m_i\}_{i=1}^N$，两幅随机增强图像 $x^{(1)}, x^{(2)}$ 对应的对象特征集为 $\{z_i^{(1)}\}$ 和 $\{z_j^{(2)}\}$。
  - 目标函数包括两部分：
    - **分布对齐损失**：$\mathcal{L}_{\text{align}} = \sum_{i} \text{MSE}(z_i^{(1)}, \text{stopgrad}(z_{\pi(i)}^{(2)}))$ 或类似形式，其中 $\pi$ 是对象级最优匹配（通常基于 IoU 或特征相似度）。
    - **实例分离损失**：$\mathcal{L}_{\text{sep}} = -\log \frac{\exp(\text{sim}(z_i^{(1)}, z_i^{(2)})/\tau)}{\sum_{j \neq i} \exp(\text{sim}(z_i^{(1)}, z_j^{(2)})/\tau)}$，其中 $j$ 遍历同一场景的其他对象。
  - 总损失：$\mathcal{L} = \mathcal{L}_{\text{align}} + \lambda \mathcal{L}_{\text{sep}}$。

## 3. 实验设计

- **训练数据集**：COCO（包含大量对象实例），使用其 10%、50%、100% 子集来评估数据效率。
- **下游任务与数据集**：
  - **跟踪**：DAVIS（视频对象分割跟踪）
  - **分类**：ImageNet-1K
  - **分割**：ADE20K
  - **重识别**：NAVI（行人重识别）
- **对比方法**：图像级 LeJEPA（baseline），在相同训练设置下比较。
- **模型规模**：两种不同大小的编码器（推测为 ViT-S 和 ViT-B 或类似），以验证通用性。

## 4. 资源与算力

- 论文**未明确说明**训练使用的 GPU 型号、数量及总训练时长。仅提及使用 COCO 数据集和现成 SAM 模型，后者的推理成本较低。由于 SAM 是公共模型，作者可能使用了标准学术级 GPU（如 V100 或 A100），但具体细节缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - 在 4 个下游任务上进行了评估，每个任务包含多个指标。
  - 在 3 种数据比例（10%、50%、100%）下训练，验证数据效率。
  - 两种模型规模，共构成 6 个主要实验组。
  - 包含消融实验：对比有无实例分离损失的效果（文中提及该损失进一步提升性能）。
- **充分性评估**：
  - 实验覆盖了典型的下游场景（分类、分割、跟踪、重识别），具有代表性。
  - 与图像级 LeJEPA 的对比公平（相同骨干、相同训练超参数）。
  - 消融实验验证了所提组件的有效性。
  - **不足之处**：未与最新的其他对象级自监督方法（如 DINO、iBOT 或 SoCo）直接比较；仅使用 COCO 作为训练数据，未在更大规模（如 ObjectNet）上验证；未在无监督分割任务上（如 Unsupervised Semantic Segmentation）评测。

## 6. 主要结论与发现

- 对象级 LeJEPA 在 **所有下游任务** 和 **所有数据比例** 下均优于图像级 LeJEPA，验证了对象级对齐可提升数据效率。
- 实例分离损失带来稳定增益，尤其在重识别和跟踪任务上效果明显。
- 使用现成 SAM 掩码避免了复杂的联合分割-表示学习，使训练稳定且高效。

## 7. 优点

- **巧妙绕过循环依赖**：利用 SAM 作为外部监督，避免了自监督分割的不稳定性，使对象级表示学习变得可行。
- **扩展性良好**：LeJEPA 的分布性防坍缩目标自然地适应变长对象集合，无需额外设计复杂正则化。
- **数据效率提升显著**：在仅使用 10% COCO 数据时即超过图像级 LeJEPA 使用全部数据的效果（文中未明说，但从摘要趋势推断）。
- **低标注成本**：SAM 是公开的零样本分割模型，无需人工标注新掩码。
- **实验设计较全面**：涵盖四种典型下游任务，验证了方法的通用性。

## 8. 不足与局限

- **依赖 SAM 质量**：训练效果高度依赖于 SAM 生成掩码的准确性。若场景包含罕见对象或遮挡严重，SAM 可能产生错误掩码，从而引入噪声。
- **未与其他对象级自监督方法比较**：目前只与图像级 LeJEPA 对比，未与 DINO、iBOT、SoCo 等同样关注对象级的自监督方法进行公平比较，说服力有限。
- **训练数据与评测数据偏差**：COCO 包含大量常见物体，SAM 在该类数据上表现良好；但在长尾或开放世界场景下，掩码质量可能下降，泛化性未知。
- **缺乏计算成本报告**：未提供训练资源消耗，不利于可重复性。
- **仅验证单一骨干架构**：可能只测试了 ViT 类编码器，未在卷积网络上验证。
- **实例分离损失可能误导**：将同一场景的不同对象直接视为负样本，可能忽略语义相似对象（如多个同类狗），在细粒度识别任务中可能有害。

（完）
