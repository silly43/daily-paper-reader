---
title: Understanding Geometric Representations in Self-Supervised Vision Transformers via Subspace Intervention
title_zh: 通过子空间干预理解自监督视觉Transformer中的几何表示
authors: "Weichen Zhou, Yawen Zou, Chunzhi Gu, Ran Dong, Haoran Xie, Chao Zhang"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01987v1"
tags: ["query:ssl"]
score: 9.0
evidence: 研究自监督视觉Transformer中的几何表示
tldr: 自监督视觉Transformer如何编码几何信息？线性探针无法解耦拓扑结构。本文提出子空间干预框架，对线性探针权重做SVD分解，隔离低秩几何子空间。发现DINOv2对齐空间特征利于线性提取，MAE分散信号需更大上下文；几何表示高度可压缩；中间层几何精度最高。这些结果连接内部编码与下游性能，指导特征选择和轻量解码器设计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01987v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1245, \"height\": 819, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1224, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 588, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1256, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1269, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1258, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 597, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01987v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 605, \"height\": 432, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01987v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01987v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 551, \"height\": 279, \"label\": \"Table\"}]"
motivation: 现有线性探针无法解耦自监督ViT中几何表示的拓扑结构，需新方法分析内部编码机制。
method: 通过SVD分解收敛线性探针权重，隔离包含几何信号的低秩子空间，进行可控干预。
result: DINOv2对齐特征利于线性提取，MAE分散信号需更大上下文；几何表示高度可压缩；中间层几何精度峰值后转向语义抽象。
conclusion: 揭示了内部编码与下游性能的关联，为特征选择与轻量解码器设计提供理论依据。
---

## 摘要
我们引入了一个受控子空间干预框架，以研究自监督视觉Transformer（ViT）如何编码密集的几何信息。虽然线性探测被广泛用于评估几何表示，但它将特征视为黑箱，未能解开底层拓扑结构。为了解决这个问题，我们使用奇异值分解（SVD）分解收敛线性探测器的权重，以隔离包含显式几何信号的低秩子空间。我们的视角产生了三个关键见解：（1）预训练目标决定了特征如何被编码。DINOv2对齐空间特征以实现高效的线性提取，而掩码自编码器（MAE）倾向于分散这些信号，需要更广泛的空间上下文。（2）显式几何表示具有高度可压缩性，表明密集预测头可能被限制在低秩子空间中，性能损失极小。（3）逐层任务亲和性表明，几何精度在中间层达到峰值，然后在最终层让位于语义抽象。通过将内部编码机制与下游性能联系起来，这些发现为有效的特征选择和轻量级解码器设计提供了基础。源代码可在https://github.com/Zhou-Weichen/Geosubprobe获取。

## Abstract
We introduce a controlled subspace intervention framework to investigate how self-supervised Vision Transformers (ViTs) encode dense geometric information. While linear probing is widely used to assess geometric representations, it treats features as a black box, failing to disentangle the underlying topology. To address this issue, we decompose the weights of converged linear probes to isolate the low-rank subspaces containing explicit geometric signals using Singular Value Decomposition (SVD). Our perspective yields three key insights: (1) Pre-training objectives determine how features are encoded. DINOv2 aligns spatial features for efficient linear extraction, while Masked Autoencoders (MAE) tend to disperse these signals, requiring a broader spatial context. (2) Explicit geometric representations are highly compressible, suggesting dense predictive heads could potentially be constrained to low-rank subspaces with minimal performance loss. (3) The layer-wise task affinity suggests that geometric precision peaks at intermediate layers before yielding to semantic abstraction in the final layers. By connecting internal encoding mechanics with downstream performance, these findings provide a basis for effective feature selection and lightweight decoder design. The source code is available at https://github.com/Zhou-Weichen/Geosubprobe.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：自监督视觉Transformer（ViT）在密集预测任务中表现出强大的几何理解能力，但现有评估方法（如线性探测）仅关注最终预测精度，将特征视为黑箱，无法揭示几何信息在特征空间中的拓扑结构。具体地，当线性探针性能差时，不清楚几何信息是缺失、纠缠在非线性流形中，还是分散在空间patch上。
- **核心问题**：不同自监督预训练范式（自蒸馏、掩码图像建模、混合方法）如何影响几何信息的编码格式？几何信号是否集中在低维子空间？逐层几何与语义表示的演变规律是什么？
- **整体含义**：通过理解内部编码机制，可为特征选择、轻量解码器设计及下游任务适配提供理论指导。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：提出**可控子空间干预框架**，通过对收敛线性探测器的权重矩阵进行奇异值分解（SVD），分离出与任务对齐的低秩几何方向，从而量化几何信息的可压缩性和逐层分布。
- **关键技术细节**：
    1. **三层次探测（Readability Gap）**：
        - 线性探测（Tier 1）：1×1卷积直接映射patch特征到目标空间，衡量显式线性可读几何。
        - MLP探测（Tier 2）：点式非线性MLP（GELU激活），隔离局部非线性纠缠。
        - DPT解码器（Tier 3）：多尺度特征聚合与全局感受野，评估全局空间碎片化程度。
        - 性能差异定义“可读性差距” = 局部纠缠 + 空间碎片化。
    2. **子空间干预（Subspace Intervention）**：
        - 对线性探针权重 $W \in \mathbb{R}^{C \times D}$ 进行SVD：
          $$W = U \Sigma V^\top$$
          其中$V$的列构成任务对齐的正交基。
        - 定义秩$k$的**对齐子空间** $S_k = \text{span}\{v_1,\dots,v_k\}$。
        - 特征投影：$\tilde{Z}_k = Z V_k V_k^\top$，通过固定线性头评估其性能。
        - 控制实验：
            - **随机子空间** $R_k$：投影到随机正交方向，破坏任务对齐结构。
            - **残差子空间** $S^\perp_k$：原始特征减去对齐投影，验证几何信号是否集中在top-k方向。
    3. **能量分配分析**：将全局探头的右奇异向量按层分块（如四层），计算每层在奇异值加权下的能量占比：
       $$E_i = \frac{\sum_{m=1}^C \sigma_m^2 \|v_m^{(l_i)}\|^2}{\sum_{j=1}^4 \sum_{m=1}^C \sigma_m^2 \|v_m^{(l_j)}\|^2}$$
- **算法流程**（文字描述）：
    1. 冻结ViT骨干，在不同层提取patch特征。
    2. 训练线性探针、MLP探针、DPT解码器，记录性能（SA-δ1等）。
    3. 对收敛的线性探针权重做SVD，提取前$k$个右奇异向量。
    4. 对特征进行三种投影（对齐、随机、残差），通过固定线性头评估。
    5. 分析不同$k$下的性能恢复曲线，以及按层能量分布。

### 3. 实验设计：数据集、基准与方法对比

- **主要数据集**：NYU Depth V2（室内深度估计），使用SA-δ1（阈值准确率）、RMSE、AbsRel指标。扩展至表面法线估计（角精度 $d_1 < 11.25^\circ$）和40类语义分割（mIoU）。
- **补充数据集**：KITTI（室外深度）、NAVI（深度/法线）——仅出现在补充材料中。
- **基准模型**：三种自监督ViT-Large变体：
    - DINOv2（自蒸馏）
    - MAE（掩码图像建模）
    - iBOT（混合方法）
- **对比方法**：
    - 探测架构对比：线性 vs MLP vs DPT。
    - 子空间秩$k$的消融：$k=4,8,16,32,64,128$。
    - 控制基线：随机子空间、残差子空间。
    - 层对比：单层特征 vs 多尺度融合。
    - 任务对比：深度、法线、语义分割。
    - 跨域验证（补充）和ViT-Base变体（补充）。

### 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量或训练时长。仅在结尾处提到“受资源约束，未能评估ViT-G模型”，说明算力有限。
- 根据论文细节，训练线性探针和MLP/DPT解码器需要中等计算量，但具体数值未报告。

### 5. 实验数量与充分性

- **实验数量**：核心实验约8组（表1、图2-5、图7-8、表2）。补充材料包含更多跨域、跨模型、跨任务的结果。
- **充分性**：
    - **优点**：控制实验严谨（固定骨干、冻探头；随机/残差基线排除混淆因素）；逐层分析覆盖所有24层；任务对比区分几何与语义；子空间稳定性验证（多初始化）。
    - **不足**：主要结果仅基于NYUv2单室内场景，跨域验证只在补充材料中部分呈现；仅测试ViT-Large，未验证ViT-B或更大模型（ViT-G）以佐证规律普适性；线性探针本身收敛性对子空间可能敏感，但已通过多初始化稳定性检验。

### 6. 主要结论与发现

1. **预训练范式决定编码格式**：
    - DINOv2：几何信息高度对齐，线性探针即可恢复大部分信号（SA-δ1=0.9157），仅需轻微非线性增强。
    - MAE：几何信号分散，线性探针性能差（0.6033），需DPT全局聚合才能明显提升（0.7022），表明依赖空间上下文。
    - iBOT：介于两者之间，平衡线性可读性与局部细节。
2. **几何表示高度可压缩**：
    - 所有模型在低秩（$k \leq 32$）对齐子空间下恢复超过95%的线性探针性能。随机子空间和残差子空间性能极低（SA-δ1 < 0.18），证明显式几何信号集中在top-k方向。
    - MAE的饱和速度最快（$k=32$时>98%），但绝对信息量少；DINOv2需要更多维度（$k \geq 64$）以保留精细边界，说明其线性可读几何更丰富。
3. **逐层任务亲和性**：
    - DINOv2中几何能量集中在中层（$l_{12},l_{18}$占72%），末层（$l_{24}$）仅10%，对应几何精度峰值后转向语义抽象。
    - MAE和iBOT能量分布均匀（接近均匀0.25），需跨层聚合。
    - 不同任务峰值层不同：表面法线在$l_{18}$，深度在$l_{21}$，语义分割在$l_{24}$，表明任务特定特征路由的必要性。
4. **子空间稳定性**：低秩（$k\leq16$）子空间跨初始化高度一致（>0.93），说明几何核心是稳定结构，非优化伪影。

### 7. 优点

- **分析框架创新**：从黑箱精度评估转向内部拓扑检查，提出“可读性差距”和“子空间干预”两个层次，系统解耦了信息存在性、可访问性和分布性。
- **实验设计严谨**：引入随机子空间和残差子空间作为严格对照，确保任务对齐子空间的性能提升归因于几何信号集中；冻结探头和骨干消除了训练干扰。
- **发现具有实用价值**：揭示了几何表示的可压缩性，启发轻量解码器设计（如低秩投影头）；逐层任务亲和性指导特征选择（如中间层特征用于几何任务，末层用于语义）。
- **结论具体可操作**：如指出DINOv2适合线性探测、MAE需全局解码器；子空间秩选择有定量指导（$k=64$即可接近全秩）。

### 8. 不足与局限

- **分析局限**：仅对线性探针进行了子空间干预，无法解开非线性纠缠部分。高度非线性的几何信号（如MAE中的细粒度细节）仍未被分离。
- **实验覆盖不完整**：
    - 主要实验仅在NYUv2室内场景上进行，跨域（KITTI、NAVI）仅作为补充，且未与主要结论深度绑定。
    - 仅评估ViT-Large，未验证ViT-B或ViT-H/G，结论的模型规模泛化性未知。
    - 缺少与其他自监督方法（如MoCo v3、CLIP）的对比。
- **资源限制**：未报告算力消耗，且因资源无法测试ViT-G，可能遗漏更大模型的不同行为。
- **潜在偏差**：线性探针的训练可能引入优化偏差，尽管通过多初始化稳定性测试降低了此风险，但未彻底排除（如不同优化器、学习率对子空间的影响）。
- **应用限制**：框架依赖收敛的线性探测器，要求任务有明确的低维输出空间（深度bins、类别等），对于回归任务需离散化，可能影响子空间解释性。

（完）
