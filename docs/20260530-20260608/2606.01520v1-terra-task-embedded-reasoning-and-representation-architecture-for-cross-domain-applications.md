---
title: "TERRA: Task-Embedded Reasoning and Representation Architecture for Cross-Domain Applications"
title_zh: TERRA：面向跨领域应用的任务嵌入推理与表征架构
authors: Shayan Shokri
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01520v1"
tags: ["query:world-models"]
score: 9.0
evidence: 动作条件化潜在世界模型
tldr: 跨域结构化状态表示与预测的迁移问题缺乏理论验证。TERRA通过将每个域建模为受控马尔可夫过程，分解为薄域适配器和共享领域不变核心，并引入近似MDP同态、松弛互模拟差异以及Gromov-Wasserstein距离来度量跨域对应性。在Lipschitz预测器假设下，导出迁移误差上界（与源误差和结构失配相关，预测步长几何增长）以及基于互模拟度量的Lipschitz价值性质将潜误差与决策遗憾关联。提出结构化状态迁移假设及预注册实验（从驾驶场景到订单簿的迁移测试），将直觉转化为可检验理论。
source: arxiv
selection_source: fresh_fetch
motivation: 现有跨域迁移依赖直觉，缺乏形式化理论；需要明确何时结构化状态表示和预测器可跨域迁移，并量化迁移损失。
method: 将各域建模为带分阶潜网格的受控马尔可夫过程，分解为薄域适配器和共享核心；利用近似MDP同态、松弛互模拟差异和Gromov-Wasserstein距离定义跨域对应性及迁移界限。
result: 导出迁移误差上界（源误差与结构失配的累加，预测步长指数增长）并连接潜误差与决策遗憾；提出可证伪的结构化状态迁移假设。
conclusion: TERRA为跨域结构化状态迁移提供形式化理论框架和可检验假设，有望在驾驶、机器人、金融等不同领域的表示学习与决策中建立统一理论。
---

## 摘要
单一的动作条件潜在预测架构原则上可以在驾驶场景、机器人工作空间或金融订单簿的结构化状态上进行训练。在任何单一领域内实现这一目标所需的要素已经存在并已得到独立验证：掩码潜在预测、动作条件潜在世界模型、离散动作标记化以及体素化状态上的联合嵌入预测。尚未确立（而TERRA正致力于解决）的是迁移问题：在一个结构化状态领域学到的表征或预测器何时能够迁移到结构类似但其他方面无关的领域，以及迁移的程度如何。我们对此问题进行了形式化处理。我们将每个领域建模为分级潜在网格上的受控马尔可夫过程，将任何实例分解为薄领域适配器与共享的领域不变核心，并通过近似马尔可夫决策过程同态来识别跨领域对应关系，其质量通过宽松的互模拟差异来衡量，对于缺乏共享坐标系的领域，则通过其动作条件转移算子之间的Gromov-Wasserstein距离来衡量。在Lipschitz预测器下，我们推导出一个迁移界，该界将源模型误差与结构失配分离，在预测视界上呈几何增长，并由Gromov-Wasserstein距离从下方认证；随后，我们通过互模拟度量的Lipschitz值性质将潜在误差与决策遗憾联系起来。由此产生的结构化状态迁移假说被表述为一个可证伪的主张，并附有预先注册的实验程序，核心是从驾驶场景到订单簿的迁移测试，包括其被反驳的条件。我们未提供经验结果：这是一份将广泛重复的直觉转化为可检验理论的研究提案。

## Abstract
A single action-conditioned latent predictive architecture can in principle be trained on the structured state of a driving scene, a robot workspace, or a financial order book. The ingredients for doing so within any one domain already exist and are individually validated: masked-latent prediction, action-conditioned latent world models, discrete action tokenization, and joint-embedding prediction on voxelized state. What is not established, and what TERRA addresses, is the transfer question: when does a representation or predictor learned in one structured-state domain carry over to a structurally analogous but otherwise unrelated domain, and by how much. We give this question a formal treatment. We model each domain as a controlled Markov process on a graded latent grid, factor any instantiation into thin domain adapters and a shared domain-invariant core, and identify a cross-domain correspondence with an approximate Markov decision process homomorphism whose quality is measured by a lax bisimulation discrepancy and, for domains lacking a shared coordinate system, by a Gromov-Wasserstein distance between their action-conditioned transition operators. Under a Lipschitz predictor we derive a transfer bound that separates source-model error from structural mismatch, grows geometrically in the prediction horizon, and is certified from below by the Gromov-Wasserstein distance; we then connect latent error to decision regret through the Lipschitz value property of bisimulation metrics. The resulting Structured-State Transfer Hypothesis is stated as a falsifiable claim with a preregistered experimental program, centered on a transfer test from driving scenes to order books, including conditions under which it is refuted. We present no empirical results: this is a research proposal that converts a widely repeated intuition into testable theory.

---

## 论文详细总结（自动生成）

# 论文详细总结：TERRA: Task-Embedded Reasoning and Representation Architecture for Cross-Domain Applications

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：动作条件潜在预测架构在单一领域（如驾驶场景、机器人工作空间、金融订单簿）已取得成功，但跨领域迁移（例如从物理场景到非物理结构化系统如市场微观结构、电网）的“迁移问题”从未得到正式检验。现有工作要么是领域内结果，要么依赖直觉断言“相同的架构/表征可以跨领域复用”，却缺乏量化理论支持和实验验证。
- **核心问题**：在一个结构化状态领域学习到的表征或预测器，何时以及多大程度上能够迁移到结构类似但其他方面无关的领域？
- **整体含义**：TERRA 旨在将这种广泛流传的直觉转化为可检验的数学理论，提供一个形式化框架（包括定义、度量、边界定理）和一个可证伪的假说（SSTH），并设计了明确的实验程序来检验该假说。论文本身是一篇**研究提案**，不包含任何经验结果。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将每个结构化状态决策域建模为**分级潜在网格上的受控马尔可夫过程**。
- 任何实例被分解为**薄领域适配器**（domain adapters：编码器前端和动作标记器）和**共享的领域不变核心**（shared core：上下文编码器、目标编码器、动作条件潜在预测器）。
- 跨领域对应关系被识别为**近似马尔可夫决策过程（MDP）同态**（homomorphism），并通过**松弛互模拟差异**（lax bisimulation discrepancy）和**Gromov-Wasserstein距离**来量化其质量。
- 在Lipschitz预测器假设下，推导出预测迁移上界，该界将源模型误差与结构失配分离，随预测视界几何增长，并由Gromov-Wasserstein距离提供下界认证。
- 通过互模拟度量的Lipschitz值性质，将潜在误差与决策遗憾联系起来。

### 关键技术细节
- **结构化状态域定义**：元组 D = (S, A, T, r)，其中状态空间 S 是具有分级网格表示的可测空间；动作空间 A 具有离散标记化 τ: A → V*；转移核 T: S × A → P(S)；奖励信号 r: S × A → R。
- **领域不变接口**：
  - 域适配器：编码器前端 eD: S → Z（将网格映射到规范潜在空间 Z）和标记器 τD。
  - 共享核心：上下文编码器 fθ（掩码预测），指数移动平均目标编码器 f̄θ，动作条件潜在预测器 gϕ: Z × V* → Z。
- **跨域对应与度量**：
  - 定义对应 Φ = (ϕZ, ϕA)，其中 ϕZ: Z1→Z2，ϕA: V1*→V2*。
  - 转移缺陷 ε_T(Φ) = sup_{z,a} W(ϕZ♯T1(·|z,a), T2(·|ϕZ(z),ϕA(a)))
  - 奖励缺陷 ε_r(Φ) = sup_{z,a} |r1(z,a) - r2(ϕZ(z),ϕA(a))|
  - 松弛互模拟差异 ε_lax(Φ) = c_r ε_r + c_t ε_T
- **假设**：
  - 假设1：状态映射 ϕZ 是 Lϕ-Lipschitz。
  - 假设2：源域训练的核心实现潜在核 ĝ(·|z,a) 单步误差 ≤ δ。
  - 假设3：传输后的单步预测器在潜在参数上是 L-Lipschitz（在Wasserstein距离下）。
- **关键定理**：
  - 命题1（单步分解）：传输预测器在目标域上的单步误差 ≤ Lϕ·δ + ε_T(Φ) = ε_1。
  - 命题2（视界界）：滚动传输核心 H 步，累计误差 Δ_H ≤ ε_1 * (L^H - 1)/(L - 1) (L≠1)；当 L≤1 时误差保持 O(ε_1 H)，当 L>1 时几何增长。
  - 命题3（结构性下限）：若 ϕZ 是等距嵌入，则 GW(D1,D2) ≤ 2 ε_lax(Φ)，即任何对应都不能使松弛差异低于 GW/2。
  - 推论1（价值迁移）：作用于传输模型上的价值次优性 ≤ (L_e/(1-γ))·Δ_H，其中 L_e 是潜在空间到互模拟度量的Lipschitz常数。
- **结构化状态迁移假说（SSTH）**：对于可接受域 D1, D2，从 D1 训练的核心复用到 D2 时的迁移增益是 inf_Φ ε_lax(Φ) 的递减函数，其下界为 GW(D1,D2)/2。当下界很大时，核心复用无优势甚至可能降低性能。

### 算法流程（文字说明）
1. 定义结构化状态域（状态、动作、转移、奖励）。
2. 构建域适配器（编码器+标记器）和共享核心（上下文编码器、目标编码器、预测器）。
3. 在源域训练核心：最小化掩码潜在预测损失 + 动作条件轨迹预测损失。
4. 估计两个域之间的对应 Φ（通过对齐问题求解，初始化用Gromov-Wasserstein耦合）。
5. 计算松弛互模拟差异 ε_lax 和 Gromov-Wasserstein下界。
6. 将源域核心传输到目标域：通过 ϕZ、ϕA 映射，复用共享核心（可冻结或微调）。
7. 根据命题2和推论1计算预测误差界和价值次优性界。

## 3. 实验设计

**论文无任何已运行实验**，仅提出预注册的实验程序，分为四个阶段：

- **Phase 1（域内验证）**：在合成驾驶/导航（CARLA, Habitat）上训练 TERRA 核心，确认掩码潜在预测和短期预测优于基线（无对比具体方法列表）。
- **Phase 2（近迁移）**：在驾驶体素数据集（nuScenes, Waymo, KITTI）上预训练，迁移到室内操作和导航（Open X-Embodiment）。预期差异小，SSTH 预测强迁移。
- **Phase 3（远迁移，决定性测试）**：从驾驶体素迁移到限价订单簿预测（公共市场数据）。测试三类复用：表征复用、小样本核心适配、预测器复用。
- **Phase 4（差异校准）**：估计 ε_lax 和 Gromov-Wasserstein下界，绘制迁移增益 vs 差异 vs 视界曲线。如果迁移增益随差异递减、随视界衰减且速率符合 Lipschitz 常数，则支持假说。

**提出的基准**：比较从零训练的模型与复用预训练核心的模型在目标域上的性能（线性探测准确率、中心核对齐、样本效率差异、预测误差等）。未列出具体基线方法名称。

## 4. 资源与算力

- **文中未提及任何实际算力需求**（无 GPU 型号、数量、训练时长等信息）。作为研究提案，这一点明确标注。
- 论文只在学术规范部分提及“未运行任何实验”，因此算力部分完全空缺。

## 5. 实验数量与充分性

- 论文本身**没有做任何实验**，所有实验均为计划。因此不存在已完成的实验数量或消融实验。
- **充分性评估**：提出的实验程序覆盖了域内验证、近迁移、远迁移、差异校准四个阶段，包含三类复用（表征、核心微调、预测器），并预设了四个可证伪预测（P1-P4）及 falsification 条件（见第9节）。逻辑链条完整，但尚未执行，无法评价实际充分性。

## 6. 主要结论与发现

- **论文未报告任何经验发现**。
- 主要结论是理论性质的：将跨域迁移问题形式化，推导出一组误差边界（单步、视界、价值遗憾），并提出可证伪的结构化状态迁移假说（SSTH）。贡献在于将模糊直觉转化为可检验的理论框架。

## 7. 优点

- **理论严谨性**：借用并整合了 MDP 同态、互模拟度量、Gromov-Wasserstein 距离等成熟数学工具，将跨域迁移分解为源误差和结构失配两部分，边界清晰。
- **可证伪性**：提出 SSTH 并明确列出反驳条件（迁移增益与测得的差异无关、低差异对无迁移、高差异对强迁移等），符合科学方法论。
- **可操作性**：定义了明确的域适配器/核心接口，使得理论边界可以与实际架构（如 TERRA 子体）对接；实验程序给出了具体数据集和评估指标。
- **通用性**：覆盖从物理场景到非物理结构化系统（如金融订单簿）的迁移，具有跨学科潜力。

## 8. 不足与局限

- **无经验结果**：论文是纯研究提案，所有结论均为理论推导，未经历任何实验验证。实际迁移效果、边界松紧度、Lipschitz 假设的成立性均未知。
- **估计跨域差异本身困难且噪声大**：对于非常不同的域（如驾驶 vs 订单簿），ε_lax 和 Gromov-Wasserstein 距离的实际估计可能不准确，削弱检测能力。
- **命题3的严格性依赖于等距归一化**：一般情况需估计 bi-Lipschitz 常数，增加了实际应用复杂性。
- **可接受性限制**：并非所有决策问题都天然具有网格结构，强制施加网格可能破坏结构本质。
- **子体继承风险**：TERRA 子体（掩码+轨迹预测）可能存在表征坍塌、分布偏移等问题，假设3（Lipschitz 动力学）可能只在局部成立。
- **实验设计缺乏对比基线细节**：未列出具体的基线方法（如 Dreamer、DINO-WM 等现有世界模型）进行公平比较；样本效率的“多少个百分比”没有具体数值。

（完）
